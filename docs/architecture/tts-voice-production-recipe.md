# 自托管 TTS 音色生产配方（VoxCPM2）

> **用途**：给某个语言产出「讲解员音色」并批量生成藏品音频的完整配方。
> **适用**：新增语言（pt/ru/da/sv/no/fi…）、更换模型、复现现有 10 语言。
> **来源**：2026-07~08 在 Mac mini M4 上的完整实验，全部阈值由用户逐轮耳测标定。
> **不适用**：云端 tts-1 链路（那条链路见 museum-api-contract.md 的音频端点章节）。

---

## 一、目标与验收标准

替代云端 tts-1 做**批量预生产**（Top-N 藏品音频落 R2），降低模型成本。
音频要达到 4 条硬要求（用户定义，按重要性排序）：

| # | 要求 | 量化判据 | 阈值来源 |
|---|---|---|---|
| ① | 同语言音色一致 | 所有藏品克隆同一个种子 | 实测两藏品克隆同种子 F0 差仅 1% |
| ② | 必须女声 | F0 165-205Hz **且** 亮度 ≥2250Hz | F0 145 亮度 2470 仍被听成男声；亮度 1854 也听成男声 |
| ③ | 无「丝丝」破音 | **谐波锐度 < 200%** | 用户认可 138-181%；判「太严重」的 es 是 326% |
| ④ | 语速对齐 tts-1 | 时长差 ≤7% | it 快 6 秒可接受；快 15-20 秒不可接受 |

---

## 二、核心架构（两层）

```
第一层：种子（全局，只有 2 个，永久固定）
  seeds_final/SEED_zh.wav   ← 中文种子
  seeds_final/SEED_en.wav   ← 英文种子

第二层：藏品音频（每语言每藏品）
  文本 → 用 zh 和 en 各克隆一次 → 三关质检 → 按锐度择优 → atempo 对齐时长 → 96k mp3
```

### 语速对齐：生产时没有 tts-1 参考时长，按「字/秒」算

**选型阶段**大家念同一篇《草地上的午餐》，直接对齐到 tts-1 的秒数即可。
**生产阶段**每件文本不同，没有现成参考 —— 必须按该语言的字/秒基准算目标时长：

```python
RATE = {"zh": 4.64, "en": 15.84, "fr": 15.61, "de": 15.67, "es": 15.87,
        "it": 15.78, "pl": 14.86, "ja": 5.47, "ko": 7.31, "zh-hant": 4.71}
target = len(text) / RATE[lang]
atempo = clamp(actual / target, 0.75, 1.15)
```

（该表来自 tts-1 在 Q152509 上的实测：字数 ÷ 时长，CJK 与拉丁都按字符计。）

> 🔴 **这一步漏掉过，而且没有任何下游能兜住。** 2026-08 橘园试点 28 条 guide
> 全部偏快，平均 **+15%**（fr +23%、es +18%、en +8%），远超验收线 ④ 的 ≤7%。
> 原因是生产脚本只做了「生成 → 转码」，把选型阶段的对齐环节丢了。
> **后端质量闸抓不到**：它的 `CHARS_PER_MIN` 用的是同一个基准，但放宽到
> `ratio ∈ [0.45, 2.2]` 只抓数量级错误（截断／复读），+15% 稳稳通过。
> **语速对齐必须在生成侧做完，别指望落库侧兜底。**

**实测 atempo 不降音质，反而更好**（28 条全量，0 条劣化）：

| | 修正前 | 修正后 |
|---|---|---|
| 语速偏差 | +7.8% ~ +22.8% | **全部 +0.0%** |
| 谐波锐度 | 92~235 | **79~210（全降）** |
| HNR | 13.1~19.9 | **14.3~21.7（全升）** |
| F0 | — | 变化 ≤2Hz（atempo 只伸缩时间，不改音高） |

亮度会掉 2-5%（es 2321→2222，跌破 2250 参考线 1%），但 F0 未动，
定稿基准批走的是同一处理且用户听测认可。

### 为什么是「两个种子通吃」而不是「每语言一个种子」

这是本配方最重要的发现。**噪声来自「用该语言自己的种子念该语言」这个组合，不是语言固有属性**：

| 语言 | 自己的种子 | 用 ZH 种子 | 用 EN 种子 |
|---|---|---|---|
| es | 326%（太严重） | **92%** | 150% |
| pl | 217% | **100%** | 142% |
| fr | 167% | **74%** | 137% |
| de | 175% | **103%** | 179% |
| it | 161% | **98%** | 134% |
| ko | 178% | **86%** | 130% |

反直觉但稳定：**克隆输出比种子本身更干净**（种子 SEED_zh 自身锐度 168%，克隆出的法语只有 74%）。原因推测：克隆时模型重建语音，保留了种子的「人格特征」（F0/共振峰），但没有复制其噪声成分。

### 语系例外（必须遵守）

| 目标语言 | 可用种子 | 原因 |
|---|---|---|
| 拼音文字（fr/de/es/it/pl/ko…） | **zh 优先**，en 备选 | zh 种子音质最优，无字形干扰 |
| **zh-hant** | **zh**（2026-08-31 纠正） | 见下方「一桩冤案」 |
| **ja** | **en** | 实测 EN 一致性 0.980 > ZH 0.963——ZH 种子浊音念错（「じっくり」→「つっくり」）。ZH 锐度更好（103% vs 126%）但**发音准确度优先** |
| zh / en 自己 | 用自己的种子 | — |

#### 一桩冤案：「汉字圈禁用 zh 种子」是坏工具制造的

原规则写着「zh 种子念 zh-hant 一致性仅 0.58（「仔細觀賞」→「使戏观少」）」，
据此把 zh-hant 判给了 en 种子。**那个 0.58 出自还坏着的一致性打分函数**
（`autojunk=True` + 未做简繁归一化，见 §四）。用修好的函数重测同一条件：

| zh-hant | 一致性 | 锐度 | 生成耗时 |
|---|---|---|---|
| **ZH 种子** | **0.951** | **144%** | **148s** |
| EN 种子（原定稿） | 0.913 | 189% | 187s |

**ZH 三项全胜。** 转写实证更直接——EN 种子把「花點時間仔細欣賞」念成了
「哈点时间十岁新手」，**读错字的恰恰是 en 种子**；用户听感「繁体发音很怪」即源于此
（英文种子念中文＝外国腔）。用户 A/B 听测确认 ZH 版「确实好很多」。

> 🔴 **教训（比这条规则本身重要）**：判定工具修好后，不能只想着「以后重跑全量」，
> **还要回头重验那些当初由它产出、已经写进文档当成定论的旧结论**。
> 坏工具的污染会沉淀成「常识」，之后没人再去质疑它。
> 配套：**为绕过某个坏结论而加的补丁，在结论纠正后必须一并撤掉** ——
> 这里就撤了 zh-hant 的两个放宽门槛（锐度 215 / 一致性 0.88），
> 那是为迁就 en 种子的差表现才加的；换回 zh 种子后通用门槛绰绰有余。

> 韩语无此问题（现代韩语用谚文，非汉字），一致性 0.93。

---

## 三、参数（定死，不要改）

> 🔴 **红线：任何加速方案都不得以降低音质为代价。**
> 音色基线（锐度/一致性/音高）是花了大量轮次调出来的，牺牲它去换速度是负收益。
> 加速前必须先做「只改一个变量」的对照实验，三项指标全部持平或更好才算通过。
> *反面案例*：把扩散步数从 10 降到 5，看似快（单步 5.4 it/s vs 4.0），实测 **内容一致性从 0.96 崩到 0.60（读错内容）**，且因触发模型内置 badcase 重试，**总时间反而更长**（RTF 1.84 vs 1.81）。步 7 同样双输（RTF 2.05、锐度 148%）。**步数必须保持 10。**

```python
CFG_VALUE = 1.7          # 语速主旋钮。1.3 太慢拖沓，2.0 偏快，1.1 直接崩坏成复读
INFERENCE_TIMESTEPS = 10 # 加大到 25/40 对音质无改善，只慢 3 倍
DENOISE = False          # 种子已干净
CLONE_MODE = reference_wav_path   # ultimate 续写模式（prompt_wav+prompt_text）实测无增益
BITRATE = "160k"         # 🔴 必须 160k。落库侧质量闸用「字节数 ÷ 比特率」估时长
                         # （刻意不引 ffmpeg，要跑几万次），该常量硬编码 160000。
                         # 本文档原写 96k，与生产脚本、质量闸**都不一致**：照文档
                         # 复现会让估算时长虚高 1.67 倍 → 韩语 ratio 掉到 0.44
                         # （门槛 0.45）直接被拒。见契约「音频批量生产与灌入」③。
ATEMPO_RANGE = (0.75, 1.15)   # 0.75 用户听不出拉伸；0.56 明确否决「明显拉长」
```

---

## 四、质检指标（三关 + 两个参考维度）

### 硬门（不过就重抽/换种子）

| 指标 | 阈值 | 怎么算 | 抓什么问题 |
|---|---|---|---|
| **谐波锐度** | < 200%（全语言统一） | `librosa.decompose.hpss` 后 `P.sum()/H.sum()*100` | **丝丝/破音**（最关键） |
| **内容一致性** | ≥ 0.90 | Whisper 转写 vs 原文 `SequenceMatcher(…, autojunk=False)` | 漏读/读错/**复读崩坏** |
| **时长** | 差 ≤ 7-10% | ffprobe vs 该语言 tts-1 基准 | 语速 |

> 🔴 **`autojunk=False` 不是可选项。** difflib 默认 `autojunk=True`：序列 ≥200 个
> 元素时，把出现频率 >1% 的元素当噪声丢弃。1116 字的法语文本，1% 门槛只有 11 次，
> 几乎每个字母都超 —— 绝大多数字符被丢掉，分数崩一半。实测同一对文本：
> **`autojunk=True` 给 0.5027，`autojunk=False` 给 0.9946（真值）**。
> 后果不是「分数不准」，而是**合格音频被判失败 → 重试 3 次 → 仍标 ok=False**，
> 那段内容永远不会有音频，同时烧掉 3 倍算力。3 万段规模上是灾难级的。
>
> 归一化三步（繁→简、去变音符、只留字母数字）同样缺一不可，
> 它们各自对应一类实测误判，见 `run_batch.py:_norm` 的注释。
>
> ⚠️ **质检脚本本身必须用已知正／负样本验证判断力**（`test_consistency.py`）：
> 一个失灵的检查比没有检查更糟 —— 它一边烧算力，一边把合格内容永久标成失败，
> 而且**看起来在正常工作**。上面两个 bug（autojunk、少了归一化）都是这么潜伏下来的。
>
> 🔴 **修好检查工具后，必须用新工具重跑全量，不得手工放行旧结果。**
> 2026-08 橘园试点的真实翻车：发现归一化 bug 后，看到 4 条低分（es 0.37 / es 0.60 /
> de 0.66 / it 0.85），就统一标注「旧算法误判，归一化后合格」放行。事后用修好的
> 工具重测：前三条真值 0.99+（确实是误判），**但 de 那条真值只有 0.815 —— 音频真的
> 崩坏了**，末尾把「er hat sich in der Welt vermittelt」复读了十几遍，就这么进了 prod。
> 教训是：**一个笼统的解释不能覆盖所有异常**。工具坏了的正确反应是修工具重跑，
> 而不是用人工判断替工具下结论 —— 后者恰好在最需要工具的地方失去了工具。

### 参考维度（辅助判断，不单独卡）

| 指标 | 参考区间 | 说明 |
|---|---|---|
| F0 音高 | 165-205Hz | <150 听成男声，>210 尖锐 |
| 亮度（频谱质心） | ≥2250Hz | 太暗会听成男声（意语翻车案例：F0 195 但亮度 1854 → 男声） |
| 低频 80-300Hz 占比 | ≥35-40% | 用户说的「浑厚」就是它；<20% 会被判「薄/公鸭嗓」 |
| HNR 谐噪比 | ≥ 该语言 tts-1 值 | tts-1 基线：zh12.8 en12.5 de12.5 es13.1 pl12.1 it12.5 fr13.7 ja14.5 ko14.6 zh-hant13.1 |

> ⚠️ **HNR 不是丝丝的判据**——我们的 es 版 HNR（13.9）比 tts-1（13.1）还高，但丝丝更严重。真正的判据是锐度。

### 规模实测分布（橘园 TOP7-15，490 条 × 10 语，2026-09-04）

前面的阈值是小样本定下来的。这是第一次有 490 条的分布可看，结论按语言分化得很清楚。

| 语言 | 首次未过 | 锐度中位（门槛 200） | 一致性中位（门槛 0.90） |
|---|---|---|---|
| **en** | **9/39** | **176** | 1.00 |
| **ja** | 4/23 | **145** | **0.93**（p10 = 0.78） |
| zh / zh-hant | 0/8 | 122 / 122 | **0.95 / 0.94** |
| fr / es / it / de | 0-3/39 | 98-110 | 0.99 |
| ko / pl | 0/8 | 89-107 | 0.99-1.00 |

**① 英语是锐度瓶颈，短片段尤甚。** en 中位 176 距门槛只剩 24 点，整个语言贴着门槛跑。
按时长分组更极端：**<20s 的英语均值 223（已超门槛）**，20s 以上是 172-181。
en 和 ja 恰好是仅有的两个用 `SEED_en` 的语言，也恰好是锐度最差的两个 —— 与 §二
日语 A/B（ZH 种子 103% vs EN 种子 126%）方向一致。
**对照组已于 2026-09-07 补做，结论是「不换」** —— 见下方「英语种子重选实验」。

#### 英语种子重选实验（2026-09-07）：负结果，但两个副产品比结论有用

起因就是上面这条：英语贴着门槛跑，首次未过 23%，想换个更干净的种子省重试成本。
做了两件事，**都指向不换**。

**A. 英语改用 `SEED_zh`？—— 锐度赢了，音色输了。**
四段真实英语正文各配对生成（同会话、同参数、无条件单抽）：

| 样本 | `SEED_en` 锐度 | `SEED_zh` 锐度 | 距 `完美_en` 指纹距离 |
|---|---|---|---|
| Q19820384/facts（16s） | 205 | **193** | 4.09 / 5.80 |
| Q28797618/guide（68s） | 176 | **164** | 2.84 / 5.26 |
| Q137697498/guide（69s） | 208 | **165** | 3.18 / 3.94 |
| Q19820384/guide（81s） | 219 | **95** | 6.65 / 6.93 |

**锐度 ZH 四比零全胜，指纹距离 EN 四比零全胜。** 两把尺子完全对立。
原因看基频就够了：`完美_en` F0=160、`完美_zh` F0=178，而 `SEED_zh` 念英语出来是
174/181/182 —— **它不是更干净的英语，是中文那把嗓子在说英语**。→ 英语保持 `SEED_en`。

**B. 重抽一条更干净的英语种子？—— 抽到了，但用户听着更差。**
翻 `design_direct.py` 发现根因：**当初选种子的判据只有「女声 F0≥140 + 时长贴近 tts-1」，
锐度根本没进判据**（锐度门槛是后来生产阶段才立的）。且每语言只抽了 3 次。
同 DESIGN 描述、同一份《草地上的午餐》英文导览词重抽 12 次，补上锐度判据：

| 候选 | 种子锐度 | **克隆成品锐度** | 成品 F0 | 距 `完美_en` | 用户听感排名 |
|---|---|---|---|---|---|
| r01 | **93.7** | 161.7 | 142 | 6.94 | **5（最差）**「完全换了个人」|
| r10 | 113.5 | 202.5 | 153 | 6.50 | 3「比较亮，一点点丝丝声」|
| r12 | 122.1 | 210.2 | 151 | 7.73 | 4「丝丝声更重」|
| r08 | 120.1 | 201.3 | 135 | **5.75** | **2**「接近现用，能接受」|
| **`SEED_en`（现用）** | 132.1 | **183.3** | **159** | 0（靶心） | **1（最好）** |

12 次里有 3 条种子比 `SEED_en` 干净，**但没有一条的成品让用户更满意**。

**副产品①：种子锐度不能当选种子的判据 —— 它甚至反向预测用户偏好。**
本轮的主判据就是「种子锐度最低」，它选出的 r01 正是用户排最后的那条。
种子分数对成品也只是弱代理：偏移 +51~+89 不稳定，排序在 r08/r10 之间就乱了
（r08 种子更毛、成品反而更干净）。**筛候选别再用它。**

**副产品②：指纹距离能预测用户偏好，锐度不能。**
用户听感排序与「距 `完美_en`」排序**前三名完全一致**（0 < 5.75 < 6.50），仅末两名对调；
而锐度最好的那条被排在最后。分工是：**锐度只测「丝丝声」这一个分项**
（它在 r10 202.5 vs r12 210.2 之间确实判对了用户听到的「丝丝声更重」），
**决定喜不喜欢的是音色身份，那要用指纹距离对标一条耳朵认可的样本**。
→ 以后做选型：先用指纹距离粗筛，再交耳朵定，别拿锐度当总分。

**⚠️ 两个把自己绊倒的坑，都是「自定阈值」：**
- `F0≥140` 的女声门槛把 r08（F0=136）判成男声，而**用户耳朵判它是女声，且是最满意的候选**。
  代码注释自己写着「男声通常<130」—— 130~140 是无人区，不该由脚本替耳朵下判决。
- 拿本次单抽结果去对配方里记录的生产旧值（166/184/166）做「环境校验」，
  三次全部偏高（+6%/+13%/+32%）。**不是环境漂移，是校验设计错了**：
  生产值是 `MAX_TRY=3` 重试后的**过闸幸存者**（条件分布），
  单抽是**无条件分布**，两者本就不可比。
  → **凡是拿"生产历史值"当基线，先问它是不是被某道闸筛过。**

**C. 顺带清掉 ja 的旧账。** ja 用 `SEED_en` 的唯一依据是「ZH 种子浊音念错」，
而当初把关那条决策的一致性函数**恰好在浊音维度上是瞎的**（见 §六）。
用修好的函数重测：**EN 0.983 / ZH 0.970**（当初记录 EN 0.980 / ZH 0.963），
方向与幅度均未变 → **ja 保持 `SEED_en`，该决策不受那个洞影响。**

**② 不要给短片段放宽锐度门槛 —— 这个方向已被实测否掉。**
自然的猜想是「短片段辅音占比高 → 尺子读数虚高 → 冤枉了它们」。
拿同一条音频截成不同长度实测，**方向是反的**：

| 样本 | 截 15s | 截 30s | 全长 |
|---|---|---|---|
| Q137697498/en/guide | 152 | 151 | 184（69s） |
| Q19820384/en/guide | 146 | 166 | 166（81s） |
| Q28797618/en/guide | 154 | 149 | 166（68s） |

尺子对短片段读数**偏低**。所以那些测出 223-313 的短英语问答是真的更毛，
闸拦得对，重试是必要的。放宽门槛等于专门放行本批最差的一批音频。

**③ CJK 一致性系统性偏低 —— 已定性:门槛是准的,不要调。**
分布上 ja 0.93 / zh 0.95 / zh-hant 0.94，而 **ko 0.99**；本批纯粹卡一致性的
三次全废全部是 ja / zh-hant。很自然会怀疑是 **whisper-small 的中日文转写差**
（实测确有大量同音听错：`油彩→魚祭`、`塞尚→塞上`、`青蘋果→清螃果`，
而 opencc 的繁简归一化吸收不了同音混淆）。

**做了 small vs large 对照实验，结论与猜测相反：**

| | 条目 | small | large |
|---|---|---|---|
| ❌ 未过闸 | ja/background | 0.845 | **0.891** |
| ❌ 未过闸 | zh-hant/guide | 0.895 | **0.897** |
| ✅ 对照 | ja/guide | 0.966 | **0.944** |
| ✅ 对照 | zh-hant/facts | 0.918 | **0.945** |
| ✅ 对照 | ja/facts | 0.913 | **0.955** |

**换更强的转写模型后两组分得更开,而不是更糊**：对照组升到 0.944-0.955，
未过闸组仍停在 0.891-0.897，中间空出 0.05，**0.90 正好落在空档里**。
若掉分真是尺子的锅，large 应该把未过闸组一并拉上去 —— 它没有。
而且 large 在 `ja/guide` 上反而**降了** 0.022，说明它不是普遍宽松，是真的在分辨。

→ **CJK 的 0.90 门槛不调。** 那些卡在 0.84-0.89 的条目是音频真的更差
（内容念全了但发音失准，whisper 忠实地转成了别的字），不是被判据冤枉。
⚠️ 对照组不能省：只看"未过闸组换 large 涨了 0.046"会得出相反结论。
必须有已通过的同语言样本，才知道 0.90 是不是够得着。

**④ 「三次数值不动」≠ 确定性失败，多数是强吸引子。**
`ja/analysis` 连续 4 次一致性都是 0.78、长度比 0.73，第 5 次跳到 0.95 过闸。
真·确定性的只有 `pl/significance`：**三轮独立运行、九次尝试全是 0.84 / 0.74**。
判断「要不要再重试」别只看数值稳不稳，样本太少时它们长得一样。

**④-b 那条确定性失败的机制:重复短语让对齐打滑,整段被跳过。**
把它的音频转写出来逐句比对，看得一清二楚 —— 原文第 3 句与第 4 句都含
`sztuki nowoczesnej,`，模型念到**第一个**之后直接跳到**第二个**的后半句：

```
原3  Jego prace utorowały drogę dla sztuki nowoczesnej, zachęcając przyszłe pokolenia…
原4  …to most do świata sztuki nowoczesnej, zapraszający cię do zobaczenia natury…
转3  Jego prace utorowały drogę dla sztuki nowoczesnej, ┃ zapraszający cię do zobaczenia…
```

中间整段没念，正好是丢失的 27%（长度比 0.73）。**重复位置固定 → 跳转点固定 →
换种子换次数都一样**，这就是"九次全同"的由来。重试对这类无效，
`_rejected/` 里的音频是唯一能看出这一点的材料（所以不能删）。

⚠️ **但「文本含重复短语」不能当预检指标 —— 这条假说已被证伪**：
通过的 `Q64309678/ja/guide` 有 **23 组重复**（`セザンヌ`×4），比失败的多得多。
绝大多数文本都有重复且完全正常，据此拦截只会误杀一片。
→ 目前没有灌入前的文本侧预检办法，**防线仍然是闸本身**。

**④-c 治本部分有效,但有一条硬边界:改的是正文,不是音频脚本。**
（2026-09-05，用户批准后对上面两条确定性失败做的实验。
结局:pl 救回并上 prod;**ja 绕一圈回到哑段** —— 教训在下面,比结果重要。）

`pl/significance` —— 只把第二处 `sztuki nowoczesnej,` 换成 `nowej sztuki,`
（英文原文 "a bridge to the modern art world"，语义等价）：

| | 一致性 | 长度比 |
|---|---|---|
| 原文（三轮九次） | 0.84 | 0.74 |
| 改后 | **0.989** ✅ 一次过 | **1.00** |

跳读诊断由此被反向证实：动了那一处重复，九次全同的确定性失败当场消失。

`ja/background` 就没这么干净，它的失败**不止一个原因**，得逐个拆：

| 改动 | 依据 | 效果 |
|---|---|---|
| `儚い` → `はかない` | 两个 whisper 都听成「良い」(よい)，说明 TTS 不认这个汉字 | ✅ 转录恢复正常 |
| ⛔ `197センチメートル×1690センチメートル` → `縦およそ2メートル、横およそ17メートル` | 长数字随机读错(try2 是 1690 对 197 错，try3 反过来) | 音频侧有效,**但忠实度闸判 False,已撤回** —— 见下方 🔴 |
| `著名な`→`有名な`、`先駆者`→`パイオニア`、`油彩`→`油絵` | 转录成「中名な」「咲起者」「受採」 | ✅ 中位再抬 0.011 |

分布（每版跑满，不提前退出）：

分布（每版跑满，不提前退出。⛔ 标记的版本**不忠实，不可用**）：

| 版本 | 忠实度闸 | 一致性分布 | 中位 | 单次通过率 |
|---|---|---|---|---|
| 原文 | ✅ | 六次全败，最好 0.79 | — | **0/6** |
| 只改数字（保精度拆句） | ✅ | 0.0 / 0.721 / 0.867 | — | 0/3 |
| ⛔ 数字改约数 | ❌ | 0.873 / 0.885 / 0.902 | 0.885 | 1/3 |
| ⛔ 约数 + 换三个难读词 | ❌ | 0.886 / 0.889 / 0.896 / 0.923 / 0.937 | 0.896 | 2/5 |
| **精确数字 + 换三个难读词（最终采用）** | ✅ | 0.111 / 0.866 / 0.887 / 0.891 / 0.901 | **0.887** | **1/5** |

**最终结局:走标准流程 `MAX_TRY=3` 跑,三次 0.89/0.88/0.88 全败 → 接受哑段。**
正文是忠实的、音频没有 —— 这才是正确状态。

> 🔴 **改输入必须重过它原本要过的**所有**闸,不只是你当前盯着的那道**
> （代价:一条已灌入 prod 的音频被撤回）。
> 把 `197×1690 厘米` 改成"约 2 米×17 米"确实更好念（通过率 1/5 → 2/5），
> 但那是**面向用户展示的正文**，不是音频专用脚本。重跑忠实度闸判 `False`：
> "dimensions are altered from 197 by 1690 centimeters to approximately
> 2 meters by approximately 17 meters"。
> **而这道闸根本不会自己触发** —— 手工 `UPDATE` 不走内容管线。
> 发现它靠的是人工评审提问，不是任何一道自动防线。
> - 判据：问「**这段文本原本归哪些闸管**」，不是「我改它是为了过哪道闸」。
> - 优先级：**上游内容的正确性不能拿去换下游产物的过闸率。**
>   改回精确数字后音频三次全败，**哑段是正确结局** ——
>   上游正确 + 下游缺失，好过上游被污染换来下游齐全。
> - 凡手工改 published 内容，**改完手动补跑一次该内容归属的闸**。
> （已升为契约纪律 28 的补充。）

⚠️ **别拿"改完过了一次"当结论**：数字改约数那版过了一次 0.902，
但中位只有 0.885 —— 按 `MAX_TRY=3` 算三次全废的概率还有 30%。
改措辞的收益必须跑满看分布，单次通过是运气不是效果。
（最终采用的版本单次通过率只有 1/5，三次全败正是这个概率的兑现，不是意外。）

⚠️ **存在天花板，且它不在文本里。** 能改的都改完，中位仍在 0.89 上下，
剩下的失分是 `晩年→万年`、`二本の柳→日本の柳` 这类**同音**转录差异 ——
音频念的读音是对的，是 whisper 选错了汉字。改文本救不回来，也不该继续投入。

**日语难读词清单（实证，可复用）**：生僻/文语汉字改假名（`儚い`→`はかない`）、
汉语词改外来语（`先駆者`→`パイオニア`）、改常用同义词（`油彩`→`油絵`、
`著名な`→`有名な`）。**都是不损事实的同义替换** —— 这是这张表能用的前提。
下一批日语段落若卡一致性，先照这张表扫一遍再谈重试。

> ⛔ **不要往这张表里加"数字改约数"** —— 它确实最好念，也确实过不了忠实度闸。
> 长数字读错只能靠拆句写法（`縦197センチ、横1690センチ`）降低出错率，根治不了。

**⑤ `atempo` 撞 0.75 下限 = 生成截断的先行信号。**
比长度比更早暴露：撞下限说明生成时长不足目标的 3/4，即文本没念完。
长度比闸（0.80-1.25）能兜住，但 0.82 这种擦边过的仍少念约 18% 内容。

**⑥ 退化输出率约 0.9%（4/470），且分散不聚集** —— 位置在批次的 19%/49%/54%/99%。
配合锐度按批次前后四分位无漂移（fr 104→100、es 112→99、en 180→169），
**可以排除 MPS 长跑导致的质量劣化**（崩溃是另一回事，仍需分块）。

**⑦ 锐度爆到 1e12 = 音频里没有谐波**（近似静音/噪声），分母被 `1e-9` 兜底所致。
反过来：**真·纯静音会算出锐度 0，能过锐度闸** —— 靠一致性和长度比两道兜住。
三道闸互相补位，任何一道都不是单点。

---

## 五、新增语言的标准流程

1. **建基准**：用 tts-1 为该语言生成一条参考音频（同一篇讲解词），量出时长（语速基准）与 HNR。
2. **双种子生成**：用 `SEED_zh` 和 `SEED_en` 各克隆一次（汉字圈语言跳过 zh）。
3. **自动质检 + 择优**：跑三关，按锐度最低者胜出。
4. **人耳抽查**：只需听 1-2 条，确认无口音、音色可接受。
5. **落库**：atempo 对齐基准时长 → 96k mp3 → R2。

脚本：`~/tts-lab/produce_v2.py`（单件）、`~/tts-lab/batch_worker.py`（批量骨架）。

> **成本对比**：旧流程要抽 6 次候选 + 人工听审定种子 + 反复调描述词；新流程只需跑 2 次 + 看指标 + 抽查一条。

---

## 六、已验证失败的方法（不要重试）

### 语速类

| 方法 | 结果 |
|---|---|
| 降 cfg 减速 | 1.3 无效；**1.1 模型崩坏**，输出 "makmakmak" 无限复读 |
| 调慢种子语速 | 传递系数仅 0.25——种子慢 1 秒，克隆只慢 0.25 秒，要达标得把种子拉到 2 倍慢（本身已失真） |
| 文本插停顿标点 | 只慢 3 秒（54-59s vs 目标 95s） |
| 加大扩散步数 | 无效 |
| 重度 atempo（0.56） | 用户明确否决「明显拉长感」 |
| rubberband / 相位声码器 | 同样被否决「音频效果太差」 |
| 静音插入（句间加停顿） | 用户否决「停顿很不自然，比 tts-1 差太多」 |
| **不做 atempo，指望模型自己语速对** | ❌ 实测偏快 +15%（见 §二）。VoxCPM2 原速就是比 tts-1 快，cfg 已定死在 1.7 不能再动，只能后处理 |
| **跨语言克隆** | ✅ **有效**——顺带解决，fr/pl/ko 时长首次精确对齐 |

### 音质类

| 方法 | 结果 |
|---|---|
| EQ 补低频（+7~11dB） | 补出的是「厚的破嗓子」，es 反而被听成男声 |
| EQ 提亮度 | 方向反了，越提越薄（「浑厚」要的是低频不是高频） |
| 后期降噪 afftdn | HNR 13.9→15.0 不够，锐度反升 |
| 描述词要求「纯净无气声」 | **反效果**，HNR 掉到 7.7-9.9 |
| 加大扩散步数 | 无效 |
| 换该语言自己的其他候选 | es 池内 23 个候选 HNR 最高才 13.5，全部不达标 |
| 英语改用 `SEED_zh` 降锐度 | 锐度 4/4 胜，但指纹 4/4 负、F0 漂到中文种子的 178 —— **中文嗓子说英语**，否决 |
| 重抽 12 条英语种子挑最低锐度 | 抽到 3 条更干净的种子，**成品没有一条用户更满意**；最干净那条被排最后 |
| **跨语言克隆** | ✅ **有效**——锐度 326%→92% |

### 质检工具类（工具坏了，比没工具更糟）

| 问题 | 症状 | 修法 |
|---|---|---|
| `SequenceMatcher` 默认 `autojunk=True` | 内容完全正确的转写得 0.50 分（真值 0.99），触发无谓重试且最终判失败 | `autojunk=False` |
| 比对前不做归一化 | 繁体音频被 Whisper 转成简体 → 0.50；`Tómate` vs `Tomate` → es 真值 1.00 被算成 0.53 | 繁→简 + 去变音符 + 只留字母数字 |
| 用合成样本验证打分函数 | 高度重复的假文本掩盖 autojunk 效应，测不出 bug | **用真实的长文本样本**（≥200 字符）|
| 归一化把**音位**当装饰删掉 | 日语浊音读错对一致性检查**完全隐形**：`アメデオ` vs `アメテオ`、`ばば` vs `ぱぱ` 都得 **1.000**（NFKD 把 デ 拆成 テ+U+3099，浊点是 combining 字符，被②一并删掉） | 保留 U+3099/U+309A 后**定点**合回假名；⚠️ 不能用全局 NFKC，那会把谚文 Jamo 也合成音节、连带改掉 ko 的分数 |

| 并发拉 R2 校验（12 路） | **截断响应**：非空、`curl --fail --retry` 不报错，md5 自然不同 → 伪装成「内容不符」，190 条误报 4 条；并行写同一结果文件还丢了 5 行 | 并发 ≤4；**大小 + md5 两个量一起比**（截断必然大小也对不上）；每条结果独立落盘，别多进程 append 同一文件 |
| `_state.json` 失败条目的分数**跨尝试错位** | `sharp`/`consist` 取「锐度最优的那次」，`lenratio`/`atempo` 却是循环残留的最后一次值，而 `_rejected/` 里的文件是最后一次的产物 —— 三方来自不同尝试。**只在失败条目上错**（成功条目 `break` 后三者同源），所以整批看起来完全正常 | 顶层统一取 `tries[-1]`（与磁盘文件同源），并把每次尝试的原始分数留在 `tries` 里；见 `tools/tts/batch_record.py` |

> 🔴 **这个盲点的要害不在日语，在于它瞎的位置。** 配方定 ja 用 EN 种子的**唯一理由**
> 就是「ZH 种子浊音念错（じっくり→つっくり）」—— 也就是说，**把关那条决策的自动闸门，
> 恰好瞎在那条决策所依据的维度上**。当初能发现是靠人工逐字看转写，不是靠这个函数。
> 修好后用它重跑橘园第二批 13 条 ja：最大变化 -0.009，**零条新暴露问题** ——
> 说明这个洞在那一批没造成漏放，但它一直是个洞。
> 推广：**为 A 语言写的归一化规则，套到 B 语言上删掉的可能是音位而不是噪声。**
>（已升为契约纪律 25。）

> ⚠️ **同一个根因栽了两次**：上一批是**空响应**（R2 偶发 connection reset，裸 `curl -s`
> 返回空体，md5 `d41d8cd9…`），这一批是**截断响应**。两次都表现为「内容不符」。
> 要害是**校验管道自己的失败模式，和它要检出的失败，在输出上长得一模一样** ——
> 与契约纪律 ⑩「指标要能区分你要区分的那两种失败」是同一条，只是从「指标选错」
> 扩展到「取数环节坏了」。凡是"校验失败"必须先能区分「取不到/取坏了」和「真不符」。

> 🔴 **记账错位的危害不是脏数据，是误诊。** `ja/background` 的 state 记
> `consist=0.06`，拿 `_rejected/` 里那个文件重算实为 **0.79** —— 前者读作
> 「内容彻底跑偏、无解」，后者读作「边缘失败、改措辞可能有救」，结论完全相反。
> 差点据此把一条其实能救的段落判了死刑。
> 推广：**任何"多次尝试折成一条记录"的地方，折下来的数字必须与留在磁盘上的那份
> 产物同源**，且原始的逐次计数不能丢 —— 坍缩成单个数字后，没人能倒推它错在哪，
> 包括写它的人。（与契约纪律"小样本比例必须保留分环节原始计数"同源。）


### 模型类

| 方法 | 结果 |
|---|---|
| Chatterbox Multilingual | ❌ 淘汰：RTF 6.0（VoxCPM2 是 2-3.5）、**40 秒硬截断**导致跳读乱序（覆盖率仅 28%） |
| VoxCPM2 官方修语速 | 指望不上：Issue #56（2025-10 提，已关闭未实现）、#297（2026-05 提，零回复） |

---

## 七、运行环境注意

- **必须串行**：Mac 24GB 统一内存装不下两个 VoxCPM2 实例（各约 8GB + 推理开销），并行会让后启动的进程卡在预热阶段。
- **必须分块换进程**：单进程连续跑会 MPS 显存耗尽崩溃（实测两次：26 段、122 段）。
  用 `run_chunked.sh <jobs.json> <outdir> <每块条数>`，**不要直接调 `run_batch.py`**。
  ⚠️ 这条契约「音频批量生产与灌入」⑥ 早就有，脚本也早写好了 —— 2026-09-01 仍然踩了，
  原因是**配方（生产时真正会被打开的那一份）没写**。修复留在脚本里而没回写到会被
  读到的文档里，等于没修。
- 🔴 **`artist_bio` 任务要手工补两个字段，队列不给**：`audio_queue` 输出里 artist_bio 的
  `museum` 是 `null`、`qid` 是**作者 qid**，而生成脚本 `run_batch.py` 取正文走的是
  `/museums/{museum}/objects/{via_qid}/content` 的 `artist.bio` —— 直接喂队列输出会
  请求 `museums/None/objects/Q156272/...`，**20 条全部 404**。
  出队列后必须补：`museum=<馆 slug>`、`via_qid=<该作者名下任一作品 qid>`。
  （2026-09-03 橘园 TOP4-6 实测踩到。前一批 41 条不含作者介绍，这条路径从没被走过 ——
  **"上批跑通了"不等于这批的每条路径都跑通过**，任务类型变了就得重新验那条新路径。）
- 🔴 **任何内容侧写操作之后都要重跑一遍音频队列 —— 三个入口，不只是"改正文"**：
  `_upsert_section` 正文一变就把 `audio_key` 置 NULL，`persist_suggested_questions` 是
  delete+重建、新行 `audio_key` 天生为空，而**闸翻案**（`needs_review → published`）
  会让一条从没进过队列的行突然变成"已发布但没声音"。前两类是**已发布内容静默变哑**，
  第三类是**凭空多出一个洞**，现象不同、后果一样：用户点开有文字没声音。
  → 重生成 / 重译 / **翻案** 之后一律跑
  `audio_queue_cli.py plan --museum <slug> --langs <全部> --no-upgrades --json`
  再按 qid 过滤，与 DB 逐条诊断对上（两条独立路径吻合才算没漏）。
  （2026-09-02 重生成+重译打哑 41 条；2026-09-03 一次 22 段+26 问答的翻案又漏出 1 条
  ——《红发女孩》de/significance。**翻案这个入口此前没被想到**：它不改一个字，
  却同样制造缺口。凡是会改变 `status` 或 `body` 的批处理，都算内容侧写操作。）
- 🔴 **未过闸的产物在 `<outdir>/_rejected/`，不在输出目录根下**（2026-09-04 起）。
  重试 3 次仍不合格时，`run_batch.py` 把最后一次的文件移进 `_rejected/`。
  **必须移走**：灌入端 `check_audio` 只判时长比例与替换偏差，**不看锐度和一致性**，
  留在根目录会一路绿灯进 prod（实测已漏进去一条 `Q3937645/ja/qa_0`，
  灌入时 md5 逐条核对 127/127 全过 —— 核的是「上传的和本地一致」，
  **验不出「本地这条本身没过闸」**，校验方向错了一维）。
  **又必须留着**：排查失败原因要拿这条音频去转写比对。第一版直接 `unlink`，
  当天就撞上 —— 查那条六次卡在 0.84 的 `pl/significance` 时，证据已被自己删干净。
  ⚠️ `_state.json` 里失败条目的指标是**混的**：`sharp`/`consist` 取自第 1 次，
  `lenratio` 是循环变量泄漏出的最后一次。只用于诊断，别当作那个文件的真实读数。
- **RTF**：Mac M4 上克隆模式约 3.5-4，纯生成约 2-3.5。夜跑 8 小时约产 60-80 段（2 分钟/段）。
- **环境**：`~/tts-lab/venv`（voxcpm 2.0.3 + torch 2.13 MPS）；分析依赖 `librosa` + `praat-parselmouth` + `mlx-whisper`。

---

## 八、当前 10 语言定稿（2026-08）

| 语言 | 种子 | 锐度 | HNR | 一致性 |
|---|---|---|---|---|
| fr | ZH | 74% | 22.9 | 0.98 |
| ko | ZH | 86% | 21.9 | 0.93 |
| es | ZH | 92% | 20.3 | 0.98 |
| it | ZH | 98% | 20.1 | 0.95 |
| pl | ZH | 100% | 18.2 | 0.96 |
| de | ZH | 103% | 20.2 | 0.94 |
| zh | 自己 | 138% | 18.6 | — |
| ja | **EN** | 138% | 19.6 | 0.96 |
| en | 自己 | 181% | 17.4 | — |
| zh-hant | **ZH**（2026-08-31 纠正） | 144% | — | 0.951 |

> ~~**zh-hant 的两条门槛与众不同**：锐度天然 200-235、一致性 0.89-0.93，
> 生产脚本对它放宽到锐度 <215、一致性 ≥0.88。~~
> **已撤回（2026-08-31）** —— 那两个数字是 **en 种子念繁体**的表现，是为迁就一个
> 错误的种子选择才放宽的。换回 zh 种子后实测锐度 144%、一致性 0.951，
> **通用门槛绰绰有余**。留着放宽的门槛只会继续放行本该被拦下的东西。
>
> **一致性阈值受 ASR 特性限制**。实测分布：拉丁语系 0.98-1.00、zh 0.937-0.958。
> ⚠️ zh-hant 曾记为 0.891-0.929 并据此放宽阈值，**那是 en 种子念繁体的表现**；
> 换回 zh 种子后是 **0.951**，不需要任何放宽。下面这段分析仍成立（它讲的是
> CJK 转写的同音字天花板，与种子选择无关）：逐字看过当时最低那条（0.891）的
> 全部 23 处差异，**无一例外都是同音字**——点/店、亚/阿、艺/戒、画/化、他/她、
> 「纪尧姆」/「技勇母」（人名）。长度比 1.000，一字不多一字不少，没有任何漏读或乱序。
> **音频念的是对的，是 Whisper 选错了字**，这是中文 ASR 的天花板，不是音频缺陷。
> 拿 0.90 一刀切会系统性误杀 CJK 内容。

全部时长精确对齐 tts-1 基准，用户听测评价「音色完美」。

# GoMuseum 博物馆主文档（API 契约 + 内容体系 + 上馆手册 + 路线图）

> **这是唯一权威主文档。** 所有博物馆按本文的契约与路线图上线;奥赛 = 样板馆。
> **工作纪律**:每次决策 / 发现的问题 / 零散需求 → **回写本文**。带日期的 `docs/superpowers/specs/*` = 设计记录,**喂给本文**;本文永远是当前真相。新馆只读本文即可快速上线。
> 端点前缀 `/api/v1`。**加法/前向兼容**——只增字段、不破老解析;破坏性变更走版本化端点。
> 最近更新:2026-09-12(音频节新增 ⑯:起跑前锁定生产脚本版本)。2026-09-04(音频节新增 ⑫-⑮:合格判定必须可校验、重试两轮为限、上规模按件灌入、灌入前后各三方核账)。

## 北极星

上任意馆 = **配源 → 抓全证据包 → 生成一次(分层·去重·动态·接地)→ 封板**。馆上线后基本不再动,精力转下一个馆。本文 = 实现这一目标的契约 + 手册 + 路线图。

> 详细内容体系(三层体验 / 模块库 / 证据包)见 §内容体系;演进顺序见 §路线图。

## 端点总览

| 端点 | 用途 | 实现(`museum_repo.py`) |
|---|---|---|
| `GET /museums` | 馆索引(探索页) | `list_museums` |
| `GET /museums/{slug}?language=` | 完整馆包(元数据+分类+全藏品) | `get_museum_pack` |
| `GET /museums/{slug}/objects?...` | 分页藏品列表(列表页) | `list_objects` |
| `GET /museums/{slug}/objects/{qid}/content?language=` | 单件讲解(导览页) | `get_object_content` |
| `GET /search?q=&language=&limit=` | 全局搜索(探索页;跨馆藏品+博物馆) | `search/inprocess.py` |
| `GET /museums/{slug}/search?q=&language=&limit=` | 馆域搜索(馆列表页;只搜当前馆藏品) | `search/inprocess.py` |
| `GET /museums/{slug}/objects/{qid}/audio?language=&section=` | 懒 TTS(点播放才生成;返 `{audio_url}` R2 直链;生成中 409) | `endpoints/museums.py` |
| `GET /museums/{slug}/objects/{qid}/audio/stream?language=&section=` | 流式 TTS(✅2026-07-16 #256):首播边生成边播 | `endpoints/museums.py` |
| `POST /feedback` | 用户反馈(藏品级/App 级;匿名可写;204 无响应体) | `endpoints/feedback.py` |

**音频流式端点(加法,老 `/audio` 不动)**:`/audio/stream` 仅 guide/深度段(qa 连念/artist_bio v1 仍走 `/audio`)。⚠️ **同一个 200 有两种形状**——首播返 `audio/mpeg` chunked 字节流(单次 TTS tee:客户端边收边播 + detached 落库,断连也把整段落 R2,不重复付费);已落库返 JSON `{"audio_url"}` R2 直链。**前端必须按 Content-Type 分支**;409=同段他人正在生成(重试即缓存命中),404=该段文本未发布。流式响应无 Content-Length/不支持 Range。

`language` 取值 = `DEFAULT_LANGUAGES`(`lang_config.py`,唯一真相源;**本文不复制其内容**——曾硬列举成固定语言数并滞留数月,已改为纯引用)。缺省 `zh`。**加语言=加配置**:新语言按§多语显示名的"加语言 checklist"落配置即全端点生效,**本行不再随之改动**(引用真相源,非硬列举)。新内容全语生成;老内容缺语走懒翻译/`translate` 补。

---

## 1. `GET /museums`

馆列表,不含藏品。无 language 参数。

```json
[
  {"slug": "orsay", "name_zh": "奥赛博物馆", "name_en": "Musée d'Orsay",
   "city_zh": "巴黎", "city_en": "Paris", "country": "FR", "artwork_count": 262,
   "cover_image": "<url|null>"}
]
```

- `cover_image`(✅2026-07-20 spec museum-cover-intro-quality,加法):探索页列表缩略图(thumb 档),
  同 `m.cover_image_key` 一行零成本可读;未生成封面则 `null`(前端隐藏)。

## 2. `GET /museums/{slug}?language=zh`

完整馆包:馆元数据 + **分类 facet** + 按热度降序的全藏品。`language` 仅影响 `categories[].label`。

```json
{
  "slug": "orsay", "name_zh": "奥赛博物馆", "name_en": "Musée d'Orsay",
  "city_zh": "巴黎", "city_en": "Paris", "country": "FR",
  "qid": "Q23402", "source": "...", "generated_at": "2026-...Z",
  "artwork_count": 262,
  "categories": [
    {"code": "all", "label": "全部", "count": 262},
    {"code": "painting", "label": "绘画", "count": 259},
    {"code": "unknown", "label": "其他", "count": 3}
  ],
  "description": "奥赛坐落在一座1900年的火车站里…(AI接地叙事,按language)",
  "cover_image": "<url|null>",
  "artworks": [
    {"qid": "Q334138", "title_zh": "世界的起源", "title_en": "L'Origine du monde",
     "artist_zh": "居斯塔夫·库尔贝", "artist_en": "Gustave Courbet",
     "year": "1866", "period_zh": null, "period_en": null,
     "image": "<url>", "popularity": 51}
  ]
}
```

- `description`(✅2026-07-18 spec museum-intro,加法):馆叙事介绍,AI 接地生成 `description_i18n[language]`,缺→en→任一→**null**;2-3 短段落以 `\n\n` 分隔(2026-07-20 优化,不再是单一长段)。`cover_image`:封面 R2 直链(large 档,**可 null**)——**优先馆自身建筑外观照**(Museum.qid 的 Wikidata P18,不过安全闸:架构零裸体风险),无建筑照/下载失败则回落藏品图逐件得体性判定(古典/宗教裸体可,写实露骨否决)。前端 `as String?`,null 整块隐藏。老 App 不读不炸。

- `categories`:`all` 合计 + 各 `MuseumObject.category` 分组计数,标签本地化(`_CATEGORY_LABELS`:painting/sculpture/photography/decorative_arts/unknown)。
- `artworks[].title_zh` 永不为 null(回退 `title_en`→`qid`,防前端裸 `as String` 崩)。
- ⚠️ `artworks` 是**全量**(无分页);列表页改用端点 3。本端点保留供馆详情/概览。

## 3. `GET /museums/{slug}/objects`

分页藏品列表(列表页 A2/A3:分类 tab + 无限滚动)。

**Query**:`category`(可选,`all` 或省略=不过滤)、`sort`(默认 `popularity`)、`limit`(默认 50)、`offset`(默认 0)、`language`(默认 `zh`)。

```json
{
  "items": [
    {"qid": "Q334138", "title": "世界的起源", "artist": "居斯塔夫·库尔贝",
     "year": "1866", "thumbnail": "<url>", "content_status": "ready"}
  ],
  "total": 262, "limit": 50, "offset": 0
}
```

- `title` 按多语显示名规则:`title_i18n[lang]`(Wikidata权威)→ 该语言 legacy 列 → en 兜底。`artist` 同理经 name_i18n。
- `content_status`:**按请求语言解读**(2026-07-03 定)——对象有内容但该语言无已发布内容 → 返 `empty`(前端显"待完善",防"列表看着有、点进去空")。见末节生命周期。
- 未知馆 → 404。

## 4. `GET /museums/{slug}/objects/{qid}/content?language=zh`

单件讲解(导览页)。按 `language` 返对应语种已发布内容。

```json
{
  "qid": "Q334138", "category": "painting", "language": "zh",
  "status": "ready",
  "title": "世界的起源",
  "default_guide": {"body": "想象一下,站在一幅大胆的画作前…(单主线~300-600字现场导览)", "has_audio": true},
  "images": [{"url": "<url>", "credit": "..."}],
  "facts": {
    "artist": "居斯塔夫·库尔贝", "date": "1866",
    "medium": "...", "dimensions": "...", "inventory": "RF 1995 10",
    "location": "奥赛博物馆", "provenance": "...", "artist_life": null,
    "exhibitions": ["1988, New York...", "..."], "bibliography": ["..."]
  },
  "tabs": [
    {"section_code": "overview", "label": "通用描述", "icon": "...",
     "body": "《世界的起源》是...", "audio_url": null}
  ],
  "suggested_questions": [{"question": "...", "answer": "..."}]
}
```

- `default_guide`:**默认标准讲解**(单主线·5拍·~300-600字现场导览,识别后首先呈现的"主角")。`{body, has_audio}`,无则 `null`(前端回退 tabs)。⚠️ **是 `has_audio` 布尔值,不是直链**——列表接口只告诉你"有没有",真链要播时再调 `/audio` 端点(那里才走缓存/懒生成/付费墙)。(2026-08 更正:此处曾写作 `audio_url`,与实现不符,排查音频灌入时被误判为"灌入失败"。)**不混入 tabs**。前端分层页:default_guide 置顶 → 推荐 2-3 个 suggested_questions → tabs/其余收进"更多内容"。
- `tabs`:按类目的段落清单(`SECTIONS_BY_CATEGORY`)逐段(降级为"更多内容"深度模块;**overview 已退役**——默认讲解取代其作开场;**artist 已移出 tabs**——成独立作者卡;各模块各守互斥 lane、**不复述头条**、只会重复则返空不发布);`body` 为该语种已发布正文(无则 `null`);`audio_url` 为 R2 音频直链(未生成则 `null`,TTS 阶段)。
- `artist`:**必选常驻作者卡**(不随空隐)。**数据来自 `artists` 一等实体**(按 artist QID 生成一次、同作者所有作品复用→一致/完整/省;artist 不再是 per-work 段)。`{name, birth, death, nationality, notable_works:[...], bio}`(**name/title 按多语显示名规则解析:i18n 权威→翻译→en**;bio 按语言取)——生卒年/国籍/代表作取自作者 Wikidata 实体(P569/P570/P27/P800);`bio`=artist 段已发布叙事(无则 null)。~~v1 局限~~ ✅2026-07-04 解除:`nationality`/`notable_works` 按 language 本地化(`nationality_i18n`/`notable_works_i18n`:P27/P800 多语权威标签→translate_name 兜底→en 列;生成与 names 回填两路都填,幂等)。
- `facts`:**已策展+人性化的墙签事实**(只 wall_label 级):`artist/date/medium/dimensions/inventory/location`。`medium` 优先证据包干净源(Wikidata P186,多值合并人性化),回退 Joconde `medium_fr`;`dimensions` 取 Joconde 串人性化(证据包暂不抓 P2048/P2049,非法国馆无 Joconde 时为 null)。⚠️ **`provenance` 返 null、`exhibitions`/`bibliography` 返 `[]`——已移出面板**(学术噪音;参考文献彻底不展示,收藏/展览史进证据包材料级,阶段2 由 background lane 讲成流转故事)。`artist_life` 暂 null。
- `suggested_questions`:好奇心问答(0-4 条)。
- 对象不属于该 slug / 不存在 → 404。

---

## `content_status` 生命周期

`stub`(只元数据,目录铺出) → `ready`(≥1 段已发布)/ `empty`(无可接地材料)。前端:`stub`/`empty` 显"待完善",`ready` 正常。未知值按 `ready` 容错。

**懒生成(✅2026-07-03 落地)**:content 端点命中 `stub` → 后台触发该件完整生成(全语言;**请求者语言排队首、逐语言翻完即落库**——用户最快看到自己的语言,单语言翻译失败不拖垮其他),几分钟后再取即 `ready`。

**首屏关键路径原则(✅2026-07-16 #257 定,TTFC 45.9s→~15s)**:懒生成语句顺序=**请求者首屏最短路径**——材料抓取(guide 的接地输入:对象/作者 wiki 提取 + artist_facts + 证据包,**必须在 guide 前**)→ guide(en) 落库 → **请求语言 guide 翻译立即落库(用户 ~15s 见主讲解)** → 其余全部延后(作者实体 bio/i18n、canonical 深度段、其余语言、QA)。**今后往管线加任何生成步骤,先问:在 guide 依赖链内吗?不在 → 一律放首屏之后**。纯重排:调用集/prompt 不变=质量与成本不变;每段 persist 即 commit=前端轮询立即可见。锁=内部 `attributes.lazy_lock_at`(TTL 10min 自愈),**不对外暴露中间状态**——stub 期间前端照旧"待完善",契约形状零变化、老 App 无感。`empty` 不重试(已判无可接地材料,防循环烧钱)。并发上限 2/进程;仅 staging/production 环境生效。

**`generation` 进度字段(✅2026-07-10 加法)**:content 端点返回 `generation: {published, expected}`——真实段落分数(expected=guide+该类目深度段;published=已发布的 guide+深度段)。前端显"N/M 段"诚实进度(**不做假百分比/时间进度条**:LLM 时间不可预测,假条会卡在90%伤信任)。配合 guide 先出流式,等待体验=主讲解先出+真实段落进度+计时预期。

**`generating` 字段(✅2026-07-04 加法)**:content 端点返回 `generating: bool`(信号源=懒任务锁)——true=懒生成/懒翻译进行中。前端三态:generating→"生成中"继续轮询;false+empty→"资料不足"停;false+stub 无内容→"待完善"+重试入口。老 App 不读则无感。

**懒翻译(✅2026-07-03 落地,懒生成姐妹场景)**:content 端点命中"对象 `ready`、有 en 轴心、但**请求语言**无已发布内容" → 后台**只翻这一门语言**(补语种原语:段落+问答+作者bio,数十秒、费用分钱级)。配合**列表 content_status 按请求语言解读**:该语言缺 → 列表即显"待完善",点开触发懒翻译,刷新即有——任何语言视图所见即所得,新加语言自动享受同样行为。同锁/并发/环境门。

## 图片字段（✅2026-07-03 R2 自存落地）

`image`(端点2)/`thumbnail`(端点3)→ **thumb 档**(480px);`images[].url`(端点4)→ **large 档**(1600px,兼识别参照)。`ObjectImage.image_key` 存**基础键**(`images/{qid}/{sort}`),端点拼 `_thumb.jpg`/`_large.jpg` 出 R2 直链;无 key 回退 `source_url`(Wikimedia)。字段名不变,前端零改。

- **多角度图**:catalog 收 P18 全部值 → 多行 ObjectImage(首张 `role=primary`,其余 `view`;雕塑识别参照)。Commons 分类(P373)深挖留给识别机制 brainstorm。
- **物化**:`onboard.py <slug> images --target <env> [--limit N]`——下载(合规UA限速)→Pillow两档(JPEG q82,不放大)→R2→Commons 署名(license/credit,CC-BY 摄影必署)。逐行幂等,失败留空重跑重试;SVG/超60MB 跳过。不存原图(source_url 可重下)。
- **懒补漏**:content 端点发现该件缺图 → 后台补单件(自愈运维疏漏/新进目录;不占内容锁)。
- **⚠️ 成本分界(通用原则)**:**图=目录门面必须预物化**(列表一屏几十张缩略图多数永不被点开;识别参照库必须先于识别存在;成本≈0)——与**内容=必须懒生成**($/件)相反。新馆照此:catalog→names→images 全量预跑,generate 分层。

---

## 收录策略（通用规则,任何馆 0 代码复制;2026-07-03 定）

**① 分层收录(2026-07-13 取代"有图才收录"硬规则;版权边界语义不变)。** 三层：
**视觉可识别层**(有自由版权图 P18/Wikimedia——图仍是识别参照物+合规边界,官方 RMN 类版权图照旧不碰)→向量识别+列表展示全功能；**文字可识别层**(有 Wikidata 条目无图,catalog P18 已 OPTIONAL 收全条目)→墙签 OCR/馆藏号/搜索命中,讲解懒生成照常,**列表与分类计数不出现**(浏览面密度优先),馆页双数字揭示规模;**需求层**(无条目)→unrecognized+记需求。无图件讲解页 hero=识别路径用用户自己照片(会话内回显)/搜索路径类目占位图。

**② 三层口径与北极星 KPI(2026-07-13 验收换轴)。** 口径:官方全馆藏(奥赛~15万) ≫ **常设在展(3-4千,真实分母)** > Wikidata 条目(5353=档案层) > 有图(~2065=图录层)。**上新馆验收不看导入件数,看"现场识别成功率"**——用户实拍一件眼前藏品被正确识别的概率(recognition_events 聚合:(match+确认候选)/尝试)。**KPI 归因口径(2026-07-13 实测修正)**:App 主入口是全局识别端点(事件 museum_slug 为空),按**命中对象的归属馆**归因;全局且未命中(top_qid 也空)的事件无法归馆、不计入任何馆的分子分母(诚实局限,不引入方向性偏差)。`onboard coverage-report` 是配方最后一步:输出分层数字/展陈分布/KPI 并回写 museums.stats(馆页双数字数据源)。

**③ 定期重跑 catalog 吃增量 + 展陈状态动态化(2026-07-13)。** Wikimedia 持续增长,catalog 幂等重跑收新;**展陈状态绝不做静态布尔**——`attributes.display`={status(CONFIRMED_ON_DISPLAY/LIKELY_ON_DISPLAY/TEMPORARY_EXHIBITION 预留/NOT_ON_DISPLAY/UNKNOWN), evidence[], confidence, verified_at},证据源按普适性三层:**自家识别流量(最强,零适配器,近30天确认识别→CONFIRMED)** > Wikidata P276(稀疏先验→LIKELY) > 区域适配器(可选增强;样板=Joconde 开放API,reference=P347,localisation 为保管馆级,覆盖全法博物馆)。报告 CLI 触发重算,不做实时。

**④ 通用分类法(全馆共用 8 大类 + unknown 兜底)。** `CATEGORY_BY_QID`(category_config.py)是唯一真相源:P31 QID → 类目;**加类型=加一行映射 + 馆 yaml `categories` 选用,零代码**。多 P31 作品已知类目优先于 unknown。每类目一套深度模块段落集(SECTIONS_BY_CATEGORY;未细化的类用通用兜底,真上馆再细化)。

| 类目 code | zh | 归并(示例) | 段落集 |
|---|---|---|---|
| painting | 绘画 | 油画/壁画 | 专属 |
| sculpture | 雕塑 | 雕塑/雕像/半身像/小像 | 专属(材质工艺) |
| works_on_paper | 纸上作品 | 素描/水彩/色粉/版画/习作/草图 | 同绘画 |
| photography | 摄影 | 照片 | 专属(摄影师) |
| decorative_arts | 装饰艺术 | 家具/陶瓷/玻璃/金工 | 专属(maker/用途) |
| textile | 纺织 | 挂毯/服饰 | 兜底 |
| artifact | 文物器物 | 考古/礼器/武器 | 兜底 |
| manuscript | 手稿古籍 | 手抄本/书籍 | 兜底 |

**⑤ 内容生成分层(目录便宜、生成贵;2026-07-05 定 N 默认=0)。** 目录+显示名+图物化全量做(全馆 ~$20 内,零 LLM 成本);**讲解内容默认不批量预生成(N=0),完全靠懒生成/懒翻译按需兜底**——上新馆零内容预付,用户点开哪件才生成那件(请求语言优先,~2-3 分钟)。上线运营后按**实际热度/需求**由运营决定某馆 top-N 值再 `generate --limit N` 批量富化(热门件零等待)。**全量目录/图只在 prod 跑;staging 只做小样本验证机制**(`catalog --limit`)。

**⑥ staging 轻量化(2026-07-17 定,spec 2026-07-17-staging-lightweight)。** staging=机制验证平台,**永不为数据规模付 LLM 费**:机制验证用小样本(护栏强制——onboard names/generate/translate 在 staging 默认 limit=50,rescan 系需显式 `--allow-full`);规模数据一律从 prod 搬运(`sync_staging_from_prod.py`,slim=金样本 ~300-500 件/full=发版前拉真;内容是 prod 已付费资产,共 R2 桶 key 复制即解析,零 LLM);**用户表(users/benefits/devices)与 recognition_events 永不跨环境**。上新馆同理:staging 只跑 `catalog --limit` 验证配方,全量只在 prod。

## 上新馆 = 纯配置(零核心改动)

> **⓪ 预检清单(2026-07-19 橘园实战补;写配置前先做,两条各一分钟)**:
> 1. **QID 核验+规模预估**——馆名易记错 QID(橘园教训:Q1094962 查出 0 条,真身 Q726781)。先
>    `wbsearchentities` 搜馆名拿 QID,再 WDQS 数 `P195=<QID>` 条目数与 P18 有图数——既验 QID 又预估
>    图录/文字层规模与 names 成本(基准:~$0.60/140 件,names 占 98%)。
>    ⚠️ **同时数一遍 `P31` 落在 `categories` 白名单内的有图件数** —— 那才是真正会
>    落库的可识别件。只数 P18 会高估:小皇宫 P18 有 1884、实际落库有图 1663
>    (差额=无 P31 无法归类的件 + 类目在白名单外的零头)。
>    ⚠️ 还要**顺手看馆名对不对得上**:正式名与建筑名常是两个词条,小皇宫的馆是
>    "musée des Beaux-Arts de la ville de Paris"(Q59546080),Petit Palais 是它
>    `P276` 指向的**建筑**。填错就查不到藏品。
>    ⚠️ 这两个词条**通常只有一个有 enwiki**(小皇宫是建筑那个有)——顺手记下
>    带 enwiki 的那个 QID,配方第 6 步的 `intro_qid` 要用,否则 `intro` 会返
>    `skipped: no_material`(小皇宫实测,收尾时才发现)。
> 2. **Joconde 馆名必须用 POP 精确官方名**——先 `search(nom_officiel_musee,"关键词")` 探查
>    (橘园官方名="musée de l'Orangerie des Tuileries",直觉名查出 0 条),命中的字符串原样填
> ✅ **Joconde 已修复(2026-09-20 #615),但通道换了形态** ——
> 以下是断供近两个月后查清的根因与新通道,**上新法国馆前必读**:
>
> - **根因**:`data.culture.gouv.fr`(opendatasoft 查询 API)**整站 301** 到
>   `culture.data.gouv.fr`,而且重定向**把路径也丢了** —— 任何 API 路径都落到
>   门户首页、返回 HTML 且 **status 200**。所以"返回法语 HTML"不是源故障,
>   是**源搬家了**;而 `status_code != 200` 那道判断形同虚设,一路崩在 `.json()` 上。
> - **新通道 = data.gouv.fr 的 Tabular API**,与富化源 `JocondeSource`
>   **同一条通道、同一份资源**(`sources/joconde.py` 早在 2026-09 就迁好了,
>   目录源这次才跟上)。资源 id 按 dataset slug `collections-des-musees-de-france-base-joconde`
>   解析 —— **别硬编码 id**,文件重传时 id 会变,slug 不会;
>   `resolve_resource_id()` 已有进程内缓存,两个源共用。
>   筛选走 `?Nom_officiel_musee__exact=<馆名>&page=N&page_size=200`。
> - **`page_size` 上限 200**(超过直接 400);深翻页正常,无 offset 天花板
>   —— 旧 opendatasoft 的 `limit+offset ≤ 10000`(旧代码 `_MAX_OFFSET=9900`)没有了。
>   实测奥赛 4111 条翻到第 42 页正常,**21 秒**拉完;橘园 133 条 1.9 秒。
> - ⚠️ **终止条件必须看 `meta.total`,不能看 `len(rows) < page_size`** ——
>   服务端有权把 page_size 钳小,那样第一页就会被误判成末页,**静默少一批件**。
>   同理:出错时这个 API 返回 `{"errors": [...]}` 而**没有 `data` 键**,
>   `data.get("data") or []` 会把报错读成"这馆没有藏品"。两处都要显式喊出来。
> - 📌 也**有**整包 CSV(`ministere-culture.s3.sbg.io.cloud.ovh.net/POP/joconde.csv`,
>   1.2GB,`|` 分隔,68 字段,多值 `;`),但只在需要全量统计时才值得下
>   (例:核对某馆官方名有无变体写法)。日常上新馆走 Tabular API。
> - ⚠️ **`Millesime_de_creation` 是脏的且会持续脏**:Joconde 把创作年和法语精度词
>   **反序**拼在一起(`1853 vers`=约1853、`1865 entre,1908 et`、`1836 commencé`)。
>   **源数据本身就长这样**(已核对官方 CSV 与库里记录一致,不是中间平台加工的)。
>   入库时清洗(`_clean_year`),否则它会进 LLM 材料 ——
>   `pipeline.py` 把 year 当 "Creation year" 喂给模型。
>   只归一化**语义确定**的形态(约/区间);`1867 vers,1868 ou`(约1867**或**1868)
>   压成区间是改写原意,原样留着。实测残留:奥赛 341/4111、橘园 42/133。
>   `avant`/`après` 要按界面语言渲染 → 后端没有语言维度,留给前端 `year_format.dart`,
>   **两边规则成对,改一边要改另一边**。
> - ⚠️ **`Domaine` 从 list 变成 `;` 分隔字符串**:直接丢进 `for d in domaine`
>   会**逐字符**遍历、全部落到 `unknown` 且**不报错**。换通道最容易静默踩的一脚。
>
> 断供期间(2026-07-22 ~ 09-20)生成的件材料偏薄(纸上作品/馆藏号等 Joconde
> 特有信息缺失),可对已生成件 `generate --force` 补更丰富材料。
> 这段历史也是纪律①"单源失败跳过继续"与纪律 31「容错必须配探活」的实战来源。
>    `joconde_museum`。非法国馆跳过此条。

连接器已存在时(Wikidata 全球通用),上新馆只需:

1. **`backend/museums.yaml` 加一条**:
   ```yaml
   <slug>:
     name_zh / name_en / city_zh / city_en / country
     wikidata_qid: Q...        # 馆的 Wikidata 实体(P195 收藏于)
     category_filter: Q3305213 # 主类目 QID
     categories: [Q3305213, ...]
     country_lang: fr          # 馆所在国语言(取该语维基;作者材料同用)
     sources: [joconde, wikipedia]  # 每馆补充源;缺省 [wikipedia]。Wikidata 是脊柱不在此列
     fetch_limit               # ⚠️ **SPARQL 行数,不是件数** —— 见下
     sample_size / sample_qids
     languages: []             # 空=用 DEFAULT_LANGUAGES;或指定子集
     intro_qid: Q...           # 仅「馆本体无 enwiki」时配(介绍材料源;见§博物馆介绍)
     # ↓ 仅"部门制/百科式大馆"需要(2026-07-25 卢浮宫定,见下)
     collection_qids: [Q..., ...]   # P195 挂部门而非馆本体时,列全部部门 QID
     collect_all_types: true        # 整部门全收,跳过 categories 类目过滤
   ```
   **⚠️ `fetch_limit` 是 SPARQL 行数,而查询口径是「全部藏品」而非「有图藏品」**
   (2026-07-30 小皇宫 prod 实测定;此前一版按 staging 小样本推算的比例是错的,已更正)。
   两件事要分清:
   - **`P18` 在 catalog 查询里是 `OPTIONAL`** —— 无图件也进结果、也落库(它们是
     **文字可识别层**,搜索可达、相机识别不到,见§收录策略)。所以 `fetch_limit`
     要按**总藏品数**估,不是有图数。
   - **去重比很低**:小皇宫实测 3793 行 / 3617 件 = **1.05 行/件**(多 P31、多 P18
     才产生额外行)。**按总件数 × 1.5 倍余量填足够**;卢浮宫那种部门制大馆按 2.2 估。

   **预检基准必须按 catalog 的真实口径算,否则核对永远对不上**:落库要求
   `P195 命中锚点` **且** `P31 在 categories 白名单内`。小皇宫实测:P18 有图 1884 件,
   实际落库有图仅 **1663** —— 差额是**没有 P31 的件**(无法归类)加上 22 件类目
   不在白名单(maquette/奖章/挂毯等零头)。**只数 P18 会高估**。

   跑完核对 `loaded` 与预检量级;差得远(而非差几个百分点)才是被截断了。

   **⚠️ 两个大馆开关(卢浮宫落地,惠及大英/Met 等百科式馆):**
   - **`collection_qids`(多收藏锚点)**:大馆藏品的 P195 常挂**部门实体**而非馆本体
     (卢浮宫 Q19675 直挂仅 17 件,12 个部门合计 18.4k)。**判别方法**——先跑
     `?item wdt:P195 ?mus . ?mus wdt:P361 wd:<馆QID>` GROUP BY 计数,若部门件数远超
     馆本体直挂数,就把馆本体+全部部门 QID 列进 `collection_qids`。缺省不写=回退
     `[wikidata_qid]`,单锚点馆行为逐字不变。**馆身份仍是 `wikidata_qid`**(建筑照 P18/
     intro 材料/对外把手都用它),身份与收藏锚点是两个概念,不要混用一个字段。
   - **`collect_all_types`(整部门全收)**:古物/装饰艺术部藏品的 P31 是"钻石/石碑/花瓶/
     石棺"等**长尾几百种**(卢浮宫实测 top30 类型仅覆盖 36%),`categories` 白名单列不完
     → 置 true 跳过类目过滤,整部门全收;top 类型在 `category_config` 补 P31 映射
     (decorative_arts/artifact/textile/manuscript,架构已预留),长尾留 unknown 走
     `_FALLBACK` 段落(宁缺毋滥,讲解仍接地)。**代价**:件数可能翻倍(卢浮宫 10.4k→17.3k),
     names 成本同比例上升,先算账再开。
2. **铺目录**:`python scripts/onboard.py <slug> catalog --target <staging|prod> [--limit N]`
   → `WikidataCatalog.list` 列 stub(**P18 是 `OPTIONAL`,无图件同样收**——见§收录策略①分层收录)→ `merge_stubs` 去重 → `load_stubs` 落库(`content_status=stub`,元数据+路由 external_ids/wiki_titles)。新类目先跑一次 `scripts/seed_sections.py`(幂等)。
3. **回填显示名**:`python scripts/onboard.py <slug> names --target <env>`
   → 全馆 `title_i18n` + `artist_qid`(P170 批量)+ Artist 名字行(权威标签→翻译→en;幂等)。stub 即有完整多语显示名。

   ⚠️ **权威标签不等于可直接展示的标题** —— Wikidata 标签自带一堆消歧后缀,
   原样抄进显示名就是用户在馆藏列表看到的东西(全库实测 5708 个字段):

   | 该剥 | 例 |
   |---|---|
   | 馆藏编号 | `olpé (E 647)` / `Autoportret (F 627)` |
   | 生卒·年份 | `Porträt von Bossuet (1627-1704)` |
   | 作者名/类别消歧 | `투우 (마네)` / `Koronczarka (obraz Vermeera)` / `Verwundeter Kürassier (Öl)` |

   **但同样多的括号是标题本身的一部分**,剥掉就成了另一件作品:
   高更《餐（香蕉）》、`Die Mahlzeit (Mahl mit Bananen)`;
   作者名里的 `Bruyn（長者）`、`Bruegel (der Ältere)` 更是名字本身。
   → `_clean_i18n` 按**白名单**剥(第 24 条),判据是"括号内容是不是编号/年份/类别词/作者名";
   清洗作者名本身时**不传** artist_names,「（長者）」才不会被误认成消歧。
   类别词要**整词**匹配 —— 子串会让 `(Kölner Meister)` 撞上 `öl`、`(boiler)` 撞上 `oil`。

   ⚠️ **zh-hant 的兜底源顺序是个语言学判断,不是随手排的**:链为
   `zh-hant → zh-tw → zh → zh-hans → zh-cn → zh-hk`。**zh-hk 必须排在 zh 之后** ——
   港式音译是粤语拼读的另一套传统(Cézanne→施傘 / Giorgione→佐助彌 / Christoffer→基斯杜化),
   排在 `zh` 前面等于"宁可要另一个传统的音译,也不要本传统的简体转繁"。
   实测 Q35548 无 zh-hant/zh-tw 标签时,旧序取到「保羅·**施傘**」并被当 glossary
   注入每一段繁体译文,逐段复制污染 27 段正文。

   ⚠️ **上游权威源本身可能是错的**,不能因为它是"规范名"就免检:
   `Q5592` Michelangelo 的 **fr 标签**就是 `Michel-Ange Buronarroti`(Buonarroti 拼错);
   `Q1923320` 的 es/it/pl 标签仍是卢浮宫 2019 年已弃用的冒犯性旧称
   (`Portret Murzynki`),而 en/fr 早已改成 `Portrait de Madeleine`。
   → 上新馆后至少做一次形式普查(编号/年份/括号/引号配对/非拉丁语位无本族文字),
   **这类缺陷用户不会专门反馈,但会一直长**(第 24 条)。
4. **物化图片**:`python scripts/onboard.py <slug> images --target <env>`
   → 全馆缺图行下载→两档→R2→署名→**入库即嵌入**(幂等;见§图片字段。存量老库用 `backfill_embeddings.py` 补漏,新馆此步已自动嵌入,无需额外跑)。
5. **生成内容**:`python scripts/onboard.py <slug> generate --target <env> [--qid Q..|--limit N] [--langs zh,en,fr]`
   → 逐件:stub 抓材料(Wikipedia/Joconde)→ 生成 → 接地闸 → 翻译 → 落库 → `ready`/`empty`。
6. **馆介绍+封面**:`python scripts/onboard.py <slug> intro --target <env>`(见§博物馆介绍)
   → 接地生成馆叙事简介(按语言补缺)+ 选封面。**封面零额外配置**:优先取步骤①已填的
   `wikidata_qid` 的 Wikidata P18(馆建筑外观照,自动下载物化);无 P18 才回落藏品图逐件
   得体性判定。**这是"零代码上新馆"最新的一条自动收益**——不用为封面单独准备图源。
7. **验收闸(全绿才算上完)**:`python scripts/onboard.py <slug> verify --target <env>`
   → 把卢浮宫暴露的**六类事故固化成自动检查**:各语言译名覆盖 ≥95% / 英文标题空值 <1% /
   作者中文名 ≥90% / `artist_qid` 格式合法(挡 blank node 假身份)/ 图物化无残留 /
   介绍已分段 + 封面已选。**每项红了直接给补救命令**;非零退出可串进上馆脚本当闸。
   ⚠️ 契约里的教训是文字、会被跳过,**这个是跑得起来的**——上新馆最后一步必跑。
   ⚠️ **「门面响应 <1s」这项会假红,判它之前先预热**(2026-08-24 小皇宫添)。
   刚部署 recreate 容器、或刚跑完 `intro` 让馆详情缓存失效后,**进程内缓存按 worker
   各自冷建**,单测会撞到 1.3-1.5s;连打 20 次即收敛到稳定 0.14s。它给的补救提示
   ("查是否又有全量加载混进门面路径")在这种情况下是**误导**,今天为此白查两轮。
   **正确姿势:先 curl 打热几次再跑 verify。** 顺带记一笔:小皇宫过闸时是 **931ms,
   离 1s 阈值只剩 69ms**——上更大的馆时这项很可能真红,那时才该按提示去查。
8. 端点自动产出上述五个契约;前端零改接入。

> **⚠️ 上新馆期间的"半成品可见"(2026-07-26 卢浮宫实测)**:`Museum` 无任何可见性开关——
> **catalog 一跑完,馆就出现在 `GET /museums` 里**,而 names/images 还要跑数小时。用户在这段
> 窗口会看到半成品:标题中英混杂(权威标签已落、机翻未回填)、作者名全拉丁文、缩略图大面积缺失。
> 卢浮宫实测该窗口约 **6 小时**。
> **当前对策(无代码)**:大馆上新按 catalog → names → images 顺序**连续跑完再对外说**,期间不做
> 版本发布、不引导用户进该馆;小馆(千件级)窗口短可忽略。
> **待办**:若后续上万件级馆更多,再考虑给 Museum 加发布闸(数据就绪才进 `GET /museums`)——
> 属功能新增,需契约版本化考量,不在零代码范围内。

**非 Wikidata 主源的馆**(如美国 Met):一次性写个 `CatalogSource` 连接器(同插件模式,核心零改),其余同上。

`--target` 必须匹配容器 `ENVIRONMENT`(守卫防误灌)。全量富化在 prod 跑(staging 仅样本验证机制)。

> **⚠️ 批处理纪律(2026-07-03 定,prod 三次崩溃换来;适用于全部上馆命令 catalog/names/images/translate/generate 及未来新批任务):**
> 1. **单件/单批失败跳过继续**——错误计数(`errors`)不炸全局(教训:一次 Wikidata ReadTimeout 炸死整馆 names);
> 2. **分批落盘**——每 N 件 commit(names 50/images 25),崩溃/重部署不丢进度(教训:253 件进度全丢);
> 3. **批量外部查询必须分批 + 瞬时错误重试**——VALUES 有 URL 上限(教训:1300 QID 单条查询 HTTP 414);5xx 重试一次仍败跳批(教训:Wikidata 502);
> 4. **一切批任务幂等可重跑**——重跑只补缺,断点即续传。新写批任务时按此四条自查。
> 5. **空响应≠数据到底,必须重试后再判**(2026-07-11 添)——WDQS 深 OFFSET+ORDER BY 超时会返回
>    HTTP 200 的**空结果**,把空页当"翻完了"会**静默缺件**(教训:orsay catalog 两次截断 1537/1517,
>    漏收 500+ 长尾件含用户实拍的库尔贝《鹿斗》)。翻页循环连续空 N 次才允许 break;
>    上新馆跑完 catalog 核对 loaded 数与该馆 Wikidata 有图量级是否相符。
> 6. **容器内长任务:避开部署窗口 + 脱会话 + 日志必须实时落地**(2026-07-16 添)——后端容器随每次
>    staging/prod 部署 **recreate,里面的批任务被无声杀死**;起长任务前确认无部署在途/等部署落定再起。
>    启动用 `docker exec -d` + `setsid`(SSH 断连不死);日志逐行 flush 到文件(教训:staging Joconde
>    补名首跑中途死、日志 0 字节,**死因无法回溯**;重建带日志脚本后 1038 件零失败跑完)。
>    "监控到进程消失"≠"任务成功"——收尾判断只认日志 DONE 行 + 数据核验(纪律 4 幂等保证随时续跑)。
> 7. **外部服务硬上限 + 外部自由文本必须按列宽截断**(2026-07-25 卢浮宫 17k 件添,三个坑
>    **全是 staging 小样测不出、只有规模才现形的**)——上更大馆(大英/Met)前按此自查:
>    - **OpenAI Batch 单 job 上限 5 万请求**(硬限,超了提交即 `maximum_requests_exceeded`,
>      不是软限流)。件数×语种很容易破:17283×多语 ≫ 5 万 → `batch_names.run` 切 ≤50k 块,
>      **先全部提交**(OpenAI 侧并行跑,别串行等)再逐块收,`job_id` 存逗号分隔多 id 供续传。
>      staging 只 810 任务故从未暴露。
>    - **Commons 署名(credit)是自由文本,可长达数百字**(多段落说明+英法双语+联系方式),
>      超 `varchar(255)` 直接 `StringDataRightTruncation` **崩掉整轮物化**(实测 1369/10159 处崩)。
>      入库前一律按列宽截断(`_clip`),截断不影响合规归属。**凡外部来源的自由文本字段,
>      落库前都要问一句"最长能多长"**。
>    - **WDQS 深翻页(~18k 件)高频瞬时 502/ReadTimeout**,且 catalog 落库全有或全无、
>      一崩整批丢 → `run_sparql` 统一重试 4 次指数退避(5/10/20s)+ 超时 120s,两个 SPARQL
>      消费者共用。三次全量跑两次栽在这上面才补的。
> 8. **规模会让"本来没问题"的设计失效——上新馆后必须实测端点耗时**(2026-07-26 卢浮宫添)。
>    以下三条**都不是写错了,是量变引起质变**:奥赛 6.8k 件时全都正常,卢浮宫 17k 件全崩。
>    **所以判断性能不能靠读代码,必须在真实数据上测**(`curl -w "%{time_total}s %{size_download}"`)。
>    - **"全量返回"会从可用变不可用**:馆包把全部藏品塞一个响应,奥赛 1.8MB(勉强)→
>      卢浮宫 **5.0MB / 8.2s**(手机上几十秒)。更荒唐的是前端 `MuseumDetail` **根本不解析
>      `artworks`**——下载完就扔。→ 加 `artworks=false`(加法参数,缺省 true 保老 App),
>      **门面只返回 cover/categories/description,列表走分页 `/objects`**。实测 8.2s/5.0MB →
>      **0.06s/1.9KB**。**新增任何列表型响应先问:调用方真的要这些数据吗?**
>    - **进程内缓存的冷建成本随数据量涨**:搜索索引 6.8k 条目 ~2s(可忍)→ 24k 条目 **7.2s**。
>      TTL 600s × 2 worker = **每 10 分钟每 worker 坑一个用户等 7 秒**。→ **stale-while-revalidate**
>      (过期先返旧索引、后台重建)+ **启动预热**;重建失败保留旧索引(搜不到新件好过 5xx)。
>      **凡"过期后同步重建"的缓存,数据量一涨就是定时炸弹。**
>    - **每件一次的外部调用(N+1)小馆看不出、大馆致命**:names 取权威标签每件一次 SPARQL,
>      17283 件 = 17283 次串行往返(**5 小时**,CPU 仅占 12%,全在等网络);10 万件馆 ~30 小时
>      直接上不去。→ 批量化(VALUES 200/批),**5 小时 → 5 分钟**。**判据:CPU 占比远低于
>      墙钟时间 = I/O 阻塞,先查有没有 N+1。**
> 11. **小馆的 `popularity` 排序会失真,`--limit N` 选不出招牌**(2026-07-27 橘园添)。
>    `popularity` 取自 Wikidata sitelinks,对大馆有效(蒙娜丽莎 146),但**小馆/系列作品会塌**:
>    橘园**睡莲六联作 popularity 全是 0**,而《保罗·纪尧姆肖像》是 6 —— 按热度取前 N 预热,
>    **招牌反而排在最后**。原因:同一作品拆成多条目,每条的 sitelink 都很少。
>    **对策**:小馆预热/排序**按"有图"筛选或显式列 qid**,别依赖 `--limit N`;
>    ("有图"=可拍照识别=用户魔法时刻的入口,比热度更贴近真实价值)。
>
> ⬇️ 原纪律 **⑨⑩⑫⑬** 与博物馆无关(shell 编排 / 判进程 / 不可逆批量操作 / 替换过闸),
> 已移到下一节 [§工程与运维纪律](#工程与运维纪律与馆无关适用于任何批任务)。
> **编号一律不变**——`material.py` 等处的代码注释按号引用,重排会让它们全部失准。


---

## 工程与运维纪律（与馆无关,适用于任何批任务）

上一节那些纪律都是**跑上馆管线**才会踩的;这一节与博物馆、与语言都没关系 ——
凡是写批任务、跑容器内长任务、做不可逆的批量替换/删除,都适用。
⚠️ **本节开节时(2026-07-29)只有四条(⑨⑩⑫⑬),此后持续增长(截至 2026-09-20 二十七条),
「上第二个馆的人可以跳过本节」已不成立** —— 第 15(只给已发布内容做下游产物)、
第 28(挂起≠缺口)两条与内容质量直接相关,上馆验收(配方第 7 步 `verify`)会直接踩到。

> ⚠️ **编号沿用原「批处理纪律」序号(⑨⑩⑫⑬),搬家不改号。**
> `material.py` / `batch_names.py` 的代码注释、plans/specs 都按号引用(如"纪律①""纪律⑥"),
> 重排编号会让这些引用全部指错地方。

> 9. **多步批任务:用 `&&` 不用 `;`;完成标记只在真成功时出现**(2026-07-27 预热添)。
>    `;` 连接的命令链,中间崩了后面照跑、末尾照打 "ALL_DONE" —— **"打印了完成标记"≠"全部成功"**。
>    实测教训:三馆预热六条命令用 `;` 串起来,Joconde 崩掉 orsay/orangerie 两步,链条仍打印
>    `PREHEAT_ALL_DONE`;**第二次加了分步标记 `STEP_*`,用的还是 `;`,同样无效**——同一个坑踩两次。
>    两次都是靠**核验数据**(件数没涨)才发现的。改用 `A && echo OK_A && B && echo OK_B` 后,
>    标记才真正等价于成功。**收尾一律:看标记 + 核验数据,两者都对才算完(纪律⑥的加强版)。**
> 10. **判进程要认执行体,不认编排它的外壳**(2026-07-27 添)。用 `/proc` 或 `docker top` 按
>    命令行子串判断"任务还在跑"时,`sh -c "A && B && C"` 外壳的 cmdline **包含整条链的字符串**,
>    A 的进程早退出了外壳仍匹配 → 误判"还在跑"。实测后果:自动切换逻辑没触发,本该被拦截的
>    橘园全量任务跑了 7 件。判定要求进程名以 `python` 开头(或比对 PID),别只匹配子串。
>    ⚠️ 附:**后端容器里没有 `ps` / `pgrep` / `pkill`** —— 判活用宿主 `docker top`,
>    容器内定位/终止用 Python 读 `/proc` + `os.kill`。
> 12. **不可逆的批量操作:护栏比功能重要**(2026-07-29 R2 孤儿回收添)。删除/覆盖线上
>    资产的脚本,一次跑错就没有回头路,五条护栏一条都不能省:
>    - **默认 dry-run,必须显式 `--apply`**;
>    - **宽限期**(如 7 天):覆盖"客户端还拿着旧直链在播"与"刚写入未提交"的竞态;
>    - ⭐ **引用集合为空则立即中止** —— 查询写错/连错库都会让它变空,照删就是清空整个库;
>    - **删除占比超阈值(如 50%)中止** —— 正常不该这么多,先去查对账逻辑;
>    - **先列存储再读 DB**:列举期间新写入的 key 必已在 DB,不会被误判成孤儿。
>
>    ⚠️ **引用集合必须覆盖"所有存过这个 key 的地方",漏一处就删掉正在用的文件**。
>    音频 key 存在**三处**:`object_content_sections`、`object_suggested_questions`、
>    **`artists.bio_audio`(`{lang: key}` 的 JSON,最容易漏)**。
>    prod 首次 dry-run:21 个孤儿 / 32.1 MB,全是历史改名遗留。
> 13. **替换已有内容必须过质量闸,不过闸就不替换、保留旧版本**(2026-07-29 自托管 TTS 添)。
>    替换是不可逆的(旧文件即刻成孤儿),而**模型回归会静默毁掉整个库**——不报错、
>    不变红,等用户投诉时已经全量替换完了。所以先验后替:空/截断/无限重复用字节级估算
>    就能抓住(**不引入 librosa/ffmpeg**,要在批量管线跑几万次);最有用的是
>    **替换偏差检查** —— 同一段文本换引擎,时长差一倍必有问题。同理适用于换生成模型、
>    换翻译引擎:凡"用新产物覆盖已上线产物"的批量操作,都要有一道能拦住回归的闸。
> 14. **文本比对做校验前必须先归一化,否则对特定语言系统性误判**(2026-08 橘园音频试点添)。
>    用「转写 vs 原文相似度」校验音频内容是否读对时,**不归一化会把正确的判成错的**,
>    而且是**按语言成建制地误判**——实测两类:
>    - **变音符**:西语真实一致度 1.00 被算成 **0.53**,纯粹因为 `Tómate` vs `Tomate`;
>      de/it/fr/pl 同样受害。→ NFKD 分解后去掉 combining 字符。
>    - **简繁**:转写引擎常把繁体音频写成简体字,码位全不同 → 实测 **0.50**,归一化后 **0.90**。
>      → 比对前统一转一种字形(OpenCC `t2s`)。
>    后果不只是"报错":管线会**重试 3 次再判失败**,等于三倍算力换一个错误结论。
>    **通用推论**:任何跨书写系统/带变音符语言的自动校验(音频、翻译回译、OCR 核对),
>    都要先问"我的相似度函数对这个语言公平吗",别把归一化缺陷当成质量问题去调模型。
> 15. **只给「已发布」的内容做下游产物**(2026-08 橘园音频试点添)。
>    队列若不过滤发布状态,会把 `needs_review` 的内容也排进去,而前端 `/content` 只返回
>    published —— 生成侧取不回正文(白跑+重试),灌入侧质量闸同样取不到文本(判失败)。
>    实测橘园 de+it 116 段里 9 段是 needs_review(**7.8%**),按此比例外推全量约
>    2300-2600 段无效任务。**凡"为内容生成衍生产物"的队列(音频/翻译/嵌入/缩略图),
>    都要以发布状态为准入门槛。**
>
>    ⚠️ **但"过滤掉"不等于"处理掉"** —— 这是同一枚硬币的另一面。`needs_review` 意味着
>    **该语言的用户打开这件作品连正文都看不到**(前端只返回 published),音频缺失只是连带的。
>    过滤只省下了算力,内容缺口原样还在,而且**在"生成 28/30 条成功"这类报表里是隐形的**。
>    实测橘园 `Red-Haired Girl` 的 de/it 被挂起,各是一个真实形式缺陷:德语把
>    「Red-Haired Girl」译成「Russisches Mädchen」(作品名张冠李戴)、意语正文开头混入了
>    `Italiano:` 语言标签 —— 闸门判得对,但没人去修,这件作品对德意用户就一直是空的。
>    → **上新馆验收必须包含 needs_review 清单(按语言列出),并逐条决定"修"还是"弃"**;
>    报"成功 N 条"时必须同时报"缺口 M 条",否则缺口会被成功数掩盖。
>
>    **prod 实测基准(2026-08-31,可作新馆的预期值)**:全库 needs_review **1471 段/6.8%**,
>    涉及 **602 件**作品(613 件有内容的作品里几乎每件都至少缺一段)。
>    分馆:louvre 6.5% / orsay 7.1% / orangerie 7.8% / petit_palais 16.7%。
>    **约 7% 是常态,不是异常** —— 上新馆时按这个比例预留返工量。
>
>    🔴 **订正(2026-09-01):`needs_review` 是两类东西,上面那句「意味着内容缺口」
>    只对英文轴心成立。** 译文是从**已发布的英文**翻出来的,所以译文被挂起时
>    英文原文一定是好的 —— 缺的不是内容,是那次**翻译判定**。实测问答:
>    非英语 `needs_review` **415 条全部**属于"该对象有已发布英文问答",一条内容缺口都没有;
>    重判后 202 条(49%)直接翻案。
>
>    🔴 **再订正(2026-09-02):"英文侧 822 条才是真缺口"这句当时是结构性推断、没有实测,
>    而实测下来它只对约 72% 成立。** 抽样 120 条英文 needs_review 问答、每条跑 3 遍
>    接地闸(另取 78 条 published 作对照组):**72.5% 三遍全否决**(稳定,是真缺口)、
>    **13.3% 抖动**、**14.2% 三遍全通过** —— 最后这批是躺在挂起里的历史误判。
>    即 822 条里约 190 条(23%)可翻案,而不是 0 条。
>    → 教训不是"推断错了",是**推断被当成实测写进了契约**。凡"某侧是/不是真缺口"
>    这类判断,写进契约前必须有抽样数据;没跑就明写「未实测」。
>    → 报 needs_review 时**必须按「轴心语 vs 译文」分开报**,合成一个数会把
>    "内容没写出来"和"翻译被误判"混为一谈,而这两者的处置完全不同
>    (前者要补材料重生成,后者只要重判)。
> 16. **「首次写入」和「替换已有」是两条路,必须各自被真跑过**(2026-08-31 橘园重灌添)。
>    首次写入时 `old_key` 为空,会**整段跳过**替换分支(取旧对象大小、算偏差)。
>    上新馆永远走首次路径,于是替换路径可以潜伏很久 —— 实测第一次真替换就炸:
>    `storage.size()` 返回字节数(int),调用方却拿去 `len()`,`TypeError`。
>    更值得记的是**测试为什么没挡住**:
>    - 旧测试写成 `f(n if isinstance(n, bytes) else audio_bytes)` —— isinstance 恒为假,
>      **永远在测 bytes 路径**,恰好绕开了它声称要测的 int 路径。
>    - 新加的测试里,存储替身 `size()` 返回 `None` —— 于是替换分支同样整段被跳过。
>
>    **推论**:① 凡有「首次/更新」二分的写入路径,两条都要有测试真正执行到;
>    ② **测试替身的返回类型必须与真实实现一致**(真实返回 int 就别返回 None),
>    否则测的是一条现实中不存在的路径;③ 测试里的条件分支若绕开被测路径,测试就是假的。
>    验证办法很简单:**把修复撤掉,看测试会不会红** —— 不红就是假测试。
>
>    **第三例(2026-09-01 橘园第二批,同一形状换了个身位)**:生成侧取文本时,
>    `guide` 走 `default_guide` 分支、深度段走 `tabs` 分支。第一批只做 guide,
>    于是 `tabs` 那条分支**一次都没执行过** —— 里面把字段名写成了 `t.get("code")`
>    (实际是 `section_code`),恒为 None。后果比崩溃更坏:**它不报错**,
>    60 条深度段会被判「无文本,跳过」,状态里写 `no_text`,**看起来像内容缺失而不是 bug**。
>    → 分支不只由「首次/替换」二分,**也由输入取值二分**:小样本试点若只覆盖了一种取值,
>    另一种取值的代码就从没跑过。扩量前**先用全部取值各跑一条**(纯取数、不生成,
>    成本 1 分钟),这次正是它把 6 小时的空跑挡在了门外。
> 17. **幂等要留「修复重灌」的出口**(2026-08-31 橘园重灌添)。
>    灌入按 `audio_engine` 幂等(已是目标引擎就跳过),这对防重复灌入是对的,
>    但会挡住一个真实场景:**引擎没变,坏的是产物** —— 生成侧修了 bug 要重灌。
>    没有出口时,人就会去改引擎标记或手工改库绕过,两者都更糟(前者在数据里撒谎、
>    把覆盖率报表劈成两半,后者不留代码痕迹、下次还得再来一遍)。
>    → 提供显式的 `--force`:**只跳过幂等,质量闸与偏差检查照常跑**。
>    **通用推论**:凡"按标记判完成"的幂等设计,都要同时回答"标记没变但产物要重做怎么办"。
> 18. **判定类工具必须先用已知正/负样本验证判断力;修好工具后必须重跑全量**(2026-08-31 添)。
>    失灵的检查比没有检查更糟:它一边烧算力(重试),一边把合格内容永久标成失败,
>    而且**看起来一直在正常工作**,于是人会去怀疑模型、调模型。
>    实测:一致性打分因 `SequenceMatcher` 默认 `autojunk=True`(序列 ≥200 元素时
>    丢弃频率 >1% 的元素),把真值 0.9946 的转写打成 **0.5027**。
>    更严重的是**发现工具坏了之后的处理**:当时把 4 条低分统一标注「误判、放行」,
>    3 条确实是误判,第 4 条却是真崩坏(音频末尾复读十几遍),就这么进了 prod。
>    → **一个笼统的解释不能覆盖一批异常**;工具坏了的正确反应是修工具重跑全量,
>    而不是用人工判断替工具下结论 —— 后者恰好在最需要工具的地方丢掉了工具。
>    配套:阈值一刀切前先看分布是否该分档(实测 CJK 一致性天然低于拉丁语系,
>    是转写引擎的同音字天花板,不是内容缺陷,拿通用值卡会系统性误杀)。
>
>    🔴 **补充(2026-08-31,同日又踩):修好工具后不能只"以后重跑全量",
>    还要回头重验那些当初由它产出、已写进文档当成定论的旧结论。**
>    实例:配方里「汉字圈禁用 zh 种子」的唯一依据是"zh 念繁体一致性仅 0.58" ——
>    那是打分函数还坏着时测的。用修好的函数重测:**ZH 0.951 / EN 0.913**,
>    锐度、耗时同样 ZH 全胜;转写实证是 **en 种子**把「花點時間仔細欣賞」念成
>    「哈点时间十岁新手」。用户反馈"繁体发音很怪"才把这条线拉出来 ——
>    **坏工具的污染会沉淀成"常识",之后没人再质疑它。**
>    配套:**为绕过某个坏结论而加的补丁,在结论纠正后必须一并撤掉**
>    (这里撤了 zh-hant 放宽的两个质检门槛,它们是为迁就错误种子才加的)。
>
>    🔴 **补充(2026-09-01):「重跑全量」的「全量」要按表清点,别默认成「我手上这张表」。**
>    第 22 条修好忠实度闸之后跑的那次 recheck,384 行**全是 `object_content_sections`,
>    `object_suggested_questions` 一行没碰** —— 于是段落每语言只剩 7-20 条 needs_review,
>    问答还是 36-74 条,差了 2-4 倍。同一个闸、同一个 bug,只因为分两张表存,
>    修复只落地了一半,一个月没人发现(用户翻到某幅画 fr/it 没问答才暴露)。
>    更刺眼的是 `qa_suggester.py` 里当时就留了注释「prod 存量 595 条非英语问答卡在
>    needs_review」—— **知道有数据债、只在注释里记一笔,等于没还。**
>    → ① 修判据后先列出「这个判据写过哪些表/哪些字段」,逐个确认重跑覆盖到;
>      ② 代码注释里写下的存量数据债,必须同时进 backlog,否则它只会变成考古材料。
> 19. **质检的两端不能来自同一个被污染的源**(2026-08-31 添)。
>    音频质检拿"转写 vs 原文"算一致性 —— 但当原文自己带缺陷时,缺陷会**同时**
>    出现在两端,于是判"完全一致"直接放行。实测:4 条讲解正文开头被模型回贴了
>    语言标签(`Polish:` / `日本語:`),TTS 老实地把标签念了出来,一致性照样满分,
>    三道闸全过,灌进 prod 后用户在 App 里听到"日本語"开头。
>    → 校验只能证明"产物忠实于输入",**不能证明输入本身是对的**。
>    凡"拿输入当标准答案"的检查,都要另有一道**只看产物形态**的检查
>    (这里是:正文不得以语言标签开头)。
> 20. **形式缺陷需要形式检查 —— 内容闸一概看不见它**(2026-08-31 添)。
>    译文回贴语言标签的问题波及**全库 1091 段**(pl 289 / it 267 / de 144 …),
>    而两道闸都放行:忠实度闸看事实(前缀不改变任何事实)、语言检测闸看语种
>    (正文确实是目标语言)。根因更值得记:**翻译 prompt 的输入格式在自我诱导** ——
>    user 消息写成 `English:\n{原文}`,模型照猫画虎输出 `Polish:\n{译文}`;
>    system 里那句 "Return ONLY the translated text" 拦不住,**格式示范比指令更强**。
>    → ① 给模型的输入别用「标签: 内容」的形态(改用 XML 标签包裹);
>    ② 落库前加一道形态剥离兜底;③ **剥离只认已知白名单,不做通用的"短词+冒号"**
>    —— prod 广谱扫描证实正文合法地以 `Oto ciekawostka:`「這裡有個有趣的小知識：」
>    这类短语开头,通用剥离会削掉正文的头,**误伤比漏网糟**。
> 21. **LLM 判定的批量翻案,必须带旧判据对照组;而且要读旧判据的原文,不能只看判决翻转**
>    (2026-08-31 添)。修好一个 LLM 闸的 prompt 之后重跑存量,一部分条目会**仅仅因为
>    模型的随机性**翻盘 —— 那不是修复的功劳。做法:每条用新旧两版 prompt 各判一次,
>    `旧✗新✓`=修复归因、`旧✓新✓`=闸自身噪声、`旧✗新✗`=真该卡。没有对照组就分不清
>    "修好了"和"掷骰子掷赢了",而后者会让你**凭运气往 prod 放内容**。
>    实测(539 段忠实度复检):fix 68.8% / noise 3.3% / real 27.3% —— 噪声只有 3%,
>    归因站得住;若噪声很高,那说明闸本身不可靠,是另一个要单独谈的问题,
>    绝不能混在批量里悄悄放行。
>
>    🔴 **只看判决翻转会漏掉最重要的东西。** 判决翻转只说"变了",不说"为什么变"。
>    这次的替代解释是:新 prompt 里那句 "must NOT be reported as an infidelity"
>    可能只是把检查器整体调宽松了(与标题无关)—— 若如此,72% 的"解锁"就是批量放行
>    坏译文。**验证办法是抽样重跑旧 prompt 并读它原本抱怨的文本**:60 条里约 50 条
>    抱怨的确实是标题,归因才成立。⚠️ 所以复检脚本**必须保留旧判据的理由字符串**,
>    只存布尔结论等于把证据扔了(本次就扔了,只能补跑一轮)。
>    呼应第 6 条:小样本推出的比例,必须保留分解到各环节的原始计数与理由。
>
>    🔴 **补充(2026-09-01):判据自己也要跟自己比一遍,而且批量写入取多遍的交集。**
>    `default_complete` 用 `temperature=0.3`,同一条输入两次判定结果可能不同。
>    实测 415 条问答重判:第一遍 208 条可发布、第二遍 211 条,**两遍不一致 15 条(3.6%)**。
>    单遍的数字看不出这一层。→ 批量翻案**跑两遍取交集**(本次 202 条),
>    偏保守的一侧:误发布比误扣住糟得多。抖动率本身也是信号 —— 3.6% 可接受,
>    若两位数则说明判据不堪批量使用,该先修判据而不是先翻案。
> 22. **管线注入给一侧的规范名,必须同样告诉另一侧 —— 这是一类,不是一个 bug**
>    (2026-08-31 添)。翻译侧会把规范**标题**、规范**作者名**(未来还有馆名)当
>    glossary 注入译文以消除分叉;忠实度检查侧只被告知了标题,不知道作者名,
>    于是把注入的作者名判成错译 —— 与标题那桩冤案**同构**,只是低一层。
>    → 修一个实例时,先问"同一个注入机制还喂了什么进去",把整类一起修;
>    只修被投诉的那个实例,剩下的会继续流血(实测:147 条判定为"真不忠实"的段里,
>    抱怨的几乎全是作者名)。
>
>    🔴 **配套发现:被注入的规范名本身可能就是坏的,而且会逐段复制。**
>    `artists.name_i18n` 里 Cézanne 的 zh-hant 是「保羅**施傘**」(正解 塞尚)、
>    Michelangelo 的 fr 是「Michel-Ange **Buronarroti**」(Buonarroti 拼错) ——
>    坏名字被当权威注入,**每一段正文都抄一遍**,污染面等于该作者的全部段落
>    (实测:施傘 27 段、Buronarroti 11 段)。
>
>    **根因不是"LLM 音译瞎编",是上游 + 回退链**(2026-09-01 查清后订正):
>    这两个坏名**就是 Wikidata 实体上的标签原文**,我们一字不差抄下来的。
>    - `Q35548` Cézanne 没有 zh-hant / zh-tw 标签,`_SCRIPT_VARIANTS["zh-hant"]`
>      的链是 `zh-hant → zh-tw → zh-hk → zh`,落到 **zh-hk** 取到「保羅·施傘」;
>      再往后一格的 `zh`=保罗·塞尚 经 OpenCC 转繁本来是对的。
>      **zh-hk 是粤语拼读的另一套传统**(施傘/佐助彌/基斯杜化),排在 zh 前面等于
>      "宁可要另一个传统的音译,也不要本传统的简体转繁" —— 兜底源的**优先序**
>      是个语言学判断,不是随手排的。全库实测影响面:作者 6 位、标题 0 个。
>    - `Q5592` Michelangelo 的 **fr 标签本身**就是 `Michel-Ange Buronarroti`
>      (it 标签是正确的 `Michelangelo Buonarroti`)。这类无链可调,
>      只能靠跨语言交叉比对发现,或去上游改。
>
>    ⛔ **两条看起来很自然、实际都不成立的查法**(都实跑失败,别再试):
>    - ~~「繁简对照读音应完全相同」~~ —— 繁体**不是**简体的字形转换。外国人名音译
>      台港有独立惯例(`Jean→让/尚`、`François→弗朗索瓦/法蘭索瓦` 两边都对),
>      读音天然不等。实跑 1324 命中,绝大多数是误报。
>    - ~~「比对 Wikidata 标签」~~ —— **名字就是从 Wikidata 抄的,两端同源**(违反第 19 条)。
>      实跑:两个已知坏名**一个都没抓到**(存的值与源完全一致),
>      而 355 个"不符"里多数是合法异写(扬·范·艾克 / 扬·凡·埃克)。
>    → 真正把问题定位下来的,是**去读源数据的原始记录**(Wikidata 实体 JSON),
>    而不是继续调筛子。**筛子连着两次漏掉已知坏样本时,该怀疑的是问题模型,不是阈值。**
>
>    🔴 **元教训:写进契约的"应该这样查",必须是已经跑过并验过的查法。**
>    上面那两条错误处方,是本条纪律**初次写入时凭设想写的**(还没跑过就当结论落文档),
>    第二天实跑两条全废。契约是给后人照做的规程 —— **一条没验过的处方比没有处方更坏**,
>    因为它会让后来者把假设当定论、照着做还找不到问题在哪。
>    → 纪律条目里出现"应该检查 X"时,先问一句:**这个查法我自己跑过吗?已知坏样本它抓得到吗?**
>    抓不到就别写,或者明确标注"设想,未验证"。
> 23. **批量改动的 dry-run 必须按「改动类型」分桶,不能只给样例**(2026-09-01 添)。
>    样例是**自己挑的**:显示名清洗那次打了 12 条样例、全是「剥括号」类,
>    于是「剥引号」那一类始终不在视野里 —— 而"看过样例"给了我一种已经检查过的错觉。
>    直到写完 1217 件、在核验里才发现改坏了 36 个标题。
>    **样例是抽查,分桶才是普查**:把每条改动归类(改了什么字符/长度怎么变),
>    打出各类计数,异常类会自己跳出来问"为什么会有这一类"。
>
>    同一个方法在后续三次拦住错误,值得逐条记:
>    ① 「剥引号 36」混在「剥括号 5708」里被揪出;
>    ② B 块出现「变短(其他) 10」→ 查出老 bug 连 `»` 也吃,我的检测口径(只数 `"`)
>       低估了真实损伤(565 → 实为 737);
>    ③ C 块出现「前置补齐 53」→ 查出德语 `„…“` 里 `“` 是**闭**引号、英语里是**开**引号,
>       配对模型对这两种语言互相矛盾,差点把好标题改坏。
>    → 推论:**分桶里出现你解释不了的那一类,就是还没想明白的地方**,先查清再写库。
> 24. **清洗函数的错误会随每次回填静默累积,且永远没有报警**(2026-09-01 添)。
>    `_clean_i18n` 用 `v.strip("《》\"'…")` 剥外层引号 —— 但 **`str.strip(字符集)` 的语义是
>    「两端各自独立地剥」,不是「剥成对的包裹符」**。引号在标题**中间**时(引用某个名号)
>    会被吃掉一侧:`Coupe à décor dit "grain de riz"` → `…"grain de riz`。
>    每跑一次 names 回填就多毁一批,**没有任何报错、没有任何指标变动**,
>    prod 累积到 737 个字段才被一次无关的批量操作顺带暴露。
>    → ① 裁剪/剥离类操作先问一句:**"它会不会只削掉一侧?"**
>       stdlib 的语义要对着意图验一遍,不能望文生义;
>    ② **存量数据的缺陷要定期普查,不能等用户报** —— 这类损伤对用户是"标题有点怪",
>       没人会专门反馈,但它会一直长;
>    ③ 剥离一律**白名单**(同第 20 条):显示名的尾部括号只剥馆藏编号/生卒年/
>       作品类别或作者名,**标题自带的括号必须留**
>       (高更《餐（香蕉）》、Bruyn（長者）——剥掉就成了另一件作品/另一个人)。
>
>    🔴 **配套:同一个判据在不同语言里可能是反的。**
>    引号:德语 `„text“` / 波兰语 `„text”` / 英语 `“text”` —— `“` 在德语里是闭、英语里是开。
>    判不准的一类**整类不动**(43 个 `“”` 不配对的标题就是这么留下的),
>    宁可漏,不可伤。同第 18 条的"阈值一刀切前先看是否该分档"(CJK 一致性天然低于拉丁语系)。
> 25. **归一化在消除噪声的同时可能消除信号 —— 而且可能正好是你要检测的那一类**
>    (2026-09-01 橘园第二批添)。这是第 14 条的**配重**:14 说「比对前必须归一化,
>    否则对特定语言系统性误判」;25 说「归一化的每一步都要问:它对哪些语言删掉的不是噪声」。
>
>    实例:一致性打分的归一化第②步「去变音符」(为 `Tómate`/`Tomate` 而写)对日语同样生效 ——
>    NFKD 把 `デ` 拆成 `テ`+U+3099(浊点),浊点是 combining 字符,于是被删。
>    实测 `アメデオ` vs `アメテオ`、`ばば` vs `ぱぱ`、`だいがく` vs `たいかく` **全部得 1.000**:
>    **日语浊音读错这一整类错误,对一致性检查完全隐形。**
>
>    🔴 **要害不在日语,在于它瞎的位置。** 音色配方定 ja 用 EN 种子的**唯一理由**就是
>    「ZH 种子浊音念错(じっくり→つっくり)」—— **把关那条决策的自动闸门,恰好瞎在那条
>    决策所依据的维度上**。当初能发现是靠人工逐字看转写,不是靠这个函数;
>    这个洞从来没被这个工具自己发现过,是绕过它才看见的。
>
>    → ① 写归一化时**逐步列出「这一步对哪些书写系统生效」**,别只验手头那个语言;
>      变音符、连字、大小写、全半角、简繁,在某些语言里是装饰,在另一些语言里是音位/语义。
>    ② 修归一化**必须跑控制组**(同第 21 条):拿真实语料逐条比对新旧结果,
>      **差异必须只落在目标维度**。实测教训:第一版修法图省事用了全局 NFKC,浊点是合回去了,
>      但顺带把谚文 Jamo 也合成音节(`한`→`한`,长度 3→1),**ko 的 12 条跟着一起变** ——
>      控制组当场抓住。改成只对假名定点重组后,127 条里只有 ja 的 13 条变化。
>    ③ 修好后按第 18 条重跑全量:本例 13 条 ja 最大变化 -0.009、零条新暴露问题 ——
>      **「这个洞在这一批没造成漏放」和「这不是洞」是两回事**,前者是结论,后者是侥幸。
> 26. **你以为你在量 A,其实在量 B —— 判据的名字不等于它实际在查的东西**
>    (2026-09-01 问答闸存量翻案添)。第 20 条说「内容闸看不见形式缺陷」;
>    26 是它的镜像:**内容闸会把形式偏好当成内容缺陷去拦**,而它报出来的字段名
>    仍然叫「不忠实」,于是下游把它当内容问题处理,越修越偏。
>
>    实例:忠实度闸(`check_faithfulness`)本该查「译文说了英文没说的事吗」,
>    实际拦下来的理由却是这些 ——
>    - `Q17190953 it`:"Untranslated word 'anonimato' should be 'anonimato'"(**建议与抱怨字面相同**)
>    - `Q3793237 ko`:"'Canal du Loing' 未翻译"(法语地名,本就该保留)
>    - `Q770649 pl`:"'Tiara Sajtafernesa' 是英语没翻译"(那是波兰语)
>    - `Q2985455 pl`:"'pana Bertin' 用得不一致"(称谓风格,与忠实无关)
>
>    它查的其实是**翻译完整性/风格**,不是忠实度。两件事被压进同一个布尔值,
>    而这个布尔值决定内容发不发。后果:第 22 条把规范名喂给检查侧之后误判大幅下降,
>    但**没消除** —— 剩余 213 条里仍有 `Le Guerchin`(Guercino 的法语规范名)、
>    `Scènes des massacres de Scio`(德拉克洛瓦原题)被判成"错译"。
>    → ① 一个闸只判一件事;要判两件就出两个字段,别压成一个布尔。
>      ② **定期抽样读拦截理由本身**,而不只看拦截率 —— 拦截率正常不代表拦对了地方。
>      ③ 判据跑偏时,存量的 needs_review **不能当内容缺口结案**(呼应第 15 条订正)。
>
>    ✅ **已修(2026-09-02,PR #435/#436)**:忠实度闸改为只判事实,并显式声明"不判翻译
>    完整性,语言由别处检查" —— 因为**语言混杂本来就有 `lang_detect` 独立判**
>    (`qa_suggester` 的 `lang_ok` / `translator` 的 `_lang_ok`,两处都会 and 起来),
>    压在忠实度里是重复的、且比统计方法更不可靠的第二道。
>    重判存量 169 条非英语问答 → 翻案 86 条,非英语问答挂起率降到 0.8%;
>    拆开后判定也更稳(重判抖动率 2.4%,此前 13.3%)。
>
>    🔑 **拆闸必须带"破坏样本"组,否则验证不成立。** 本次三组对照:
>    挂起 40 条(2%→45% 通过)、已发布 30 条(100%→100% 零误伤)、
>    **人为破坏 15 条(改年份/塞捏造事实)→ 0% 通过、零漏网**。
>    只有前两组时,"45% 翻案"与"闸被拆松了"在数据上**完全无法区分** ——
>    放宽判据的每一次改动,都必须证明它还拦得住本来就该拦的。
>    配套:**职责移交要验证接收方真的接住了**(新增测试
>    `test_language_gate_catches_what_faithfulness_stopped_judging`),
>    否则"拆开"和"把检查丢了"看起来一模一样。
>
>    🔴 **配套:统计口径也会「量成别的东西」,而且更隐蔽。**
>    本次第一版 census 按 `(object_id, sort)` 把英文和译文对齐,算出"276 条翻译侧判定"
>    —— 错的。译文的 `sort` 是按**英文已发布行**用 `enumerate` 重新编号的
>    (`persist_suggested_questions`),英文 sort=1 的那条在法语里是 sort=0。
>    对齐键同名不同义,数出来的每个数字都错位。改对后是 **415,且 100% 是翻译侧判定**
>    —— 结论的方向都变了(从"一半是内容缺口"变成"一条都不是")。
>    → 跨表对齐前先**验证对齐键的语义**:找一条已知样本,人工确认两侧配对正确,
>      再让它去数几百条。本例的验证是"译文行数 == 英文 published 行数",
>      4683 个(对象,语言)对零例外。

> 27. **判定类 LLM 调用必须 temperature=0;但 0 也不等于确定**(2026-09-02 接地闸添)。
>    判定是分类不是创作。此前接地闸(`check_section`)与忠实度闸(`check_faithfulness`)
>    跟生成/翻译共用 `default_complete` 的 `temperature=0.3`,于是判定带上了随机性:
>    **同一条内容三遍重跑,13.3% 会给出不一致的结论**(prod 英语问答 120 条实测)。
>    这类噪声不报错、不进任何报表,只会在库里静默积压成"看起来像内容缺口"的挂起行。
>
>    ⚠️ **但换成 0 之后两遍一致率是 95%,不是 100%** —— OpenAI 的 `temperature=0`
>    不保证确定性。所以:
>    → ① 判定通道一律传 `temperature=0`(生成/翻译保持 0.3,它们要的就是多样性);
>    ② **批量重判照旧多遍取交集**(第 21 条),别因为设了 0 就省掉这步;
>    ③ 测试要守**装配**(谁拿到了确定性通道)而不只是签名 ——
>    "函数有 temperature 参数"和"闸真的用了 0"是两回事,后者才是会坏的那个。
>
>    副产品:同一个抽样还证伪了一个看似合理的假设 —— "问答答案 99.6% 是 1-2 句,
>    存活率阈值 0.6 退化成一票否决,所以短文本被系统性误杀"。阈值退化是真的
>    (1-2 句必须全过),但它**不是**挂起的主因:实测 1 句通过率 22%、2 句 25%,
>    几乎无差,而 published 组同样 74% 是单句、通过率 93%。
>    → **机制上讲得通 ≠ 它就是主因**。归因前先按该维度分组比一次数据。

> 28. **内容挂起不等于内容缺口;但"重生成到过闸为止"是作弊**(2026-09-02 内容缺口收尾添)。
>    prod 928 条英语正文段挂起,一度被当成"材料不足、要补材料源"。实测三组
>    (真缺口/可翻案/已发布)的材料**几乎完全一样**(material 中位 13244/12865/14167 字符,
>    wikipedia 覆盖 87%/90%/92%,wikidata 事实数 8/9/8)——**材料丰富度根本不是区分因素**。
>    真凶是那批内容 2026-07 一次性生成时 `temperature=0.3` 的**生成质量波动**:
>    写得脑补多的那次被接地闸删光,闸判对了,换一次生成就写得出接地的版本。
>    → 判"是不是真缺口"要**比较各组的材料**,不是看挂起率。
>
>    **首次重生成的通过率 = 材料能支撑的真实水平**(实测 background 89.2%、
>    facts 79.7%、significance 49.4%),差异来自 role 与闸的兼容性:
>    `analysis`("笔触/光色/构图"——站在画前看得出来)属闸放行的 IMPRESSION/FRAMING,
>    通过率 99%;`significance`("改变了什么、影响了谁")全是可核查事实主张,材料里罕有。
>
>    ⛔ **但对同一批内容跑第三次就是抽奖** —— 生成有随机性,试的次数够多总有一次蒙混
>    过闸。那不是内容变好,是用随机性绕过闸,与"正确性靠构造、宁缺毋滥"正相反。
>    **课题的终点是「闸拦下来的都拦对了」,不是 needs_review 清零。**
>    真要再降,出路是补材料源或换更强生成模型,不是重跑。
>
>    配套两条操作纪律:
>    ① **只重跑挂起的,已发布的一概不碰**。对照实测已发布内容重生成有 15.6%
>       (significance)/5%(facts) 会翻黑 —— 不设边界的"全量重跑"会毁掉现有好内容。
>    ② **整组替换的写入要有"净损失保护"**。问答译文是 delete+重建
>       (`persist_suggested_questions`,sort 按英文已发布行重编号,只能整组来),
>       新译文过不了闸就落成 needs_review、用户反而少看到内容。加了"新 published 数
>       < 旧的就跳过、保住旧的"之后,全量 790 组里**实际触发 6 组**。
>
>    🔴 **补充:「改输入后重试」与「不改输入的重生成抽奖」是两回事,不受本条约束 ——
>    但仅当同时满足**(2026-09-05 音频改措辞实验添):
>    ① 改动针对**已诊断出的具体缺陷**(逐字定位到问题位置,不是盲改),且有**跑满样本的
>       前后分布对照**证明改动确实移动了通过率 —— **单次通过是运气不是效果**
>       (实例:某候选改法过了一次 0.902,跑满 5 次中位只有 0.885,按 MAX_TRY=3 算
>       三次全废概率仍有 30%);
>    ② 落地的产物是按**标准流程、真实阈值**过闸的,不能因为整体中位提升就豁免单件阈值,
>       更不能为了让它过而放宽阈值。
>    三条都满足 → 属本条自己写明的合法出路"补材料源",不是抽奖;缺一条 → 退回抽奖范畴。
>
>    ⚠️ **改输入必须重过它原本要过的**所有**闸,不只是你当前盯着的那道**
>    (2026-09-05 实测,代价是一条已灌入 prod 的音频被撤回)。
>    实例:为让 TTS 过一致性闸,把日语正文的 `197×1690 厘米` 改成"约 2 米×17 米"。
>    音频侧一致性闸通过、通过率还从 1/5 提到 2/5 —— 但那是**面向用户展示的正文**,
>    重跑忠实度闸判 `False`("dimensions are altered from 197 by 1690 centimeters
>    to approximately 2 meters by approximately 17 meters")。改动为过 A 闸而在
>    B 闸留了洞,**而 B 闸根本不会被触发,因为改动是手工做的、不走内容管线**。
>    - 判据:问「**这段文本原本归哪些闸管**」,不是「我改它是为了过哪道闸」。
>      **手工改内容尤其危险** —— 走管线的改动会自动过闸,手工 UPDATE 不会。
>    - 优先级:**上游内容的正确性不能拿去换下游产物的过闸率**。约数确实更好念,
>      但这个取舍已被"正确性靠构造、宁缺毋滥"定死。改回精确数字后音频三次全败,
>      **接受哑段**才是正确结局 —— 上游正确 + 下游缺失,好过上游被污染换来下游齐全。
>    - 这次绕过了**所有**自动防线(手工改不触发管线闸、音频闸只管音频),
>      发现它靠的是人工评审提问。凡手工改 published 内容,**改完手动补跑一次
>      该内容归属的闸**,别指望有东西会替你跑。

> 29. **多次重试折成一条汇总记录时,各字段必须与实际保留的产物同源**(2026-09-05 添)。
>    生成脚本每条产物最多重试 N 次,写成一条摘要:`sharp`/`consist` 取"历史最优的那次",
>    `lenratio`/`atempo` 却是循环残留的"最后一次"值,而磁盘上保留的失败产物本身是
>    "最后一次"的文件 —— 三者本应描述同一份产物,实际来自三次不同尝试。
>    **只在失败条目上错**(成功条目提前 `break`,三者天然同源),所以整批统计完全正常,
>    从汇总层面根本察觉不到。
>    后果不是脏数据,是**误诊**:某条失败记录写 `consist=0.06`(读作"内容彻底跑偏、
>    无解"),拿磁盘上那个文件重算实为 `0.79`(读作"边缘失败、改措辞可能有救")——
>    险些据此把一条其实能救的内容判死。
>    → 摘要记录的每个字段必须来自"**实际保留下来的那次尝试**"(通常是最后一次,
>      因为磁盘只留最后一次);确需"历史最优"用于诊断,单独存进 `tries` 数组,
>      不混进顶层字段。
>    → 通用推论:任何"多次重试 → 写一条摘要"的批处理,先问一句"**摘要里的字段是否都
>      描述同一次尝试**",**重点查失败分支** —— 成功分支往往因提前退出循环而天然正确,
>      只有失败分支会踩这个坑。
>    (实现见 `tools/tts/batch_record.py`;与纪律 21"批量结论要留原始计数"同源 ——
>    坍缩成单个数字后,没人能倒推它错在哪,包括写它的人。)

> 30. **文档里记录的参考值,若来自"重试直到过闸"的流程,它描述的是条件分布(幸存者),
>     不能当无条件基准去比对新的单次采样**(2026-09-07 添)。
>    配方记录的三条英语锐度参考值 166/184/166,是生产批次里"≥200 打回重试(`MAX_TRY=3`)、
>    直到 <200 才采纳"的产物 —— **高锐度的尾部已经被这道闸滤掉了**。
>    新实验每个条件只生成一次(不重试),测得 176/208/219,逐一对比文档基准发现
>    三次全部偏高(+6%/+13%/+32%),一度判"环境异常、数据作废"。
>    实际上两者统计对象不同:文档基准是**过滤后**分布,新样本是**原始**分布;
>    原始分布均值天然更高,**系统性偏高是选择过程决定的必然结果,不是环境问题**。
>    最危险之处在于它伪装得极像"环境坏了" —— 偏差方向固定、三次全朝上,
>    看起来正是漂移该有的样子。
>    → 用文档/历史记录的参考值做校验前,先问一句:**这个值本身有没有经过门槛、
>      重试或筛选?** 有的话,它只能拿去比"同样跑满那道门槛"的新样本;
>      要比对原始分布,基准本身必须另留一份**过滤前的原始值**。
>    (与纪律 28 补充"单次过闸是运气不是效果、`MAX_TRY` 掩盖真实通过率"同源 ——
>    都是"重试让人只看得见幸存者"的两种表现:28 管**评估改动效果**时单样本不可信,
>    本条管**引用历史基准**时那个基准已被同一道闸污染。
>    与 ⑩ 不重叠:⑩ 说"指标要对你要区分的差异敏感",本条说"参考值属于哪种分布,
>    决定它能不能被拿来比" —— 本例中指标本身是敏感的,错在两个对象不是同一个总体。)
> 31. **容错把"故障"变成了"静默劣化" —— 凡有降级路径的依赖,都必须配主动探活**
>     (2026-09-12 Joconde 断供复盘添)。
>    纪律① 要求单源失败跳过继续,这是对的:少一个源=内容薄一点,远好过整馆停摆。
>    但它的另一面是**上游死掉不再是一次故障,而是一条 warning 加两个月的悄悄变薄**。
>    实测:Joconde 老 API 2026-07-22 断供,日志里只多了些 warning,没有任何东西变红、
>    没有任何指标变动,**整整两个月没人发现**,期间生成的内容全少一路材料,
>    最后是靠一次无关的排查顺带撞见的。
>    → ① **每个可降级的外部依赖都要有一个每天问一次的探针**(cron + 非零退出)。
>       推送式的报错不会来 —— 容错的全部意义就是不让它来,所以只能主动去问;
>    ② **探针走真正的生产调用路径**(`source.enrich()`),不自己拼 HTTP 请求。
>       自己拼的探针会与生产代码漂移:上游改了列名时它照样 200 绿着,
>       而真正的 enrich 拿到一堆 None。**走真链路 + 断言金标样本的关键字段非空**,
>       才能同时探到"域名死了"和"字段名改了"两种死法 —— 后者往往更隐蔽,
>       因为它不触发任何异常(同第 24 条:没有报错的损伤会一直长);
>    ③ **探针必须自证判断力**(同第 18 条):喂一组已知坏输入,断言它确实会喊 FAIL,
>       并让"探针自己坏了"与"源死了"用不同退出码 —— 处置完全不同,
>       而一个恒说"一切正常"的探活脚本比没有探活更糟:它让人以为这件事有人看着。
>
>    🔑 **配套:外部依赖的时间线不能信公告,要信自己的库。**
>    文化部公告的关停日是 2026-06-01,实际断供是 07-22,**差 7.5 周**。
>    按公告日划线会把 6/1–7/22 之间正常富化的约 80 件误判成"缺材料"。
>    最精确的断点探针是**本源最后一次成功写入是什么时候** ——
>    按日统计命中率会给出一个干净的阶跃(07-22 当天还 100%,07-26 起 0%),
>    比任何外部公告都准,而且是你自己的数据。
>    (与 ⑩ 同族:监视器的沉默被读成一切正常。⑩ 管"指标要能动",本条管"根本没有指标"。)
> 32. **用来论证判据的统计,必须就是那个判据本身的命中数**
>     (2026-09-14 开头断裂判据添)。第 26 条讲**判据本身**名实不符;这条讲
>    **证据与判据不是同一件事** —— 拿 A 的数据论证 B 的实现。
>
>    要解决「接地闸删掉首句后正文从半截开始」:
>    - 我统计的是:剩余首句**看着悬空**的段数 → prod guide 段 **3 段**
>    - 我实施的是:**首句被删**就挂起
>    - 真实频率:首句被删 **23 段** —— 差一个数量级
>
>    中间隔着一个我没验证的假设「首句被删 ≈ 开头看着悬空」。大量段的首句被删后
>    第二句恰好独立成立(技法描述段根本不需要总起句),于是 23 段里 **17 段是完全
>    自洽的好内容**(接地率 0.67-0.89),真断裂只有 6 段。收窄成词表判据后:
>    拦 3 段真断裂、误杀 0 段,英语发布段 120 → 147。
>
>    → ① **要实施哪个判据,就测哪个判据的命中数。** 一句话自检:
>      「我统计的这个条件,和我要写进 `if` 里的条件,是同一个吗?」
>    ② **收紧任何闸/过滤器之前,先在真实数据上「只统计不生效」跑一遍,逐条读拦下来的东西。**
>      这次的代价是两轮重跑(约 $0.8);若那批已灌音频,就是 10 语音频两次全废。
>    ③ **「宁缺毋滥」只适用于"滥"的是脑补内容。** 误伤接地率 0.89 的好内容不是审慎,
>      是误杀。我当时在代码注释里把误杀写成「少数代价,宁缺毋滥」——
>      **用原则给一个没测过的数字背书**,而它其实是主要效果。
>    ④ 结论被推翻时,**把被否决方案的数据也留下**:词表方案我一开始以
>      「表外的断法照样漏」否决,实测是漏 3 段 vs 误杀 17 段。没有这组对照,
>      下一个人还会再走一遍同样的弯路。
>
>    🔑 **配套:测试必须能区分「修好了」和「修过头了」。**
>    那版过严判据的测试**全绿** —— 因为它只钉了「该拦的要拦」,没钉「不该拦的不能拦」。
>    之后每处修正都做**双向**破坏验证:撤掉修复 → 拦截用例红;换成过严的实现 →
>    **误杀对照组红**。对照组的样本直接取自 prod 实测的真实开头,不是编的。
>    同理适用于任何"收紧"类改动(阈值、正则、黑名单、校验规则)。
> 33. **派生产物必须跟着它的源失效 —— 而且要一次找齐所有派生物**
>     (2026-09-14 译文过期添)。`_upsert_section` 里英文正文变化会清 `audio_key`,
>    却不碰译文。可**旧译文和旧音频是同一类东西**:都派生自这段英文正文,源一变就都过期。
>
>    实测:重跑小皇宫 47 件后,4 件的 **136 段** fr/de/es/it/ja/ko/pl/zh-hant 译文
>    停在 12 天前、status 仍是 `published`,而英文正文已经换了一版 ——
>    用户切到法语看到的就是对不上的内容。(这批恰好还没有音频。)
>
>    → ① 写「源变了要失效」的逻辑时,**先把这个源的派生物列全**:
>      音频、译文、嵌入、缓存、索引、下游统计。清一个漏一个**不会报错**。
>    ② 与「半个单一真相源」(生成侧 `sections_for` 有兜底、seed 侧遍历 dict 没有,#547)
>      是同族:**同一条不变量在两个地方各写一半,永远不会红。**
>    ③ 判断式:**这个字段变了,库里还有谁是照着它算出来的?**
> 34. **权威来源已有的字段,不要再让模型从非权威渠道提取一遍**
>     (2026-09-14 签名转写添)。视觉描述要求模型转写作品上的文字,签名被顺带列进了
>    转写目标。实测 18 件里 **17 件转写的是签名**,抽查两件全错:
>    - Daumier《下棋者》签的是 `h. Daumier` → 读成 `L. Beaumier`
>    - Sérusier 签名下是两位数 → 扩写成 `1890.`,而编目年代是 1920
>
>    关键不在准确率,而在**这件事本身没有价值**:签名唯一能提供的是作者和年代,
>    而编目(Wikidata/Joconde)里已经有且更可靠。**读对是冗余,读错是注入假事实**,
>    还带着「画上亲笔签着」这种极具说服力的口吻 —— 净损失。
>
>    → ① 让模型提取某字段之前先问:**这个字段权威来源有吗?** 有 → 不要让它读。
>    ② 修法是**取消这件事**,不是提高它的准确率。给它加「读不清就说读不清」
>      对画面铭文有效(编 3 条 → 编 0 条),**对签名无效** —— 模型自认读清了。
>    ③ 改后 46 件里仍有 3 件提到签名(禁令约 95% 有效),但两件转写的日期都与编目一致、
>      一件只说「有签名」,零污染。**禁令不会 100% 生效,所以它管的那件事本身必须是低危的**
>      —— 这正是「取消 > 修准」的理由。
> 35. **prompt 的禁令要长在它约束的那一拍上**(2026-09-14 补记,两次实证)。
>    同一条约束,放在 prompt 末尾当通则 vs 写进它要约束的那一句里,效果差一个数量级:
>
>    | 场景 | 通则版 | 长在那一拍上 |
>    |---|---|---|
>    | guide 结尾套话(#555) | 留出集 **8/14** 违反 | **0/14** |
>    | 看图转写铭文(#557) | 4 条里编 **3 条 / 3 条** | 编 **1 条 / 0 条** |
>
>    模型不是在违抗禁令,是在**执行那一拍的指令**,而那一拍没有约束它。
>    guide 的五拍结构里开头约束极细(7 条 BANNED OPENINGS),结尾只有一句
>    「end on a memory point or an open question」—— 于是 12/14 的结尾都是
>    「As you ⟨动词⟩ this ⟨名词⟩, consider…」这一个公式。
>    → ① **禁令挪到 prompt 末尾「归拢」等于取消它。**
>    ② 用**位置断言**钉住(约束必须落在这一拍与下一拍之间),否则后人整理 prompt 时
>      会把它挪走,而挪完**什么都不会报错**,只是效果没了。
> 36. **出包核验必须从产物里读,不能从源码里读;清单本身要有来源**(2026-09-20 v35 添)。
>    Android 出包有一组"错了就整包作废、而且构建全程不报错"的属性。核验它们时
>    **唯一有效的证据是从 aab 里把值读出来**,"我刚改过源码"不算证据 ——
>    改了源码但没参与构建、构建用了缓存、改的是另一个 flavor,都会让两者对不上。
>
>    | 属性 | 怎么从产物里读 | 错了的后果 |
>    |---|---|---|
>    | versionCode / 包名 | `build/app/intermediates/packaged_manifests/prodRelease/**/AndroidManifest.xml` | 传不上去 / 传成 staging 包 |
>    | 签名 | `keytool -printcert -file <aab>/META-INF/UPLOAD.RSA`,比对 keystore 指纹 | **静默回落 debug 签名**,Play 直接拒 |
>    | `API_BASE_URL` | 从每个 ABI 的 `base/lib/*/libapp.so` 里提字符串 | 连 localhost,开屏白屏 |
>    | **版本脚注 `kVersionFootnote`** | 同上,grep `GoMuseum x.y.z (N)` | 见下 |
>
>    ⭐ **本条的由来,是核验清单漏了最后一项,而漏的恰好就是错的那项。**
>    v35 出包时 `pubspec.yaml` bump 了、脚注常量没动,四项核验全绿、包发出去了,
>    是 CI 的 `settings_version_test` 把它拦下的。**清单是凭记忆列的,只覆盖列清单的人
>    想得到的错法** —— 所以清单要有来源:**每道钉住出包属性的测试,都必须在清单里有对应的一行**。
>    (`aapt2` 读不了 aab —— 它的 manifest 是 protobuf 不是 AXML,别在这上面浪费时间。)
>
>    ⚠️ 关于 `kVersionFootnote` 为什么是手写常量而不是读 pubspec:读它要引
>    `package_info_plus`,那是**原生插件、会改插件树**,而 #434 那个致命缺陷正长在
>    "CI 与出包机解析出不同插件树"的缝里,只有真机能发现。为一行脚注冒这个险不划算。
>    **它同时被 `feedback_repository.dart` 当 `app_version` 上报** —— 忘了改不只是
>    显示错,是之后所有用户反馈都标着上一个版本号,排查时会认错版本。
>
>    ⚠️ 构建坑:**不带 `--flavor prod` 时 gradle 会把 staging 和 prod 两个 flavor 都构建**,
>    而 flutter 工具找的是无 flavor 的 `app-release.aab` → 报
>    `Gradle build failed to produce an .aab file`。**包其实已经生成了**,这条错误信息会骗人。
>    出包命令固定为:
>    `flutter build appbundle --flavor prod --release --dart-define=API_BASE_URL=https://api.gomuseum.app`
>
>    ⚠️ 出包前先确认目标 versionCode 在 Play 侧没被占(号被占时别逐个 +1 猜,
>    去 Console 看最高号一次到位 —— 曾为此白出三版包)。
> 37. **⛔ 共享实体不许被单件/单馆操作顺带改写;动 prod 前的备份范围 = 这条命令真正会写的所有表**
>     (2026-09-26 跨馆作者事故添,**最高严重级**)。
>
>    **事故**:为调小皇宫 guide 文本,对 TOP20 跑了三次 `onboard generate --force`。
>    `generate` 听起来只动作品,但 `--force` 会顺带把**作者实体**(`artists` 表,按 QID
>    **全库共享**,卢浮/奥赛/橘园/小皇宫共用同一张作者卡)整篇重写:
>    - 14 位作者的简介被重写,**6 位作者 40 条简介音频从库里脱钩**
>      (大卫、安格尔各 10 语,西斯莱、塞尚各 8 语,曼特尼亚、丢勒各 2 语)
>    - 库尔贝的代表作被换成小皇宫那几件(《世界的起源》→《浪中女人》),安格尔中文名被改写
>    - **所有馆的作者卡一起变** —— 而动手前只备份了「以为在改的」作品表,没备份作者表
>
>    ⚠️ **这不是第一次**:同一 bug 在 **2026-09-14**(小皇宫 47 件 `--force` 重跑)就发作过,
>    西斯莱/塞尚的 en、zh 与库尔贝/雷诺阿的 zh 简介音频当天脱钩,**12 天没人发现** ——
>    直到用户点库尔贝的中文简介发现「不像之前秒播」(那是点击时懒生成的 tts-1)。
>    那次早于备份保留期(约 9 天),原文找不回,只能重录(见 `docs/ops/audio-followups.md` P0)。
>    **「损坏没人发现」和「损坏本身」一样严重。**
>
>    靠当天 04:20 的每日 `pg_dump` 才逐字段恢复(14/14 独立比对零差异;40 个音频文件
>    都还在 R2,只是库里的 key 被清了 —— **若期间有人跑了孤儿音频 GC,就永久丢失**)。
>    代码层根因:`pipeline._enrich_artist_and_titles` 的判断是
>    `if art is None or force or …`,且重写分支用**这一件作品**上的 `artist_en`/代表作
>    覆盖作者行;而原测试 `test_generate_object_force_refreshes_artist_bio`
>    **断言的正是这个行为**(绿着的),所以没有任何东西会拦。
>
>    → **规则**(必须全部遵守):
>    ① **共享实体只补缺、不覆盖。** 共享实体 = 任何按 QID/键被多件作品或多个馆引用的行
>      (现为 `artists`:bio / bio_audio / name_* / birth / death / nationality /
>      notable_works / *_i18n)。单件或单馆的命令(`generate --force/--qid/--limit`、
>      `translate`、懒生成)**只能写缺失字段**;`--force` 的语义是「重做**这件作品**的内容」,
>      **永远不包括共享实体**。确需刷新共享实体 → 另写**独立的显式命令**,执行前打印
>      「受影响的馆 × 件数 × 会作废的音频条数」,由用户确认。
>      (已落地于 `pipeline._enrich_artist_and_titles`,由 `test_force_regeneration_never_rewrites_a_shared_artist`
>      与 `test_existing_artist_with_junk_bio_only_fills_missing_fields` 钉住;撤掉修复两条都红。)
>    ② **动 prod 数据前先列「写入面清单」,按代码列,不按命令名猜。** 读这条命令的实际
>      写路径:会写哪些表 → 哪些行 → 这些行还被谁引用(其它馆 / 音频 / 译文 / 缓存)。
>      命令名是意图,不是写入范围(`generate` 写了 `artists`,`--force` 清了 `bio_audio`)。
>    ③ **备份范围 = ② 列出的全部表**,不是「我以为在改的那张表」。并先查一次
>      「这批操作会作废多少条已有音频」—— 不为 0 就停下来问用户。
>    ④ **兜底恢复路径**:每日 04:20 `/opt/gomuseum/backups/gomuseum_YYYYMMDD_042001.sql.gz`
>      (prod `pg_dump`,留约 9 天)。**只恢复受影响的行/字段**:从 dump 里截出该表的
>      `COPY` 段本地解析,按主键逐字段写回,再**独立重拉一次 prod 与备份比对**(不信恢复脚本
>      自己的校验)。音频类字段恢复前先确认 key 指向的 R2 文件都还在。
>    ⑤ **音频 key 被清空后、恢复完成前,禁止跑孤儿音频 GC**(`gc_orphan_audio.py`):
>      那些文件此刻正是「孤儿」,GC 会把可恢复的事故变成不可恢复的。
>    ⑥ **测试若断言了「覆盖共享实体」这种行为,它是在钉漏洞,不是在钉功能**
>      (同纪律 33 与匿名成本洞那一课:测试钉不住「这个行为本身就不该存在」)。
>      改共享实体相关逻辑时,先 grep 有没有测试在断言旧行为,有就改写成反向钉住并写明来由。

>    ⑦ **机制(不靠人记得):四层防线**(2026-09-26 落地;规则①-⑥ 是教训,这里是**强制执行**)。
>
>    | 层 | 机制 | 位置 |
>    |---|---|---|
>    | ① 拦 | **音频损失闸**:`SessionLocal` 的 `before_flush` 钩子,任何写入让已有音频「有 key→没 key」(段/问答置空或删行、作者 `bio_audio` 少了语种)即判损失。**脚本进程默认拦截**(抛 `AudioLossBlocked`,继承 `BaseException` 以穿过管线里的 `except Exception`),需显式 `--allow-audio-loss N` / `GOMUSEUM_ALLOW_AUDIO_LOSS=N`(是**额度**不是开关);web 进程只记账不拦。换成另一个 key(重录)不算损失 | `app/services/audio_guard.py`、`app.core.database.guarded_sessionmaker` |
>    | ② 存 | **音频失效台账** `audio_invalidations`:每条损失记 key、所属行、语种、**当时的文字**、命令。GC 不删台账里 60 天内的 key | `app/models/audio_invalidation.py`、`scripts/gc_orphan_audio.py` |
>    | ③ 查 | **每日音频盘点**:备份后比对每个音频槽位的 key,「昨天有今天没」即告警邮件(`OPS_ALERT_EMAIL`);台账里没有的标「来源不明」(最高级,说明有写入绕过了闸);发不出去就不推进基线 | `scripts/audio_inventory.py`、`backup.sh` |
>    | ④ 退 | **跑前快照**:`onboard generate/translate --target prod` 开跑前打印写入面清单(含已有音频条数、跨馆作者数)并把这批行快照到 R2 `ops-snapshots/`;**备份保留** 7 天 → 14 天每日 + 8 周每周 | `scripts/ops_guard.py`、`backup.sh` |
>
>    配套约束(由测试钉住,违反即 CI 红):
>    - **不许直接调 `sessionmaker(`**,只用 `guarded_sessionmaker`(补语种线程池曾自建会话、绕过闸)
>      —— `tests/unit/test_no_bare_sessionmaker.py`
>    - **不许用 `query(...).delete()/update()` 批量语句删带音频的行或改 audio_key**(绕过 session 事件);
>      `persist_suggested_questions` 已从批量删改为逐行删
>    - 两次事故的写法(正文重写清 audio_key、作者 bio_audio 被 pop)都有用例复现并断言被拦
>      —— `tests/integration/test_audio_guard.py`

---

## 音频批量生产与灌入(引擎无关;任何自托管/云端引擎都适用)

> 本节只写**与引擎无关的流程与约束**。具体某个模型的调参配方(种子、cfg、音色指标)
> 见 `tts-voice-production-recipe.md`,那是可替换的实现;本节是稳定的接口面。
> ⚠️ **本节采用独立的圆圈序号,不与全文「纪律 N」共享编号空间**——已知撞号三处:
> **⑪**(本节="排队前先做文本体检",§上新馆配方="小馆 popularity 排序失真")、
> **⑫⑬**(本节="合格判定必须可校验"/"重试两轮为限",§工程与运维纪律="不可逆批量
> 操作的护栏"/"替换已有内容必须过质量闸" —— 后者被 `material.py` 等处的代码注释按号引用)。
> **代码注释引用时务必带上节名**,只写「⑪」「⑫」指代不明。

**① 三段式分工(生成机永不碰 DB 和 R2 凭证)**

```
① VPS 出队列   audio_queue_cli.py plan --museum X --langs ... --json > jobs.json
② 生成机产文件  严格按 jobs.json 的字段命名,不自造规则
③ VPS 灌入     audio_ingest_cli.py --jobs jobs.json --dir ./out --engine <名>
               (默认 dry-run;确认无误再 --apply)
```

**幂等按 `audio_engine` 判断**:已是目标引擎的直接跳过,所以重复执行无副作用。
**修了生成侧的 bug 要重灌时加 `--force`** —— 引擎没变,坏的是产物,幂等会挡住修复。
`--force` 只跳过幂等,质量闸与偏差检查照常跑。
自托管生成跑在另一台机器(如 Mac mini),那台机器**不该也不方便直连 prod**。
这样分工的收益:生成侧零凭证、零 DB 依赖;灌入侧统一过闸、统一写 `audio_engine` 标记。

**② 文件名约定 = `{qid}__{language}__{section}.mp3`(双下划线)**
- `section` 取值:`guide` / 深度模块 code / `qa_{sort}` / `artist_bio`
- ⚠️ **`artist_bio` 的 qid 是作者 qid**(音频按作者共享一份,同作者的多件作品复用)
- ⚠️ `qa_{sort}` 的 sort 是**库里的 sort 值**,不是生成侧自己的编号
- ⚠️ **QA 音频是「问+答」连念**(`f"{question}\n\n{answer}"`),分开念会内容对不上、
  且质量闸按问答总字数估时长会判太短
- **命名不要自己推导,一律从 jobs.json 的字段拼**——队列给什么就用什么,零猜测

**③ 比特率必须与质量闸的假设一致(当前 160 kbps)**
质量闸用「字节数 ÷ 比特率」估时长(刻意不引 ffmpeg,要跑几万次)。若产出用了更低的
比特率,估算时长会被系统性低估 → 误判为"截断"。实测:96 kbps 会让韩语 ratio 掉到
**0.44**(门槛 0.45)直接被拒,其余语言的安全余量也被吃掉 40%。
**推论:改比特率 = 改质量闸的前提,两者必须一起考虑。**

**④ 上量前先做端到端小样本(1-3 件),不要一次灌全量**
橘园试点(3 件 × 10 语)一轮就暴露了三个真问题:队列没过滤发布状态、校验函数的归一化
缺陷、生成机的显存泄漏。**这些在"生成 30 段"时是小麻烦,在"生成 3 万段"时是灾难。**

**⑤ 幂等与断点续传是必须特性,不是优化项**
批量音频动辄十几小时,中断是常态(网络/重启/OOM/手误)。生成侧要"见到已存在的合格
文件就跳过",状态实时落盘;灌入侧本身幂等。**没有这条,中断一次就得从头再来。**

**⑥ 长时间批量要分块换进程(实测:MPS 连续生成 26 段后显存耗尽崩溃)**
不是模型 bug,是长驻进程的显存碎片/泄漏。分块跑(每块 10-20 段一个新进程)最省事,
不用改模型代码。**任何长跑的 GPU/加速器批任务都该考虑这条。**

**⑦ 灌入是 prod 写操作 —— dry-run 报告 → 人工批准 → apply**
`--apply` 前必须先看 dry-run 的过闸统计。橘园试点 28 段全过闸后才执行写入。
**重灌场景要跑两次 dry-run**:先不带 `--force`(应当全部 `already_done`,证明幂等这道
防误覆盖的锁还在),再带 `--force`(看质量闸判定)。两次都对了再 `--apply`。

**⑧ 语速对齐是生成侧的责任,落库侧不是安全网**
质量闸的 `CHARS_PER_MIN` 与生成侧用的是同一套字/秒基准,但它刻意放宽到
`ratio ∈ [0.45, 2.2]` —— 只拦数量级错误(截断/无限重复),**+15% 的系统性偏快稳稳通过**。
实测橘园 28 条 guide 全部偏快 15%(某语言 +23%),质量闸一条都没拦下,是用户耳朵先发现的。
→ 凡"目标形态由基准定义"的产物(语速、图片尺寸、字数),**对齐必须在生成侧做完**;
落库侧的宽闸是防灾,不是防偏。
**同一条原则的第二个实例(2026-09-08):响度。** 全量实测已灌入的 568 条,同一件作品
同一语言内部的响度极差中位 4.1 LU、最大 15.3 LU(76% 的组 >3 LU)。生成侧三道质检门
(锐度是比值/一致性是转写/长度比是字数)与灌入侧的 `check_audio` **全都对音量免疫**,
一条都拦不下。→ 已在生成侧加纯增益归一(配方 §二「响度对齐」)。
**踩第二次说明这是个类,不是个案:新增任何批量产物,先问「它的哪些维度没有任何一道闸在看」。**

**⑧-b 验证灌入结果:查 DB 的 `audio_key` + R2 对象大小,别走 `/content` 端点**
`/content` 按设计**从不下发 `audio_url`**(付费墙,只给 `has_audio` 布尔;音频链要走带鉴权的
`/audio`)。拿 `audio_url` 去验会得到一片假失败 —— **这个坑 2026-08 踩了两次**,而且契约
`default_guide` 字段处早已注明。教训不只是"看文档",而是**提示要放在会被读到的地方**:
排查灌入的人打开的是灌入章节,不是字段字典。
→ 正确做法:比对本地文件与 R2 对象的 **md5**(不是字节大小,理由见 ⑩),
既绕开鉴权、又验的是落库真相、还不受任何缓存干扰。
脚本 `backend/scripts/verify_audio_md5.py`(在 prod 容器内跑)。
⚠️ **三类段落的 audio_key 在三张不同的表**(section / qa / `Artist.bio_audio`),
核对必须三张都查 —— 旧版脚本只查 section,对 qa 与 artist_bio 完全失明**而报告照样全绿**
(2026-09-14 卢浮宫七语那批 285 条里,这两类占 111 条 = 39%)。
旧脚本还只在生成机本地(`~/tts-lab/`)、用字节大小校验,两个问题一起修掉了。

**灌入后同样要三方核对**(2026-09-04 添):CLI 报的 `written` 数 ==
DB 各类计数的增量(正文/问答/作者介绍分别数) == 逐条 md5 核对通过数。
三条独立路径都对上,才算灌入成功 —— 单看 CLI 自报的 `written` 不足以采信,
它只知道自己写了什么,不知道 DB 和对象存储的终态。

**⑨ 替换会生成新 key(不是原地覆盖),这带来两个后果**
`audio_key()` 带不可推测随机后缀,所以替换后 key 变了:
- ✅ 前端拿到的是新 URL,**不存在 CDN/App 缓存住旧音频**的问题;
- ⚠️ 旧对象即刻成孤儿(存储成本可忽略,见 [[r2-orphan-audio-gc]]);
  换个角度,**孤儿在验证期是回滚退路** —— 把库里的 key 改回去即可。

**⑩ 验证指标必须对「你要验证的那个差异」敏感 —— 否则通过等于没验**
(2026-08-31 添)重灌 3 条繁体音频(换了音色种子)后用字节大小校验,报告"30 条一致、
0 条不符",看起来完美 —— 但那 3 条替换前后的字节数**一模一样**:CBR mp3 的字节数 =
时长 × 比特率,而语速对齐把两版的目标时长压成了同一个值。**换种子改变了每一个采样点,
却一个字节都没多**。改用 md5 才真正验到内容。
→ 选校验指标前先问一句:**"如果这次改动失败了,这个指标会不会变?"** 答不上来,
这个指标就不能用。同类:改文案后比对字数、改配置后比对文件行数 —— 都可能恒等。
这是纪律 19 的延伸:19 说"两端不能同源",⑩ 说"**指标要能动**"。

**⑪ 排队前先给文本做体检;有问题的藏品/段落不进本批,修完再排**
(2026-09-03 添)音频是**不可逆的重资产**:十几小时机时 + 生成一次永久落库 + 用户直接
听得到;文本是可逆的轻资产,改一次就是几分钱。**让重的等轻的,不要反过来。**
何况文本一改 `audio_key` 就被置 NULL(配方 §七),抢跑的音频等于白跑。

体检查三类,**都要用查询查,别靠翻页看**:

| 类型 | 查法 | 处置 |
|---|---|---|
| **空洞** | 每 (件,语言) 的段数是否相等;`length(btrim(body))=0` | 少几条不害人,但要知道自己少了什么 |
| **挂起** | `status='needs_review'` 分布 | 队列只取 `published`,翻案后必须补跑(配方 §七) |
| **污染** | `museum_objects.artist_en`(**是表的列,不在 `attributes` 里**) vs `artists.name_en`(按 `attributes->>'artist_qid'` 关联) vs `attributes->>'artist_extract_en'` **三者是否同一个人** | 🔴 **剔出批次**,修数据+重生成内容后再排 |

**污染最危险,因为它一切看起来都正常**:音频过闸、音色一致、听感完美,内容却接地在
错误的材料上。2026-09-03 橘园 Q64309678《静物,梨和青苹果》—— 10 语 78 条正文全在讲
"梵高的医生保罗·加歇画了这幅静物"(真作者是塞尚,橘园官网写着)。若不体检就排队,
780 秒错误归属的音频会永久落库,而且**没有任何一道自动闸会拦**:质量闸判时长和语速,
忠实度闸判译文对不对得上英文原文 —— 英文原文本身错了,它一路绿灯。

反过来说,能发现它是因为**闸抓到了不一致**(fr/guide 与 zh-hant/facts 被拦),
所以修复时必须小心:只让各路径"取到同一个人"而不管取得对不对,会把**可检测的不一致
变成不可检测的一致错误**,比不修更糟。→ 见 `material.resolve_creator_qid`。

⚠️ "先把能跑的跑了,坏的以后再说"是对的,但坏的**不许捎上** —— 批次里混一件坏的,
事后要么整批重灌,要么留一件永远对不上的。**剔出来的成本是零,捎上的成本是重来一遍。**

🔴 **体检查询查出一片空值时,先怀疑查询,不要判定"这个字段没用了"。**
(2026-09-04 实测)我把 `artist_en` 当成 `attributes` 里的键去取,10 件全是 NULL,
于是记下"字段名不对或已不再使用",**差一点去"修正"这条本来正确的规程**。
它其实是 `museum_objects` 的列。空结果与"检查通过"在输出上无法区分 ——
**任何体检查询都要先在一条已知有值的记录上验一遍取数路径**,再拿它去扫全批。
(同一族错误:监视器的沉默被读成一切正常。见纪律 ⑩ 与 §四的判定工具纪律。)

**⑫ 只有生成侧判定合格的产物,才允许出现在灌入目录(2026-09-04 添)**

灌入端的 `check_audio` **只判时长比例与替换偏差,不看锐度、不看内容一致性**。
生成侧唯一的质量判据(过没过闸)在灌入侧没有任何对应护栏 —— 目录里放什么它就传什么。

实测后果:一条从未过闸的音频(`Q3937645/ja/qa_0`,一致性未达 0.90)灌进了 prod,
而当时**md5 逐条核对 127/127 全过** —— 核的是"上传的和本地一致",
**验不出"本地这条本身没过闸"**。校验方向错了一维,通过得毫无悬念。

→ 生成脚本必须把三次仍不合格的产物**移出灌入目录**(移到 `_rejected/`,
不是删除 —— 删了就查不出失败原因,实测当天就撞上)。
→ 但"文件放对位置"是**约定**不是**校验**。上规模后同时操作多个输出目录、
多轮重跑、跨机传输,拷错目录只是时间问题。

✅ **已落地(2026-09-04,#478)**:判定结论随文件一起交付并被灌入端核验。
载体就是 `_state.json`(生成侧本来就写,现在多记一个 md5),
灌入端读 `--dir` 下的它,三类拒绝并计入独立的 `unverified` 计数:

| 拒绝原因 | 对应的现实失误 |
|---|---|
| 判定记录里没有这条 | 拷错目录 / 别批残留 |
| `ok != true` | 生成侧三次都没过闸(漏进 prod 那条正是这形态) |
| md5 与判定时不符 | 传输损坏,或文件被换过 |

`--apply` 却没有 `_state.json` → **报错退出**。悄悄降级成"不校验"等于没装护栏,
失败方向必须是拒绝;外部引擎产出确实没有该文件时,显式加 `--allow-unverified`。
老批次的 `_state.json` 没有 md5 字段 → 跳过这一项,不把它们全判成不合格。

**⑬ 重试两轮为限,之后接受诚实缺口**

实测收益曲线(2026-09-04,570 条):脚本内每条已重试 3 次;此后**整体重跑第一轮
救回 5/8,第二轮 1/2,第三轮 0/2**。前两轮吃的是生成随机性的红利
(有过连续 4 次同一坏值、第 5 次通过的实例),再往后是真缺陷。
→ **两轮为限**,剩下的按纪律 28 记为内容/质量缺口,不许重跑到过闸为止。
上百件规模下这条决定了会不会在个别条目上无限烧机时。
**缺口与"机器判不了对错"的疑点一律记进 `docs/ops/audio-followups.md`,攒够一批统一处理** ——
逐条追要么打断跑批、要么被忘掉;而单条看是"这句念错了",攒够才看得出是不是系统性的。

**⑭ 上规模按件灌入,不要攒到最后一次性灌(2026-09-04 添)**

实测节奏:568 条(= 570 条任务 − 2 条按 ⑬ 记为缺口)≈ 生成 11h + 传输 31min(535MB) + 灌入 12min + 核对 12min,
**生成占 95% 墙钟时间**。100 件约 5700 条 → 生成 4-5 天。
攒到最后灌意味着中途任何意外都让几天产出滞留在生成机上,且失败排查面是全批。
→ 按件(或每 N 件)走完"生成→重试→核账→传输→dry-run→批准→apply→核对",
价值尽早落地、故障局部化。
⚠️ 推论:**想提高一次性成功率,该优化的是生成本身(种子/音色),不是流程**。
⚠️ 但「闸拦下的都是真实质量问题」这个前提本身要验:2026-09-14 九条盲听证伪了
英语锐度闸 —— 它拦下的内容人耳判「可接受」,放行的反而有判「不可接受」的,
门槛 200 只是切在一团连续分布正中间。**一道闸的拦截量不等于它的判别力;
在拿它当优化方向之前,先用人耳盲听验一次这道闸测的是不是你以为的东西。**
(依据见 `docs/architecture/tts-voice-production-recipe.md` §四)

**⑮ 灌入前核账(2026-09-04 添)**

```
_state.json 里 ok=true 的条数  ==  灌入目录里的文件数        ← 必须精确相等
原始任务清单条数 − 上面这个数  ==  按 ⑬ 记为缺口的条目数      ← 差额必须能对上
```

⚠️ **第二个等式不能写成"清单条数也相等"** —— ⑬ 明确允许存在诚实缺口,
所以 ok 数**合法地**小于清单数(本批 568 vs 570)。写成三者全等会与 ⑬ 互锁:
遇到 ⑬ 认可的正常收尾也要"停下查",而查不出任何问题。
**要核的是差额有没有解释,不是差额为零。**

这几个数来自三条独立路径(生成侧判定 / 文件系统 / 队列),
对上了才说明没有漏条、没有多带、**没有拷错目录**。任一对不上,停下查,不要开始传输。

**⑯ 起跑前锁定生产脚本版本:先 `git pull`,再开跑(2026-09-12 添)**

长批一跑十几小时,**中途发现代码是旧的 = 那一段白跑**。2026-09-10 卢浮宫 TOP3-10
用落后 3 个提交的本地 `staging` 起跑,缺 PR #521(zh-hant 喂简体给模型),
12 条繁体是**按已知且已修好的缺陷**生成完才发现的。

唯一把它暴露出来的信号是 `_rejected/` 里一条**确定性失败三次**的条目 ——
随机失败会被重试掩盖,只有确定性失败才浮得上来。**别指望质检闸告诉你代码旧了**:
闸判的是产物合不合格,旧代码完全可以产出合格的东西,只是不该长这样。

→ 开批固定三步:`git pull` → 确认跑批入口指向仓库里那份脚本(不是本机旧副本)
→ 把起跑 commit 记进批次日志。
→ 这次的根因是多个 session 共用同一个工作区分支。**并行 session 各切各的分支/worktree。**

---

## 内容体系（三层 + 模块库 + 证据包）

> **⚠️ 英语轴心（en axis）——核心生成约定,所有生成内容必守。**
> 一切内容(讲解 / 深度模块 / 问答 / 作者 bio)**先用英语生成**(prompt 必须含 "Write in English")→ 三类**接地闸在英语上判** → 再 `translate_object` 翻成 zh/fr/…(各自忠实度校验)。
> **为什么**:接地/事实校验只在英语做一遍,其它语言靠忠实翻译继承 → 省、且跨语言一致。
> **教训(2026-07-02)**:新加 `generate_artist_bio` 时漏了 "Write in English",作者 bio 随材料语言乱输出(en 条存成中/法文),切语言不对。**新增任何生成 prompt 都要强制英文。**
> **不变量:译文的发布状态从属于它的英文源。** 英文段不再 `published`,同段译文必须一起下架 ——
> 译文的正当性正是"接地闸在英语判过一遍"给的,源头没了,正当性就没了。
> 由 `content_repo._upsert_section` 级联保证(所有段落写入的唯一咽喉),**不靠人记得去查**。
> 教训(2026-09-03):橘园 Q64309678 修完作者归属重生成,英文 significance 因整段无材料支持
> 被闸清空,9 条译文原地留存 —— 正文已换成塞尚,significance 还在讲梵高的医生加歇,且仍在下发。
> `translate_object_language` 只翻**已发布**的英文段,所以译文既不会被重写、也不会失效。
> 当时 0 条已灌音频纯属侥幸,再晚一步就会被灌成音频、永久落库。

> **⚠️ 多语显示名规则(title / artist.name)——语言无关,核心约定。**
> 任何**显示名**(藏品标题、作者名)按请求语言解析,走固定回退链:
> **① 该语言的权威标签**(Wikidata 多语 labels / 馆方官方标签,专有名词最优)→ **② 从英语轴心机器翻译**(该语言无权威标签时兜底)→ **③ 英语轴心原名**(永不空)。
> **语言无关**:新增语言(de/es/it…)**只加进 `DEFAULT_LANGUAGES` 语言集**,生成时一次性抓该实体全部目标语言的 Wikidata labels、缺的从 en 翻译 —— **同一套机制、零 per-language 代码**。这就是"加语言=加配置"。
> **配套**:**QID 是全系统匹配键**(识别/查询/去重/跳转都用 qid);显示名纯展示,名字回退绝不影响匹配。避开脏格式(如 Joconde 的 "Lastname First (dates)")。
> **非 Wikidata 条目的身份(✅2026-07-16 #255 定)**:无 Wikidata 条目的件,qid 位=**合成把手 `<source>-<reference>`**(如 `joconde-000SC010033`)——对外稳定、全系统当 qid 同键用(识别/搜索/跳转零分叉);内部检索按 UUID 回查。`is_wikidata_qid()`(Q+数字)做门控:合成把手**跳过一切 Wikidata SPARQL**(省成本+避免 `wd:<合成号>` 垃圾查询)。**names 配方**:这类件无权威标签可抓 → 直接走显示名规则②(title_en 轴心纯翻译,staging 实证 1038 件×10 语零失败);**无 title_en 则无轴心 → 留空不硬造**(宁缺毋滥)。上新馆接任何非 Wikidata 源(区域目录/官方馆藏库)照此,零核心改动。
> **⭐ 多语字段的读写对称性(2026-07-26 卢浮宫定,两处实测事故)**:显示名的**写入落在 i18n JSON**
> (`attributes.title_i18n` / `Artist.name_i18n`),**读取必须 i18n 优先 + 列兜底,且所有语言维度对称**。
> 不对称 = 数据在库里却到不了用户,而且流程全绿、数字漂亮、无声无息:
> - `title_zh` 早做了 i18n 优先,`title_en` 只读列 → 卢浮宫 **9505/17283 件列空(其中 9497 件 i18n
>   里其实有英文名)**,英文用户看到一半藏品标题空白(#323 修)。
> - 作者本地化名真相源是 `Artist.name_i18n`,馆包却只读 `MuseumObject.artist_*` 裸列(详情接口早用
>   `_resolve_name` 做对了、馆包漏了)→ 卢浮宫 **10041 件作者名中文视图全显拉丁文**。
>
> **自查**:新增/改动任何多语字段时,列出"谁写、谁读",两边必须同源;`_resolve_name(i18n, lang,
> {列}, 兜底)` 是统一入口,别绕过它直接读列。
>
> **⭐ 外部标识符必须校验格式,不能盲取 URI 末段(2026-07-26 卢浮宫事故)**:Wikidata 对
> **"未知值"(somevalue,如作者不详)返回 blank node** `.well-known/genid/<32位hex>`,
> `rsplit("/", 1)[-1]` 会把哈希当成实体 QID —— 卢浮宫 **4642 件古物**(无名工匠)被塞进假作者
> 身份,还为它们批量建 Artist 行撞 `artists_pkey` 崩溃。**凡从 URI 取 QID,一律过 `is_wikidata_qid()`
> 门控**(项目本就有此门控,`_fetch_creators` 漏用)。推广:任何外部源的"未知/无值"都可能以特殊
> 形式返回(blank node / 空串 / 占位符),解析时校验格式、不合格就留空(宁缺毋滥),别让脏 id 进库。
> 奥赛没暴露是因为画作作者绝大多数明确 —— **异质数据(古物 vs 名画)也是一种"规模"**。

> **⭐ 同一命令多实现路径必须功能等价(2026-07-26 定)**:`names` 有 sync 与 `--use-batch` 两条路径,
> **batch 路径漏了 P170 作者解析**(只读已有 `artist_qid`、从不抓取)——新馆 catalog 不产 `artist_qid`,
> 于是 Artist 行一个都建不出来(卢浮宫 17283 件仅 1 件有)。**"用哪条路径跑"绝不该决定数据完整性**。
> 加/改批命令的任一路径时,逐项对照另一路径的能力清单;差异必须在契约里显式声明并给补救命令。
>
> **⭐ 不可假定 en 轴心存在(2026-07-26 定)**:非英语源大馆 catalog 后 `title_en` 列大面积为空
> (卢浮宫 **55%**,藏品多只有法语标签)。管线任何一步都不能假设"英文一定有"——轴心取
> `ti["en"] or 任一已有语言`,回退链要能从法语等源语言起步。

> **解析时机 = 铺目录时(2026-07-03 定)**:显示名是**目录元数据、不是生成内容**——catalog 后立即跑 `onboard.py <slug> names --target <env>`(幂等可重跑),全馆补齐 `title_i18n`/`artist_qid`/Artist 名字行;stub 一进目录就有完整多语显示名,**不等内容生成**(此前只在 generate 时填,导致列表页大量 stub 在 zh 视图显英/法文名)。generate 时同一机制增量修补。en 也权威优先(纠正目录把非英文标签误存 title_en,如 "Régates à Argenteuil")。

> **⚠️ 本地化完整性原则(2026-07-03 定,分类标签教训)。**
> - **凡端点返回的用户可见文本必须随 `language` 本地化**:内容类走生成/翻译管线;**固定小集合**(分类标签/段落标签/材质名/`all` tab 等)用**静态翻译表一次配齐全部 `DEFAULT_LANGUAGES`**——不许只配部分语言(三语时代的表在六语开放后成洞)。缺译回退 en、**永不 null**。
> - **机器码字段永不翻译**:`code`/`qid`/`section_code` 是前端逻辑键(筛选/路由/匹配),显示一律靠 `label`——由此所有文案改进都是 server-driven、免发版。
> - **"加语言" checklist(加语言=加配置的完整清单)**:① `DEFAULT_LANGUAGES`+`LANG_NAMES`(lang_config)② 静态标签表(`_CATEGORY_LABELS`/`_ALL_LABEL`/`SECTION_LABELS`)③ 非拉丁文字语言扩 `_clean_i18n` 文字检测(现查 zh/zh-hant 汉字)④ 前端 ARB 文案+语言选择器(kSupportedLocales)⑤ 存量回补:显示名跑 `names`(幂等),讲解/问答跑 **`onboard.py <slug> translate --target <env> --langs <新语>`**(✅已实现:从已存 en 段**纯翻译**落库,忠实度校验继承接地,不重生成;幂等只补缺)。
>   **⑥ 仅字形变体语言**(如 zh-hant——同一语言不同字形、可 OpenCC 逐字确定性转换):额外在 `material._SCRIPT_VARIANTS` 加一行 `(标签变体优先序, OpenCC 方向)`;前端注意繁体 API 参数发 `zh-hant` 非 `zh`(见 handoff 4 陷阱)。**独立语言不适用**——即便相似如加泰罗尼亚/葡萄牙语,词汇不同、不能逐字转,走普通翻译路径(①–⑤)即可。判据:两形式间有无"确定性逐字保词转换"。
>   **⑦ 加完必跑质量验收(强制,非拉丁语言尤其)**:按 `docs/i18n-translation-quality-checklist.md` 跑**完整**六检查点——**扫全部已发布段**(guide+深度模块+QA,不只 guide),核残片/标题多通路一致/显示名质量/忠实度/字形。残片命中要分辨专有名词语境(机构官方名=误报)。教训:2026-07-06 只跑子集漏了 zh-hant 深度模块残片,用户审计才发现。**加语言不验收 = 没加完。**
> 教训:2026-07-03 六语开放,分类标签表只配了 zh/en/fr → de/es/it 真机标签栏中英混杂(前端交接件修复,#142)。
> - **加非拉丁语言(ja/ko/zh-hant)前跑翻译质量验收**:见 `docs/i18n-translation-quality-checklist.md`(六检查点:残片/专名一致/标题多通路一致/显示名质量/忠实度/字形;全语言无关机制)。
> - **语言一致性(2026-07-10 定,核心差异点)**:所有用户可见文本(讲解/深度/问答/作者bio/藏品简介)在语言 X 下必须真的是语言 X,**零混杂**。经语言一致性闸(`lang_detect.text_in_language`:非拉丁按目标字形占比、拉丁用 lingua 识别)校验;不符→gpt-4o 重译→仍不符 `needs_review` 不发布(宁缺毋滥)。**语言无关**:检测候选集派生自 `DEFAULT_LANGUAGES`,加语言自动覆盖。取代此前查中/法语的打地鼠补丁。加语言必过质量清单的语言一致性检查点。
> - **显示名真相唯一化——标题+作者名+馆名(2026-07-05 定标题;2026-07-17 #277 扩作者名;2026-08-24 #382 扩馆名)**:显示名是正文称呼的**唯一真相源**——藏品标题=`title_i18n[lang]`,**作者名=`artists.name_i18n[lang]`**。所有内容通路(讲解 guide / 深度模块 / 建议问答)在正文里引用本作品标题或称呼作者时**必须跟随显示名**——翻译时把两者作为 glossary 注入(`translate_object(titles=, artists=)` / `suggest(titles=, artists=)` / `translate_qa_items(title=, artist=)`)。否则各通路独立翻译会分叉:标题如"显现/幻影";**作者名凡目标语言有多个通行音译即分叉**(真机实证:正文"修拉" vs 作者卡"秀拉",同为 Seurat;高危:Renoir/Toulouse-Lautrec/Courbet 等,音译型语言 zh/zh-hant/ja/ko 均有风险)。en 轴心天然一致(材料带 `Artist: artist_en`)。引用**其他**作品/作者不受此约束(各用各的真相)。
> - **多语言**列表**必须先定集合、再逐语言取标签(2026-09-01 代表作添)**:凡"一组条目 × 十语"的字段(作者卡**代表作**是第一例),**先用与语言无关的判据选定这一组是哪几条,再为每条逐语言取显示名**;某语言没有权威标签时**回退**(字形变体链 → 英文/原名),而不是让这条在该语言里缺席。否则每个语言各自筛选、各自截断,**列出来的根本不是同一批条目** —— 实测莫迪利亚尼作者卡:中文 1 条、西语 4 条、英语 5 条,而且西语那几件和英语没有交集,用户切个界面语言作者卡就换了一批作品。⚠️**兜底必须逐条判、不能整语言一刀切**:某条有权威译名就原样用(那是真相源),只有缺的那几条才翻译。整语言「全翻或全不翻」两种坏结果都会出现 —— 要么覆盖掉权威译名,要么让中日韩用户看到「5 件里 3 件外文」的半截列表(实测样本:禁翻译后 zh 的本地化条目从 34/34 掉到 17/52)。⚠️此处**可以**用 LLM 翻译:代表作是**馆藏外**作品,翻一个画名是翻译不是编造事实;但本馆藏品的标题/作者名仍严格禁止(见上一条)。
> **第三类名字=馆名(2026-08-24 #382 添)**:馆介绍正文提到本馆时跟随 `museums.name_zh`,
> 走同一个 glossary 机制(`translate_section(museum=)`)。**触发条件是馆名字面可译**——
> 小皇宫实测中文介绍写成「欢迎来到**小宫殿**」而馆列表显示「小皇宫美术馆」;
> 奥赛/橘园/卢浮宫因有既定中文译名从未暴露,所以这坑**要上到第四家馆才现形**
> (Grand Palais、Neue Nationalgalerie 一类同理,现已自动受保护)。
> ⚠️ **只钉有真相源的语言**:`zh` 用 `name_zh`,`zh-hant` 复用 `_SCRIPT_VARIANTS` 走
> OpenCC 逐字转(不另配繁体馆名),**其余语言一律不钉**——馆配置里只有 zh/en 两个名字,
> 硬拿 `name_en` 去钉会在日/韩正文里留拉丁残片,而那正是 `_TRANSLATION_SYSTEM` 规则 (1b)
> 明令要消除的(日文实测模型自己写「プティ・パレ」,钉了反而更差)。**没有真相源就别假装有。****存量修复工具**:`scripts/rescan_artist_names.py`(en 段提作者姓而译文不含规范名姓段→删段带 glossary 重译,音频随段失效;幂等)——上新馆后或发现分叉时可跑。
> - **翻译质量规则必须语言无关(2026-07-05 定)**:改进翻译质量(如"全部译出、不留源语言残片")一律靠 `{lang}` 占位符 / 忠实度闸的失败信号,**绝不硬编语言名单**(如"难句 zh 用 gpt-4o")。否则每加一种与英语高距离的语言(zh/ja/ko/zh-hant)都要重打补丁。教训:gpt-4o-mini 对 zh 稳定漏翻"severed head"类短语(距离近的欧洲语言靠同源词"照抄"绕过),按语言无关规则修则 ja/ko 上线自动受益(#197)。

> **⚠️ 完整性判断按语言维度(2026-07-03 定,一日三错的统摄原则)。**
> 凡是"内容是否已存在"的判断(跳过生成/跳过翻译/显示状态/静态表取值),一律问"**该语言**是否已存在",**绝不问"对象是否已存在"**。同一根子当天犯三次:
> ① 列表 `content_status` 按对象判 → de 视图显 ready 点开空(#146 修:按请求语言判);
> ② 作者 bio "存在即跳过" → 复用老作者时新语言永远补不上(#147 修:生成时语种补齐);
> ③ 静态标签表只配部分语言(#142 修:一次配齐全语种)。
> **推论——复用≠跳过**:任何"生成一次、全馆复用"的实体(作者 bio/显示名/将来的流派介绍等),**每个复用点必须做语种补齐**——en 轴心在、目标语言缺 → 纯翻译补(幂等,已有语种不动)。新馆复制时:凡新增"共享实体"或"存在性检查",按此自查。

**博物馆负责事实,AI 负责讲法。** 每件内容分三层,各守互斥职责,从同一份证据包生成、不重复:

- **第一层 默认标准讲解**(`default_guide`,已上 stage1):识别后首先呈现的"主角",单主线·5拍(钩子→引导观察→为什么重要→必要背景→记忆点)·按热度分档(普通 270-420 / 重点 420-675 中文字)。~1 分钟看懂。
- **第二层 预设常见问题**(`suggested_questions`):3-6 个,讲后推荐 2-3,补头条/模块**没答**的。
- **第三层 AI 自由问答**(`/ask`,规划):多轮,复用证据包,争议必 hedge。

**模块库(`tabs`,深度内容,降级为"更多内容")** —— 各守互斥 lane,**只补默认讲解没深挖的一面**:

| 模块 | 专属职责 | 不讲(交给谁) |
|---|---|---|
| artist | 作者其人(生平/性格/艺术史地位) | 不讲这幅画的丑闻/技法 |
| background | 作品史实链**事件**(何时/谁委托/在哪展/风波/流转) | 不讲"为什么重要"(→significance)、怎么看(→analysis) |
| analysis | **怎么画的(技法:笔触/光色/构图结构)** | 不复述解说已点的符号/题材(那是看什么) |
| significance | 影响与遗产(改变了什么/影响了谁) | 不复述丑闻事件(那是 background) |
| facts | 一个别处没有的趣闻 | 不与上面重复 |

**关键机制(✅阶段2a 已落地)**:① 先生成默认讲解(头条)→ 模块单调用带头条+证据包富属性+锐化 lane"各守职责、深化非复述、只会重复就返空"(生成已从证据包取料)→ 问答最后只补缝。② **动态显示**:空模块不展示(料薄优雅降级,默认讲解是地板)。**深度模块字数按热度分档**(重点件 ×1.5),名作能深则深、薄件天然短;深=用材料具体细节铺开非注水(接地闸+去重兜底)。③ overview 退役(与默认讲解头条重复)。

**证据包(`MuseumObject.evidence_pack`,内容唯一来源)**:每件生成时组装、落库(JSONB,后台中间产物,不进端点契约)。结构 `{facts:[{claim,value,source,topic,tier}], narrative:[{text,source,type}], flagged:[{text,type,source}]}`:
- `facts` = 结构原子(对象字段 + Joconde + Wikidata 富属性 P88/P180/P186/P135… + **关系属性 P4969衍生/P144基于/P941启发/P361系列**→喂 significance/background),`tier`=`wall_label`(进 facts 面板)/`material`(只喂生成),`topic`=lane 提示。
- `narrative` = Wikipedia 作品/作者全文块,标 `mainstream`。
- `flagged` = LLM 抽出的争议/推测/未证实句(`contested/inference/unverified`,供阶段2 hedge)。
- **阶段1 已落地(`build_evidence_pack`)**:富属性按 registry 门控(真实生成走网络);阶段2 才把三层+模块生成切到证据包。Europeana 暂缓(待 key)。

## 识别（拍照→讲解;✅P1 2026-07-03;✅向量引擎+全局端点 2026-07-11）

**原则（通用,任何馆零代码）:**
- **R1 识别接地第一原则**:AI 视觉输出是**查询不是答案**——候选名必须匹配到真实目录记录才展示身份;不中就诚实"未收录",绝不把模型猜测当事实。(向量检索天然接地:命中的就是目录参考图。)
- **R2 墙签 OCR=权威接地源,但是增强不是依赖**:照片常无签是常态,主路靠画面内容;"引导补拍说明牌"是未收录 UX 的组成部分(`mode=label` 纯转写)。墙签行同时做**馆藏号精确匹配**(归一化后≥3位,编号=馆方主键,命中即满分)。
- **R3 三档呈现**:高置信(≥HIGH)直开讲解页 / 中置信确认卡(1-3 候选带缩略图) / 低置信未收录+引导拍签。**确认卡=免费人工标注**(真实照片→确认QID,数据飞轮)。**GPT 兜底链命中一律走确认卡不直判(同名撞车实证:自画像/The Bathers 精确同名撞他馆无关件),直开讲解页只来自向量高置信(像素证据)。**
- **R4 引擎可替换,匹配/接地层是不变核心——✅已兑现**:主引擎=DINOv2 ViT-S/14 向量检索(参考图库余弦近邻;benchmark 非名作 Top-1 95.8%/库外零误接受,见 2026-07-11 bench spec);GPT-4o-mini+墙签 OCR 降级为**兜底**(无参考图/低分件;调用量从每张必调降为仅 miss)。引擎任何不可用形态(模型缺失/坏图/表空)→ 自动走兜底,绝不 500。
- **R5 需求自适应**:未收录拍摄记 `recognition_demands`(按 馆+感知哈希 幂等计数,全局识别时馆可空;墙签文字=高质量需求) → 目录跟真实需求生长。
- **R6 足迹 vs 归属两轴分离**:到访足迹用物理位置(GPS/App 选馆),绝不用识别到藏品的拥有馆(借展场景两轴背离)。后续落地,原则先立。

**端点(两个,响应同形):**
- `POST /api/v1/recognize`(**2026-07-11 新增,App 主入口**):multipart `image` + query `museum`(slug,**可选**) + `language` + `mode`。不传 `museum` = 全局识别(拍前无需选馆——导航栏识别按钮直达相机);传了 = 只搜该馆。
- `POST /museums/{slug}/recognize`(保留,行为不变):馆域内匹配。**⚠️ 馆域调用不做全局回退**(2026-07-11 裁决:老 App 不读 `museum` 字段,跨馆命中会拿他馆 qid 撞详情 404 死胡同——前向兼容;将来带馆提示要"馆内优先+全局回退"时加显式参数再开)。

响应:`{outcome: match|candidates|unrecognized, match:{qid,title,artist,thumbnail,confidence,museum}, candidates:[{…,score,museum}], label_text, reason, phash}`
——**`phash`(2026-07-13 加法字段)**=该次识别的感知哈希,配套 **`POST /api/v1/recognize/confirm`**(`{"phash","qid"}`,可选 Bearer,永远204,fire-and-forget):确认卡点选时上报,回填 recognition_events.confirmed_qid——**确认卡=标注飞轮**从此有服务端落点(KPI 的 hits=match+确认候选;match 直跳不上报,confirmed 语义=用户确认)。识别每次调用落 `recognition_events`(只存 phash 不存照片;engine 记 vector/vector_crops/text/cache)——**KPI 与展陈证据一表两吃**。museum pack 加法字段 `catalog_count`/`archive_count`(馆页双数字"在线图录N·档案M",数据源=museums.stats,coverage-report 回写)。
——**`museum`=归属馆 slug(2026-07-11 加法字段)**,前端跳详情用它(老 App 不读不炸;新前端解析 `as String?` + 回退)。`outcome/reason` 机器码不译;`title/artist` 按 language 走显示名规则;thumbnail=thumb 档。命中跳详情 → 懒生成/懒翻译/懒补图自动接管。同图重复识别走 Redis 缓存(命中 30 天/未收录 1 天;键空间 `recog3`)。**阈值 server-driven**:向量档 `RECOG_HIGH=0.85`/`RECOG_LOW=0.72`(环境变量,由 benchmark 校准,库外零误接受);文字兜底档沿用 HIGH=0.85/LOW=0.5。

**低分自动多裁剪重查(2026-07-11)**:全图向量分 < LOW 时(即原本注定失败的照片)自动裁 中心60%/35%+四象限+左右半幅 批量重查取最大分,再不中才进 GPT 兜底。**正常近拍照片走快路径零延迟影响**;触发裁剪的照片 +~2s,仍快于其下一站 GPT(2-5s)。已知代价(bench 实测):库外照片经多裁剪误出候选卡比例升高(48%→81%)——确认卡"都不是"出口兜底,可接受。

**⚠️ 识别优化的优先级原则(2026-07-12 定,真实用户测试得出)**:**App 用户是"有意图取景"的**——想识别某件作品的人会主动找能看清整幅作品的角度,拍歪了自己重拍;合影/极偏构图不是主流场景。Commons 随手拍照片测出的失败案例会高估怪构图的真实占比。**故:不再对怪构图做裁剪/阈值工程加码**(已合并的多裁剪留作免费保险丝);识别精度投入主线=**二维靠参考图覆盖,三维靠多视角图库**(建设策略并入藏品覆盖率机制,另立 spec)。

**识别参照库(与目录共生)**:参考图入库(物化)即嵌入(DINOv2 向量落 `object_embeddings`,生成一次永久落库,model 字段版本化);存量用 backfill CLI。**雕塑多视角**:Commons 分类(P373)拉他人照片入 `role=view`(≤4张/件,Special:FilePath 规范 URL 保署名链),物化即嵌入——3D 单视角是 benchmark 实测短板(真实照 Top-1 ~33%)的对症药。**view 自动治理(2026-07-13 落地,纯自动无人审)**:Commons 分类混入非本体图(orsay 实测 14%)的清洗策略——view 与该件正面照相似度 **<0.25 自动删**(实测全为垃圾)/**0.25-0.4 隔离**(`role=view_quarantine`:留档、不进索引、不上图集、不计有图)/**≥0.4 入索引**;物化嵌入钩子同规则前置闸(新图入库即治理);错杀极端角度由照片飞轮(Phase 3)补回。**上新馆配方(2026-07-13 全串)**:`catalog`(含无图条目)→`names`(⚠️ 首次收全条目时为数千件×10语回填,Wikidata 限速下**约数小时级**——须在服务器侧 nohup 脱管跑,orsay 实测 4800 件≈8h;增量重跑只处理新件,分钟级)→`images`→`backfill_embeddings`→`onboard views`+`images`→`vet_view_images`(执行)→`display-evidence --museum <slug>`(区域适配器,可选)→**`intro`(馆介绍+封面)**→**`coverage-report`(收官:分层数字/展陈分布/KPI,回写 stats)**。
> **馆介绍=门面类预生成内容(2026-07-18 定,spec museum-intro)**:同"图=门面必须预物化"侧(馆页高频入口,总成本封顶=馆数×几分钱),不走懒生成。`onboard <slug> intro` 幂等**按语言维度补缺**(en 轴心在→只翻缺语;加语言重跑自动补),gate 不过=该语言不落(无 needs_review;宁缺毋滥),语言集 `resolve_languages(cfg.languages)` 馆配置驱动。
**⚠️ 材料源 QID 可单配 `intro_qid`(2026-08-24 #381 添)**:介绍材料走 `qid → enwiki
sitelink → 全文 extract`,但**馆本体未必有 enwiki 条目**——小皇宫的馆
`Q59546080` 只有 commonswiki,英文那篇《Petit Palais》挂在 `P276` 指向的**建筑
`Q820892`** 上(那篇写的正是本馆)。缺省 `None` → 回落 `wikidata_qid`,存量馆零影响。
⛔ **刻意不做自动跟随 P276**:P276 对小皇宫指向建筑,但对别的馆完全可能指向**城市**,
那会拿城市的维基文章当馆介绍的接地材料——**还能骗过接地闸**(它确实"有材料支持"),
写出来的却不是这个馆。撞「正确性靠构造,不靠脑补」。哪个条目写的是这个馆,
是上馆时的一次性人判断,**配置里写死比运行时猜更诚实,也留住审计线索**。**封面=后端 LLM 得体性筛选**(server-driven:选错改后端即生效免 Play 审核;top-N 有图件逐件纯文本判定,古典/宗教/神话裸体=艺术惯例可,写实露骨性描绘如《世界的起源》否决)。**绝不碰开放时间/票价**(易变运营数据,AI 不脏补;也保零代码上新馆)。
**计费(2026-07-04 定)**:`match/candidates` 扣 1 次配额;`unrecognized` 不扣(不为失败付费);缓存命中不扣;配额用尽 → **402** `{reason: quota_exceeded}`(先于 GPT 调用,不烧钱)。身份=Bearer 令牌(App 自带)或 `device_id` 参数;两者皆无 → 401 `{reason: identity_required}`。服务端扣费,不再依赖前端自觉调 `/payment/consume`。

**老端点 `/api/v1/recognition` 标记 deprecated**(裸 GPT 猜测当事实,违反 R1;留给老 App,新 App 一律走新端点)。

## 付费墙(现场体验收费,内容永远免费;✅2026-07-28)

> 📎 **完整的身份/权益/付费墙架构见 [`identity-and-paywall.md`](./identity-and-paywall.md)**
> —— 19 条可检验的不变量、端点闸门矩阵、已知取舍、商店适配端口。
> 本节只留与馆 API 直接相关的部分;**改付费相关代码前先看那份**。

**⭐ 唯一执行点在后端,前端 UI 只是它的表达。** 客户端闸只为省一次往返;
老 App / curl / 改客户端都能绕过前端,所以每条产音频/产识别的路径都必须自己过闸。

**通票有 `scope`,按馆的 `city_en` 校验** —— 巴黎通票不解锁马德里的馆。
商品目录(`PASSES`)是时长/scope/SKU 的单一真相源,新增 SKU = 加一行(详见另一份文档 §3.1)。

**免费层**(游客与注册用户完全一致,按 `user_id` 索引;游客=真实 User 行 `is_guest=True`):
- 免费额度 **5 次**(真相源 `settings.FREE_RECOGNITION_QUOTA`,别在任何地方写死数字)。
  它是**一个池子**:拍照识别扣它,在藏品页主动解锁语音也扣它 —— 所以"免费能体验多少"
  只有一个数字,不必维护第二个。
- 语音 = **解锁过的那些作品**的主讲解段,可无限重播、永不弹墙
  (2026-09-19 起;清单存 `user_benefits.free_audio_qids`)。两条解锁路径:
  识别成功当场解锁(写在扣费的同一处);或用户在藏品页**显式确认**后花 1 次额度解锁。
- 浏览/搜索/**完整文字讲解**/预设问答 → 永远免费,不需登录

**必须过闸的端点**(新增任何产音频/产识别的端点,先问它在不在这张表里):

| 端点 | 闸 | 备注 |
|---|---|---|
| `POST /recognize`、`/museums/{slug}/recognize` | 额度 | match/candidates 扣 1;未识别与缓存命中不扣 |
| `GET /museums/{slug}/objects/{qid}/audio` | `audio_access` | 402 `pass_required` |
| `GET .../audio/stream` | 同上 | ⚠️ 播放器直取、**不经 Dio 拦截器**,前端须自带令牌 |
| `POST /content/tts/generate` | 同上 / 通票 | section 模式按 qid;ad-hoc(任意文本)需通票生效 |
| `POST /entitlements/audio/unlock?qid=` | 额度 | 花 1 次额度解锁该件主讲解段;**先扣费再解锁**;幂等;额度为 0 → 402 |

**⛔ 内容端点绝不下发 `audio_url`。** `/objects/{qid}/content` 无鉴权(正文免费是策略),
发直链等于拆掉付费墙 —— 音频 key 做成不可推测也白搭,是我们自己发出去的。
只给 `has_audio` 布尔:前端据此显示播放按钮,要听走上表的闸门端点。

**闸门实现的纪律**(都踩过):
1. **判定无副作用**。闸跑在 TTS 生成之前(对的,否则钱已花),而生成可能
   404 无正文/409/503 —— 闸门若顺手写库,用户什么都没听到、权益却变了。
   ⚠️ 旧的 "claimable + 送达后认领" 两档机制已随纪律 5 整个删除,但这条约束留着。
2. **免费额度只有一个数字**(2026-09-19)。此前「免费试听 1 件」与「免费识别 5 次」
   是两个互不对齐的数字:用户识别第二件、点播放被弹墙,观感是"那 5 次是假的"。
   新增免费维度前先问:它能不能**跟着已有的那个数字走**?
3. **「数据行还没建出来」≠「没有权限」**(2026-09-19)。`user_benefits` 是**懒建**的
   (首次 `/payment/benefits` 或首次识别才建)。闸门当年把 `benefits is None` 判成
   denied,而**真实的新注册用户正是没有行的那一类** —— 每个新用户的首体验都是付费墙。
   实证:prod 78 个账号里 35 个没有这一行;免费试听上线至今,19 个用过识别的账号
   认领数 **0** —— 这个功能一次都没成功过,而三道测试全绿。
   ⚠️ 纪律 5 之后同一状态改判 denied 了,而那**不是**退回老坑:解锁与建行现在
   发生在同一处(`get_or_create_benefits`),"有权限但行还没建"从结构上不存在了。
   通则仍然成立:判权限前先分清"缺数据"与"无权限"。
4. **客户端前置闸只为省一次往返,判据必须与服务端同一套**。它是这道闸的**表达**,
   不是第二套规则 —— 抄一份就会抄歪:前端曾把「还没认领」判成「没有权限」,
   于是请求根本发不出去,服务端准备好的 `claimable` 永远等不到人。
   前端那侧同一个判断出现在多处时(拦截/自动播/UI 标),**只能有一个实现**;
   各写各的会出现"自动播说可以、拦截说不行",两边的单测还都是绿的。
5. **发权益前先扣费,不能反过来**(2026-09-19)。`/audio/unlock` 若先写解锁清单再扣额度,
   额度为 0 那次就是**权益已发、费没扣** —— 而这条路径正是免费用户撞墙后走的,
   白送的正好是我们要卖的东西。同理:扣费成功后解锁失败宁可重试,也别把顺序倒过来。
   配套:**花额度这件事必须用户显式确认**(同 `/activate`)。静默扣掉一次,用户在
   一件无所谓的作品上花完、走到真正想听的那件前才发现 —— 这是差评来源。
6. **权益判据别挂在会被别的功能清空的数据上**(2026-09-19)。"免费语音跟着识别走"
   最自然的实现是查 `recognition_events`(那张表已有 user_id + qid),但
   **删足迹 = 把该表的 user_id 清成 NULL** —— 用户一做隐私操作就连带没收了
   已经花额度换来的权益。因此解锁清单另存 `user_benefits.free_audio_qids`。
   ⚠️ JSON 列**原地 append 不会被 SQLAlchemy 标脏**,更新会被静默丢掉;整列重新赋值。

**身份**:`user_id` **一律取自令牌**,永不取自请求体/查询参数(此类 bug 犯过两次:
读别人权益、烧别人的票;以及权益 user_id 存成设备号导致付了钱看不到票)。
注册/Google 登录必须让游客**就地转正**(同 UUID),新建行会让已购通票凭空消失,
且恢复购买救不回(收据幂等返回旧记录、不给新用户发权益)。

## 搜索（打字查找;✅2026-07-13;稳定契约+可替换引擎）

识别的姊妹功能,也是**文字可识别层无图 stub 的主可达路径**(与浏览列表隐藏无图件相反——搜索是无图件主入口)。**打字搜索 ≠ 识别匹配**:识别是整串模糊相似(GPT/向量给完整候选),搜索是子串/前缀/分词命中(用户打"梵高"/"moulin"/"RF1668");搜索匹配独立设计,仅复用 matcher 归一化工具。

**两端点(加法,响应同族)**:
- `GET /api/v1/search?q=&language=&limit=20`——全局(探索页),响应含 `museums`+`objects` 两段。
- `GET /api/v1/museums/{slug}/search?q=&language=&limit=20`——馆域(馆列表页),只有 `objects`;馆不存在 404。

响应:`{query, museums:[{slug,name,city}](仅全局), objects:[{qid,title,artist,year,thumbnail,museum,has_image}]}`。`title/artist` 走既有显示名规则(`_resolve_name`,全语种);**无图 stub 照常在 `objects`(`has_image:false`,`thumbnail:null`),前端类目占位图**;`museum`=归属馆 slug(点击跳讲解页,与识别 match 同款导航);空 `q`/无结果 → 诚实空态 `objects:[]`。全语种搜(藏品 `title_i18n`+作者 `name_i18n` 全语种进索引,中文界面打 Van Gogh 也中)。

**稳定契约 + 可替换引擎**(照搬识别架构):用户可感知行为由契约锁定、跨引擎不变;引擎可插拔。**首发=进程内索引**(`search/inprocess.py`,单一真相源=Postgres、零同步,馆域索引 TTL 600s 缓存;三路匹配:编号精确 1.0>标题前缀 0.8>标题子串 0.6>作者子串 0.4,同分按 popularity)。**规模触发换引擎**(藏品越过 ~5-10 万件/卢浮+蓬皮杜级别,或实测延迟退化)→ 自托管 Meilisearch/Typesense(CJK 分词好、百万级即时);换引擎只换 `search()` 实现,**契约与前端零改**,唯排序权重可微差、"能搜到什么"一致。

**识别未收录→搜索闭环**:相机识别兜底的「展签检索」sheet 即全局即时搜索,命中点选→讲解页(识别与搜索兜底互通)。

## 用户反馈（内容质量的兜底环;✅2026-09-18 #578/#579）

> 📎 设计与取舍见 [`../superpowers/specs/2026-09-18-user-feedback-design.md`](../superpowers/specs/2026-09-18-user-feedback-design.md)。

**为什么它属于契约而不是一个杂项端点**:CLAUDE.md 的 AI 内容质量原则写着「人只审形式安全、
不审事实对错,**错漏靠用户反馈兜底**」。生成侧的闸(接地/忠实度/语言一致性/锐度/响度)全建了,
这是**消费侧唯一的入口** —— 没有它,那条原则的后半句没有实现。

**加法端点**:`POST /api/v1/feedback` → **204 无响应体**(成功什么都不返回)。

**一条管道吃两类反馈,靠 `scope` 分**(不拆两个端点:共享几乎全部字段、同表、同护栏):

| `scope` | 含义 | 必填 | `kind` 取值 |
|---|---|---|---|
| `object` | 某件藏品的内容/音频有问题 | `museum_slug` + `qid` + `language` | `content_wrong` `audio_bad` `audio_missing` `other` |
| `app` | App 整体意见 | 无坐标 | `app_crash` `recognition_bad` `feature_request` `other` |

其余字段全可选:`text`(≤1000)、`device_id`、`app_version`、`platform`。
**`scope=object` 缺任一坐标 → 422 拒收**:没坐标的藏品级反馈是垃圾数据,存下来也定位不到行。

**🔴 `language` 必须传 API 语言参数,不是 UI locale**(繁体是 `zh-hant` 不是 `zh`)。
10 语 = 10 份独立内容(独立生成/翻译/音频),带错语种 = 让人去修错的那一行。

**匿名可写**(Bearer 可选):最需要反馈的时刻往往正是游客态,要求登录等于把它挡在门外。
带了令牌就认人;**令牌过期/无效不报错,当匿名收下** —— 不能让用户填好的文本因为令牌过期白丢。
`user_id` 一律**由后端从令牌取**,前端不传、传了也不信(与付费墙同一条纪律)。

**`kind` 是用户能感知到的现象,不是技术分层。** 绝不让用户选「文本问题 vs 音频问题」:
他不懂这个分层,而且会选错 —— 繁体念错那次文本是对的、TTS 念错了,用户只会说"读得很怪"。

**去重在读侧,不在写侧**(`scripts/feedback_report.py` 按不同 `device_id` 计数)。
双击提交/超时重试会造重复行,但"第三个人报同一件事就浮上来"这个排序数的是**多少人报过**,
不是多少行;写侧加幂等键更贵且更不准。

**限流 60/hour**(不是 `guest_login` 那种 5/hour):博物馆共享 WiFi 下同出口 IP 多人反馈是真实场景,
而这个端点写坏了最坏后果是垃圾行(SQL 能批量清),不是账号/资金风险。
⚠️ 按 IP 限流要**真实客户端 IP** 才有意义,见 `app/main.py` 的 `TRUSTED_PROXY_HOSTS`(#577 之前全站共享一个桶)。

**⚠️ 自由文本 = 个人数据**:`text` 是用户手写内容,上线前隐私政策必须有对应披露段。
凡加「用户能自由输入」的字段,连带三件事一起做:**政策披露 / 删号断链 / 数据导出包含**。

## 路线图（执行顺序;每阶段 = 独立 spec→plan→实现,完成回写本文）

**✅ 已落地**:目录主干 + 4 端点契约 + 分类 facet + 作品信息 facts;接地·引人入胜生成 + 三类闸 + 多语翻译 + 建议问答;默认讲解 stage1(staging)。

- **阶段 1 — 材料地基(证据包)**:1a 源配全(Europeana + 更全 Wikidata P 属性;法国官方=Joconde 已有,非法国馆逐馆官方连接器)· 1b 证据包模型 + 分类落库。→ 每件一份完整分类证据包。
- **阶段 2 — 内容生成重构**:2a 统一分工去重(lane)+ 全从证据包生成 · 2b 动态模块 + 争议 hedge · 2c 两段式质量评估(事实质量 + 讲解质量)。
- **阶段 3 — 体验补全**:3a AI 自由问答 `/ask`(多轮)· 3b TTS 音频落库 · ~~3c 懒生成接线~~(✅落地)· ~~藏品识别 P1~~(✅2026-07-03 落地,见§识别;P2=CLIP 前置+需求聚合+足迹)。前端增强(生成中提示/自动刷新/识别相机流程)见交接文档。
- **阶段 4 — 规模化**:图像 R2 自存 + `cover_url` + 首页真实化 · 上新馆(逐馆官方连接器 + 纯配置)· **官方馆藏库连接器**(抓元数据+在展清单/展厅号,不抓图;用"在展×有自由图"校准优先级,修正名作偏置)。

**奥赛 = 样板馆**:作为先行者会多吞一两次重生成;流水线成型后,新馆从第一天即"配全·生成一次·封板"。

---

## 变更记录

- 2026-09-26:**⛔ 跨馆作者事故,新增纪律 37「共享实体只补缺不覆盖 + 备份范围=真实写入面」**。
  为调小皇宫 guide 文本对 TOP20 跑了三次 `generate --force`,顺带把 14 位跨馆作者(`artists`
  全库共享)的简介重写、6 位作者 40 条简介音频从库里脱钩、库尔贝代表作与安格尔中文名被改写,
  卢浮/奥赛/橘园的作者卡一起变。动手前只备份了作品表。用当天 04:20 `pg_dump` 逐字段恢复
  14/14(独立比对零差异,R2 音频文件都在)。代码:`--force` 不再参与作者重写判断,已有作者字段
  只补缺;原测试 `force_refreshes_artist_bio` 断言的正是事故行为,已改写为反向钉住。
- 2026-09-20:**候选态不再当场扣费,扣费点挪到用户确认之后**。
  `POST /recognize/confirm` 从纯埋点端点变成**候选态唯一的扣费点**(可选 Bearer,
  首次确认扣 1 次并解锁该件语音;仍恒 204)。起因是用户问「选『都不是』为什么也扣次」——
  `candidates` 正是"识别器没把握"那一档,返回的瞬间就扣,与计费规则自己写的
  **"不为失败付费"直接矛盾**:用户为一个他当场否掉的答案付了钱。现在
  match 当场扣、candidates 等确认、「都不是」一次不花。
  三条配套约束(都由测试钉住):
  ①**幂等看"该 phash 24h 内有没有任何一行已确认"**,不是"最新那行确认没有" ——
  重拍同一张图会命中识别缓存并再落一行事件,只看最新行就会重复扣。
  ②**只在首次确认时解锁**:事件行只存 `top_qid`,服务端无从知道那次识别给了哪几个候选,
  重复确认还放行的话 qid 可以任填 = 一次额度解锁全馆。改选的那件走详情页手动解锁。
  ③候选态**扣费与解锁必须一起挪走**:只挪扣费、照旧解锁 = 反复拍到候选就白拿语音。
  已知取舍:从不确认的候选请求不计费,因此"反复拍到候选"能绕开计数 ——
  与删号重注册刷额度同源(设备身份可刷),根治要靠 Play Integrity,MVP 接受。
  同批修**闪光灯从来没被关过**:`CameraController` 没调过 `setFlashMode`,
  插件默认 `FlashMode.auto` → 暗展厅每拍一张都闪,而取景框里并没有开关可关它
  (很多博物馆禁闪)。初始化后显式 `FlashMode.off`。
- 2026-09-19:**免费额度合成一个池子 + 额度 UI 两页口径统一**(接着下一条)。
  真机截图:同一秒钟设置页写「剩余 2/5 次」、权益页写「3/5」(已用) —— 用户当场问
  哪个对。两页现在共用 `quotaValue` 这一条串,进度条也统一成**递减**(满→空)。
  同一批把免费额度泛化成一个池子:**拍照识别扣它,在藏品页主动解锁语音也扣它**。
  起因是上一条收紧后,从列表/搜索进来的人一次语音都听不到 —— 他手里明明还有额度,
  只是这件没拍过(玻璃反光、人多、或压根没图没法识别)。新端点
  `POST /entitlements/audio/unlock`,**先扣费再解锁**、幂等、必须用户显式确认(→纪律5)。
- 2026-09-19:**免费语音改为跟着识别走**,音频闸门补纪律 2/6、纪律 1-3 随之改写。
  上一条修好了「免费试听一次都没发生过」,但真机再跑一遍就暴露出规则本身讲不通:
  **免费识别 5 次、免费语音 1 件** —— 用户识别第二件、点播放被弹墙,
  观感是"那 5 次识别是假的"(报这个问题的人自己都把它记成了「识别被拦」)。
  新规则只留一个数字:**识别出来的作品,主讲解段都能听**,上限天然等于识别次数。
  实现上是净减法:"claimable + 送达后认领"两档机制整个删除(闸门只剩 allowed/denied、
  不再写任何库),解锁挪到识别扣费的同一处 —— 额度在哪花,就在哪记账。
  顺带故意放宽了语言维度:旧规则绑死 (作品,语言,段),中文识别完把 App 切成法语
  就撞 402,而那个人什么都没多拿。
  ⚠️ 上线顺序**后端先行**:新前端读 `free_audio_qids`,老后端没这个键 → 空表 → 全锁。
  反过来老 App 读到的 `free_audio_qid` 恒为 null,本地闸放开、由服务端 402 兜底,
  退化方向是对的(所以这条后端改动对已装的 v33 立即生效,不必发包)。
- 2026-09-19:**免费试听从没成功过 + 一次识别扣两次额度**,音频闸门补纪律 3/4。
  真机报的是「新账号一次免费试听都没有,直接要买通票」,查下去是三件独立的事:
  ①服务端把「`user_benefits` 行还没建」判成「没有权限」(→纪律3);
  ②客户端把「还没认领」判成「没有权限」,请求根本发不出去(→纪律4);
  ③前端识别成功后仍调 `/payment/consume`,而服务端 2026-07-04 起已自己扣
  (见本文件「计费」段最后一句)—— **一次识别扣两次,5 次免费实际 2.5 次**。
  ①②各有测试且**全绿** —— 自动播准入与拦截各抄了一份判据,分头判、分头绿,
  合起来是「识别完自动播,然后弹墙」。prod 实证:19 个用过识别的账号,认领数 0。
- 2026-09-19:**`POST /feedback` 回写契约**(#578/#579 落地时漏了这一步)。它不是杂项端点 ——
  「人只审形式安全,错漏靠用户反馈兜底」这条原则的后半句此前没有实现入口。
  同时钉住三条容易写错的:`language` 必须传 API 语言参数(`zh-hant` ≠ `zh`)、
  `user_id` 只从令牌取、自由文本字段连带政策披露/删号断链/导出包含。
- 2026-09-14:**小皇宫首批生成的验证复盘,新增纪律 32-35**(#552/#554/#555/#557/#560)。「看图写外观描述进材料」这条路跑通并上 prod(技法段过闸 10/10、接地 0.83-1.00),但**三处缺陷全是重跑真实数据 + 对着原图核对抓出来的,没有一处来自测试或 CI**:①开头断裂判据误杀 17 段好内容(→32,含「测试要能区分修好了与修过头了」)②签名转写往材料里注入假事实(→34)③英文正文改了内容译文不跟着失效(→33)。并补记 prompt 禁令的位置纪律(→35,guide 结尾与看图转写两次实证)。
- 2026-09-12:**Joconde 断供复盘,纪律 31「容错必须配探活」**(#529/#530/#531)。
  老 API 断供两个月无人察觉,根因不是源死了而是**容错把死讯咽掉了**;
  配套两条方法论:探针走真生产调用路径(才探得到"改列名"这种不抛异常的死法)、
  外部依赖的时间线信自己的库不信公告(公告日 06-01 vs 实际断供 07-22,差 7.5 周)。
  存量补课只写 `attributes` 不碰 `object_content_sections` ——
  展签面板是实时从 attributes 算的,补属性即可复活面板,
  而重生成正文会清空 `audio_key` 让已发布内容静默变哑。
- 2026-07-30:**第四馆(小皇宫)上线,配方全程零核心改动** —— 本次上馆同时是一次配方验证。
  只动两个文件:`museums.yaml` 加一段 + `category_config.py` 加 4 行 P31 映射
  (圣像画→painting、三种版画→works_on_paper;77 件占 4%,不加则落 unknown 走
  `_FALLBACK`,讲解仍出但分类 facet 归不了位)。**卢浮宫那轮需要的三样一样没用上**
  (`collect_all_types` / `collection_qids` / 20 余行映射),且卢浮宫暴露的规模坑
  (外部硬上限、自由文本超列宽、names N+1、全量返回、缓存冷建)这次一个没再踩 ——
  它们已是管线默认行为,新馆自动受益。
  配方补一条:**`fetch_limit` 是 SPARQL 行数不是件数**(见 §上新馆第 1 步)。
  ⚠️ 同日 prod 实跑后**更正**了这条:当时按 staging 小样本推出的"3.3 行/件"是错的 ——
  那是「去重 1.05 行/件 × 有图率 52% × 类目过滤」的合成比率,把三件事混成了一个数。
  真实去重比只有 **1.05**,且查询口径含无图件。预检基准也一并改为按 catalog 真实
  口径(P195 + P31 在白名单 + 有图)数,只数 P18 会高估(1884 vs 实际落库 1663)。
  实测记录:catalog 15 件分类全对、names 首轮 WDQS 429 出 2 errors 幂等重跑归零、
  images 15/15、generate en 3 段+zh 4 段、四个端点中文完整(连馆藏编号 PPP3130 都有)。

- 2026-07-29:**分节修错位**——13 条纪律里有 4 条(⑨ shell 编排 / ⑩ 判进程 / ⑫ 不可逆批量护栏 /
  ⑬ 替换过质量闸)与博物馆、与语言都无关,却挂在「上新馆 = 纯配置」底下。上第二个馆的人
  读到第 9 条就开始跳读,而跳读是这类文档失效的开始。→ 提成同文件内的独立一节
  **§工程与运维纪律(与馆无关)**,上馆手册只留 ①-⑧⑪。**编号一律不变**:`material.py` /
  `batch_names.py` 的注释与 plans/specs 都按号引用("纪律①""纪律⑥"),重排会让它们全部指错。
  只搬位置,一个字没改。

- 2026-07-29:**实战纪律补两条通用运维原则**(PR#363 回写)。⑫ 不可逆批量操作的五条护栏
  (默认 dry-run / 宽限期 / **引用集合为空必中止** / 删除占比上限 / 先列存储再读 DB;
  引用集合必须覆盖全部存 key 的地方——音频 key 在三处,`artists.bio_audio` 最容易漏);
  ⑬ 替换已上线产物必须过质量闸、不过闸就保留旧版本(模型回归会**静默**毁库)。
  付费墙侧的三条钱路径不变量(I17/I18/I19)与 §3.1 商品目录写入
  [`identity-and-paywall.md`](./identity-and-paywall.md),本文不重复。

- 2026-07-28:**付费墙落地 + 堵四条旁路**(PR#352-357)。新增「付费墙」章节:唯一执行点在
  后端、必须过闸的端点表、闸门两条纪律(判定无副作用/拦了必须认领)、身份一律取自令牌、
  游客就地转正。**破坏性变更**:`/objects/{qid}/content` 的 `audio_url` → `has_audio` 布尔
  ——内容端点无鉴权,发直链等于拆掉付费墙;且那些直链在 P0-b 迁移后已全是死链(实测 404),
  移除不会让任何**当时能用**的东西变得不能用。同批下线 `/api/v1/recognition/recognize`
  (不鉴权不计费的裸 GPT 后门,交接文档 2026-07-03 已判"将下线")→ 410。

- 2026-07-20:**馆封面/简介质量收尾**(spec museum-cover-intro-quality,前端真机实测发现三处)。
  `GET /museums` 加法字段 `cover_image`(探索页列表缩略图,此前完全无图数据可用);
  `select_cover` 改为**建筑外观优先**(Museum.qid 的 Wikidata P18,不过安全闸——架构零裸体风险且无
  title/artist 可喂),无建筑照/下载失败才回落藏品图逐件得体性判定(原逻辑不变);
  简介 prompt 改 2-3 短段落(`\n\n` 分隔),存量馆(orsay/orangerie)需 `force=True` 重跑才吃到新版。
  ⚠️ 前端交接文档提及 `opening_hours`/`official_url` 两字段——**故意不做**:属易变运营数据,
  撞"AI 绝不脏补"红线(museum-intro 原则);前端已用 `as String?` 优雅处理永远 null,无需后端补。
- 2026-07-19:**第二家馆橘园落地——"零代码上新馆"实证成立**(spec orangerie-onboarding)。全程唯一改动=museums.yaml 一个条目;配方全链(catalog×2→names→images→物化即嵌入→intro→coverage-report)原样跑通:档案140/图录15/向量16;封面得体性筛选一次到位(卢梭《婚礼派对》);探索页/搜索/懒讲解(zh 18s)全自动可用。**实测上馆成本基准(llm_usage 首份完整账单):全馆 ~$0.60,其中 names(gpt-4o)$0.59=98%**——坐实成本工程②(Batch API 砍 names)是唯一值得的刀。Joconde 馆名须用 POP 精确官方名(橘园="musée de l'Orangerie des Tuileries")。

- 2026-07-18:**博物馆介绍+封面落地**(spec museum-intro)。馆包加法字段 `description`(AI 接地叙事,按语言回退,null 安全)/`cover_image`(得体性筛选封面,可 null);`onboard intro` 命令(复用富化管线,按语言维度幂等补缺,gate 不过不落);封面=后端 LLM 得体性判定(《世界的起源》类否决,server-driven 免 Play 审核);上新馆配方加 intro 步。迁移 q1n3。门面类预生成(成本分界),不碰运营数据。

- 2026-07-17:**staging 轻量化落地**(收录⑥)——护栏(staging 默认小样本/--allow-full)+ prod→staging 搬运两档(slim 金样本/full 拉真)+ 用户表红线。背景:staging 已成 prod 近镜像,十万件×10语全量回填≈百万级强模型调用不可持续。
- 2026-08-24:**第四馆小皇宫收尾,两条只有第四家馆才现形的坑**。①**显示名真相唯一化扩到馆名**(#382)——`museums.name_zh` 成为馆介绍正文称呼本馆的唯一真相源,`translate_section(museum=)` 第三个 glossary 槽位;只钉有真相源的语言(zh/zh-hant,后者走 OpenCC),其余不钉(硬钉 name_en 会在日韩正文留拉丁残片)。触发条件=馆名字面可译,前三馆有既定译名故从未暴露。②**馆介绍材料源可单配 `intro_qid`**(#381)——馆本体未必有 enwiki(小皇宫馆 Q59546080 只有 commonswiki,英文条目在建筑 Q820892);刻意不自动跟随 P276(可能指向城市,拿城市文章当材料还能骗过接地闸)。③配方补 verify「门面响应」项的**预热纪律**(刚部署/刚跑 intro 后按 worker 冷建,会假红 1.3-1.5s,预热后 0.14s)。
- 2026-07-17:**显示名真相唯一化扩到作者名**(#277)——`artists.name_i18n[lang]` 成为正文/问答称呼作者的唯一真相源,glossary 与标题双锁(`translate_object(titles=, artists=)`);修机制级音译分叉(修拉/秀拉)。存量工具 `rescan_artist_names.py`(staging 实跑:92 段+51 问答分叉修复)。原则语言无关、作者无关——新馆多音译作者自动受保护。
- 2026-07-16:**#252-257 批次回写**。API 面加法:`/audio`(懒 TTS)与 `/audio/stream`(流式,单次 tee,⚠️200 双形状按 Content-Type 分支)入端点总览。原则×2:**首屏关键路径原则**(懒生成顺序=请求者最短路径,新增生成步骤不在 guide 依赖链内一律延后;TTFC 45.9s→~15s)、**非 Wikidata 条目身份**(合成把手 `<source>-<ref>` 当 qid 同键用+`is_wikidata_qid` 门控跳 SPARQL+names 走 title_en 轴心)。批处理纪律第 6 条:容器内长任务避开部署窗口+脱会话+日志实时落地("进程消失≠成功",只认 DONE 行+数据核验)。
- 2026-07-13:**搜索功能落地**(spec 2026-07-13-search-feature-design)。API 面加法:`GET /search`(全局,museums+objects 两段)+`GET /museums/{slug}/search`(馆域,仅 objects);无图 stub 可搜(has_image/thumbnail:null)、全语种、三路匹配。原则:**搜索=稳定契约+可替换引擎,进程内起步(零同步)→规模触发换 Meilisearch,契约与前端零改**。前端:探索页升级全局搜(debounce 300ms 分区结果)+馆列表页激活馆内搜+识别未收录→搜索闭环。
- 2026-07-06:加语言 checklist 补⑥字形变体(_SCRIPT_VARIANTS)+⑦强制跑质量验收清单(完整六检查点/全段落扫描;i18n-translation-quality-checklist.md)。
- 2026-07-05:定**标题真相唯一化**原则(显示名=标题唯一真相,内容三通路引用跟随;glossary 注入,修显现/幻影分叉)。
- 2026-07-05:**内容生成 N 默认=0**(收录策略⑤)——上新馆零内容预付,纯懒生成兜底;N 上线后按实际热度/需求由运营逐馆决定。
- 2026-07-04:content 端点加法字段 `generating`(懒任务锁即信号)——前端等待提示三态化(生成中/资料不足/待完善可重试),修盲轮询三隐患。
- 2026-07-13:**覆盖率机制 Phase 1 落地**(spec 2026-07-12-collection-coverage-phase1):§收录策略改**分层收录**(视觉/文字/需求三层,P18 OPTIONAL 收全条目,列表隐形+馆页双数字+用户照片hero);**北极星 KPI 换轴**(验收=现场识别成功率,不看件数);**展陈状态动态化**(attributes.display+证据三层:流量>P276>区域适配器,Joconde样板);**view 自动治理落地**(0.25删/0.4隔离/入库闸,取代dry-run待定);recognition_events 一表两吃(KPI+证据,只存phash);API 面加法:响应 phash+`/recognize/confirm`+pack 双数字;上新馆配方全串至 coverage-report 收官。迁移 p1m2。
- 2026-07-12:定**识别优化优先级原则**(App 用户有意图取景,怪构图非主流,不再加码裁剪/阈值工程;精度主线=二维参考图覆盖+三维多视角库,并入覆盖率机制 spec)+ 记录 **views 污染已知问题**(Commons 分类混入非本体图 14%,vet_view_images 分诊,清洗策略待覆盖率轮;配方加 dry-run 步骤)+ 多裁剪已知代价(库外误出候选卡 48%→81%,确认卡出口兜底)。
- 2026-07-11:**低分自动多裁剪重查**(全景式拍法对症,真实用户实证 0.51→0.84;近拍快路径零影响)+ 批处理纪律第 5 条**空响应≠到底**(WDQS 深 OFFSET 静默截断实证,catalog 漏收 500+ 长尾件;上新馆必核 loaded 数量级)。
- 2026-07-11:**GPT 兜底文字链一律不直判**(命中只出确认卡,不产 `outcome=match`;同名撞车 E2E 实证:自画像/The Bathers 精确同名撞他馆无关件)——直开讲解页只来自向量高置信像素证据;label 模式亦然(多一次确认点)。
- 2026-07-11:**识别主引擎换 DINOv2 向量检索**(R4 兑现:benchmark 裁决 DINOv2 胜 CLIP,非名作 Top-1 95.8%/库外零误接受;GPT+OCR 降为兜底,不可用绝不 500)。**新增全局端点 `POST /api/v1/recognize`**(museum 可选,拍前不选馆)+ 响应加法字段 `museum`(归属馆 slug);裁决**馆域调用不做全局回退**(老 App 前向兼容,跨馆命中=详情 404 死胡同)。参照库=物化即嵌入+backfill;雕塑多视角 view 图(P373 深挖落地);墙签行加馆藏号精确匹配;阈值 server-driven(RECOG_HIGH/LOW 环境变量)。
- 2026-07-04:识别端点**服务端计费**落地(match/candidates 扣1 ——
  ⚠️ candidates 那一半已于 2026-09-20 改成「确认后才扣」,见顶部;
  unrecognized/缓存不扣,超额402;身份=令牌或device_id)——堵住新端点绕过配额的洞,弃用前端自觉调 /payment/consume 的客户端计费。
- 2026-07-04:作者卡 `nationality`/`notable_works` 多语本地化落地(前端交接③;v1 局限解除)。交接①分类标签已由 #142 先行修复;交接②"老件补语种"因 prod 内容清空+六语生成而失效(translate 命令备用)。
- 2026-07-03:**识别 P1 落地**+§识别入契约(R1-R6:接地第一/墙签增强非依赖/三档呈现/引擎可替换/需求自适应/足迹vs归属)。新端点 `/museums/{slug}/recognize`;老 `/recognition` deprecated;P2=CLIP/需求聚合/足迹。
- 2026-07-03:定**批处理纪律**四条(单件容错/分批落盘/外部查询分批+重试/幂等可重跑)——prod names 三次崩溃(ReadTimeout 炸全局、进度全丢、414、502)的血泪成文,全部上馆命令适用(#158/#160 落地)。
- 2026-07-03:**图像 R2 自存落地**(阶段4提前):两档(thumb480/large1600)/多角度(P18全收 primary+view)/署名/懒补漏;image_key=基础键;上新馆步骤加 `images`(第4步);定"图=预物化 vs 内容=懒生成"成本分界。Commons P373 深挖留识别轮。
- 2026-07-03:定**完整性判断按语言维度**原则(一日三错 #142/#146/#147 的统摄:存在性检查按语言问不按对象问;复用≠跳过,共享实体每个复用点做语种补齐)。#147:生成时为已存在作者补齐 bio 缺失语种。
- 2026-07-03:**列表 content_status 按请求语言解读**(该语言无内容→empty"待完善",防列表骗人)+ **懒翻译落地**(ready 但请求语言缺→后台只翻该语言,复用补语种原语+懒生成锁)。
- 2026-07-03:懒生成**请求语言优先**(lang_priority,逐语言翻完即落库,单语言失败不拖垮)+ **补语种命令落地**(`onboard translate`:存量对象缺失语言从 en 段纯翻译,checklist⑤ 闭环;老 243 件补 de/es/it 用它)。
- 2026-07-03:定**本地化完整性原则**——用户可见文本必随 language(固定小集合用静态表配齐全语种,缺译回退 en);机器码永不翻译;"加语言"checklist(五步)。教训:六语开放分类标签只配三语(#142 修)。前端六语选择器 #138-140。
- 2026-07-03:**懒生成落地**(路线图3c):stub 首次访问 → 后台生成(attributes.lazy_lock_at 锁,TTL 10min;并发2;仅部署环境)。组件装配抽 factory(onboard 与懒生成共用)。契约形状零变化。
- 2026-07-03:定**§收录策略**(通用):有图才收录(识别参照+合规)/三层口径(全馆藏≫在展>有图)/定期重跑吃增量/通用分类法8大类(CATEGORY_BY_QID 真相源,多P31已知优先)/生成分层(top-N=奥赛200 只在 prod;staging 小样本;懒生成入路线图3c;官方馆藏库连接器入阶段4)。类目代码统一(photograph→photography、decorative→decorative_arts);奥赛 categories 扩至四大类,catalog P18 必填。
- 2026-07-03:定**显示名解析时机=铺目录时**——新增 `names` 回填命令(title_i18n/artist_qid/Artist名字行,幂等);`_fill_i18n` en 也权威优先;Artist 行存在但 bio 空时 generate 补 bio。修列表页 stub 在 zh 视图显英/法文名问题(截图反馈)。翻译兜底规则经讨论**维持所有语言统一**(权威→机翻→en),不按拉丁/非拉丁分叉。
- 2026-07-03:契约-代码对齐——端点3 `artist` 真正走 name_i18n(此前只 title);facts.medium 优先证据包 P186(修正 P2048 措辞:尺寸仍 Joconde);`country_lang`/`sources` 从 museums.yaml 读(去 France 硬编码,上新馆=纯配置成立)。
- 2026-06-28:新建本活文档。纳入近期加法:端点3 `/objects` 分页;端点2 `categories` facet + language;端点4 `status/title/images/facts`;`content_status` 生命周期;上新馆路径。
- 2026-07-02:多语显示名规则**落地**(title_i18n/Artist.name_i18n:Wikidata多语标签+翻译兜底;端点 resolve_name 语言感知;避 Joconde 脏格式)。
- 2026-07-02:定**多语显示名规则**(权威标签→翻译兜底→en;语言无关,加语言=加配置)入契约。
- 2026-07-02:补记**英语轴心**核心约定(所有生成先英语→接地闸→翻译;新增 prompt 必强制英文)+ 修 generate_artist_bio 漏英文致 bio 语言错。
- 2026-07-02:作者一等实体(artists 表,按 artist QID 生成一次、同作者复用,修每件重复/漏)+ 缺中文标题生成补 + status 按语言判空(修 ready 却空)。
- 2026-07-01:配源round2a——证据包补 Wikidata 关系属性(衍生/基于/启发/系列),给 significance/background 接地影响钩子。
- 2026-07-01:问答去重(QA 收 covered=解说+模块,只问没讲过)+ analysis lane 收到技法(怎么画的,不复述解说符号)。
- 2026-06-30:模块深度——深度模块字数抬高+按热度分档(重点件×1.5,section_target_chars),名作能深则深、薄件天然短、不注水。
- 2026-06-30:作者卡——content 增必选常驻 `artist` 卡(生卒P569/P570·国籍P27·代表作P800 from 作者实体 + artist段叙事为bio);artist 段移出 tabs。
- 2026-06-30:阶段2a 落地——生成从证据包取料(用上 P88/P180 富属性)+ guide 互知去重(模块各守 lane、不复述头条、返空不发布)+ overview 退役(迁移删 category_sections 映射)。
- 2026-06-29(晚):阶段1 证据包落地——`MuseumObject.evidence_pack`(facts/narrative/flagged);端点4 `facts` 策展+人性化(去学术噪音,provenance/exhibitions/bibliography 移出面板)。
- 2026-06-29:升级为**主文档**(契约+内容体系+上馆手册+路线图)。纳入端点4 `default_guide`(默认标准讲解);新增 §内容体系(三层+模块库+证据包+去重lane+动态模块)、§路线图(阶段1-4)、§北极星 + 工作纪律。

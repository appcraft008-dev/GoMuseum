# 奥赛前端呈现 — 想法记录（停车场，待讨论）

> 创建于 2026-06-17。**这不是设计稿，是用户陆续想到的前端呈现改动的记录。**
> 流程：先把想法记全 → 逐个讨论 → **对照后端契约**（`2026-06-17-artwork-content-enrichment-mechanism-design.md`
> §15）→ 判断每条是否**牵动契约（需要新数据/字段/端点）**：
> - **牵动契约** → 把那个**契约改动**回填进机制 spec（后端任务）；UI 仍在前端任务。
> - **纯呈现**（不需新数据）→ 留在本文档，归独立**前端任务**，后端零改动。
>
> 判据见 CLAUDE.md「架构约束与原则」。状态标记：🟢 纯呈现（不碰后端）/ 🟡 可能牵动契约（待确认）/
> 🔴 确定牵动契约（需回填机制 spec）/ ❓ 待讨论。

## 已记录的想法

| # | 前端想法 | 现状 | 预判契约影响 | 状态 |
|---|---|---|---|---|
| 1 | **馆页用 tab 切换不同类别藏品清单**（绘画/雕塑/摄影…） | 打开奥赛只显示"绘画"列表 | 需"按类别筛"的列表数据 → **已在机制 spec §15 契约**（分页+类别筛端点） | 🔴→已覆盖 |
| 2 | **馆藏列表按热门/Top 排列** | — | 需 popularity 排序 → 后端已 `order_by(popularity.desc())`，契约 sort 参数已列 | 🔴→已覆盖 |
| 3 | **列表分页**：每次 50 条，上滑加载下 50（无限滚动） | `/museums/{slug}` 一次性返全部 | 需分页端点 → **已在机制 spec §15 契约**（limit/offset）；滚动 UI 纯前端 | 🔴(端点)+🟢(滚动) |
| 4 | **详情页用 tab 呈现 介绍/作者/背景/故事 等段落** | 现描述很简单 | 需分段 body → `object_content` 已返回有序分段，契约已支撑 | 🟢 纯呈现 |
| 5 | **每个 tab 内有语音播放键** | — | 需各段 audio_url → 契约已返回 | 🟢 纯呈现 |
| 6 | **播完自动跳下一段 tab 并续播下一段语音**（自动连播） | — | 纯前端按 sort_order 顺序连播，后端零改动 | 🟢 纯呈现 |
| 7 | **播放速率 0.75/1/1.25/1.5x** | guide 已有 setSpeed | 纯前端本机变速，后端零改动 | 🟢 纯呈现 |
| 8 | **识别未收录提示页**（"未收录"+反馈入口） | — | 需后端返回"未收录" + 反馈 → 后端逻辑已在机制 spec §12 | 🟡(后端逻辑已覆盖)+🟢(页面) |
| 9 | **入口改向**：馆藏点击 / 识别命中 → 详情页 | 现 push `/guide` 实时重生成 | 纯前端跳转改向；命中服务后端逻辑已在 §12 | 🟢 纯呈现 |

## 待用户补充的想法

（明早/后续用户继续想到的前端呈现，往这里加。每条记：想法、现状、预判契约影响。）

| # | 前端想法 | 现状 | 预判契约影响 | 状态 |
|---|---|---|---|---|
| 10 | **详情页展示"硬事实"**：墙签速览（作者·年代·媒材·尺寸·馆，始终可见）+ 可展开"作品信息"全表（材质/尺寸/编号/现藏地/来源流转/展览史/参考文献/铭文/作者生卒）；叙事讲解 tab 仍为主体 | 讲解页只有叙事，无硬事实 | 🔴 **牵动契约**：`object_content` 现不返硬事实 → 加法式补结构化 `facts`（多语言：数值型语言无关、描述型本地化）。**已回填机制 spec §15** | 🔴→已回填 |

## 讨论结论（逐条敲定后填）

（每条讨论完，结论写这里：纯呈现 → 归前端任务；牵动契约 → 已回填机制 spec 哪一节。）

-

## 已回填机制 spec 的契约改动（对照用）

- §15 契约：分页+类别筛+热门排序列表端点（覆盖想法 1/2/3 的数据面）。
- `object_content` 有序分段 + audio_url + status（覆盖想法 4/5/6 的数据面）。
- §12 识别命中服务 / 未命中"未收录"+记录（覆盖想法 8/9 的后端面）。
